# Project Log (Bitácora)

The project's historical memory — governed by `company/CLAUDE.md`'s "Project
Log" and "Session Recovery Protocol" sections. Read this file in full at the
start of any recovery, before reconstructing current state from the
repository (`git status` + the active artifacts it points to). This file
answers three questions per entry, nothing more: what happened, why it
mattered, and where the full detail lives. It never duplicates the
authoritative artifact — it references it.

Append-only, newest entry last, half a page or less per entry. Never delete
or rewrite a past entry — correct forward with a new one.

## Log

---
**2026-08-04/05 — Experience Review workstream completed**
*Context:* One-time emotional/experiential walkthrough of the
Medium-Fidelity prototype, requested by the Product Owner, expanded same day
into three workstreams.
*Discovery/Decision:* All three closed: (1) Resultados icon/comprehension
audit, 11 findings resolved; (2) the post-sale moment redesigned into a
full-viewport digital receipt, closing a real privacy leak (Ana's daily
revenue was visible to the customer mid-transaction); (3) a connected NFC
activation-to-sale demo chain built end to end.
*Impact:* Closed the "does this feel real" gap the prototype had going into
this week's live usability sessions. Full `ux-critic`/`reviewer` cycles
clean on all three.
*References:* `product/02-ux/experience-review-2026-08-04.md`.

---
**2026-08-05 — Session Recovery failure: governance gap discovered and fixed**
*Context:* A session restart erased Main's visibility into the completed
Experience Review workstream (above) and an existing Merchant Experience
Kit. Main, unaware either existed, re-reviewed `events.md`'s already-"done"
Medium-Fidelity build as if for the first time, and — while remediating
findings from that redundant review — had `ui-designer` delete 3 legitimate
Journey 2 ("existing NFC merchant") demo clone frames, despite `ui-designer`
flagging mid-task that they were real and documented.
*Decision:* Established the Session Recovery Protocol and this Project Log
(`company/CLAUDE.md`) as permanent governance, at the Product Owner's
request — not a one-off fix.
*Impact:* Recovery is now a mandatory, gated procedure before any new
dispatch. `events.md` Journey 2 restoration itself is a separate open item
— see the repository's current `git status` and `product/02b-medium-fidelity/CLAUDE.md`
for its live state; Product Owner decision pending as of this entry.
*References:* `company/CLAUDE.md` (Session Recovery Protocol, Project Log,
Main's role sections); `product/02b-medium-fidelity/CLAUDE.md`.

---
**2026-08-05 — ID001 diagnosis corrected: not a structural MCP block**
*Context:* An earlier entry in `company/infrastructure-decisions.md`
diagnosed subagents as categorically unable to access plugin-provided MCP
tools (e.g. Figma). Direct counter-evidence the same day (successful live
Figma calls across multiple subagent spawns) forced a correction.
*Discovery:* Real root cause is connection-timing flakiness — the Figma
plugin's MCP connection observably drops and reconnects mid-session; a
subagent spawned during a down window gets no MCP tools for its run, with no
reconnect. Not structural, intermittent.
*Impact:* Standing practice changed: run a one-call diagnostic before
assuming any Figma-dependent subagent is blocked; fall back to
Main-as-evidence-provider only if that diagnostic itself fails. This
correction was itself missed at the start of a later session until the
Product Owner caught it — see the entry above.
*References:* `company/infrastructure-decisions.md` ID001.

---
**2026-08-05 — Marketing agent evolved to MVP-phase Market Validation mandate**
*Context:* Product Owner requested Marketing's responsibilities reflect the
project's actual current stage (validation and go-to-market prep) rather
than an advertising-execution mandate that doesn't fit yet.
*Decision:* Rewrote `.claude/agents/marketing.md` — ICP definition,
community research, validation-channel comparison (not defaulting to
interviews), proactive initiative recommendations, and a hard
approval-gate on anything that touches a real external account. Approved
by the Product Owner.
*Impact:* Marketing's first deliverables under the new mandate:
`company/market-validation.md` and `company/usability-testing-plan.md`.
*References:* `.claude/agents/marketing.md`; `company/market-validation.md`.

---
**2026-08-05 — Merchant Experience Kit: content Approved, structure open (Q13)**
*Context:* The Merchant Experience Kit (a FigJam board — persona canvas,
empathy map, journey, design principles, experience checklist — meant to
become the primary merchant-persona reference for future design work) had
never gone through `reviewer`'s Foundation-consistency pass.
*Discovery/Decision:* Review found one Important finding (a third-person,
translated-principle phrasing in the empathy map's SIENTE quadrant,
violating Product Language rules) — fixed and verified. Mid-review,
discovered the board actually has two full, independently-built pages
(English + Spanish), not one — a duplication risk with no recorded
rationale and no sync mechanism (the SIENTE bug existed identically on
both, caught by accident). `architect` ruled the two-page structure is out
of compliance with `global-principles.md`'s language-split principle as
written, and routed "why does it exist / what should happen to it" to a
new Business Decision rather than resolving it unilaterally.
*Impact:* Kit content is Approved and citable today. Its two-page structure
is not settled — Product Owner decision pending.
*References:* FigJam `https://www.figma.com/board/yjb7sUjdueUfzGKHmJHKhy`;
`company/business-decisions.md` Q13; `company/market-validation.md` §1a
(source field data).

---
**2026-08-05 — `settings.md` Approved: Low-Fidelity UX phase complete**
*Context:* Final of six Low-Fidelity UX documents (Hoy, Inventario, Eventos,
Resultados, Onboarding, Settings).
*Decision:* Approved after a three-round remediation cycle (one Blocker —
NFC activation losing its required kit-confirmation step — plus several
Majors, including two self-inflicted regressions from a `ux-designer`
full-rewrite pattern, ultimately resolved by Main applying a precise,
previously-verified fix directly rather than a fourth full regeneration).
*Impact:* All six Low-Fidelity UX documents are now Approved — that phase
is closed. Medium-Fidelity build/review is the active phase per document.
*References:* `product/02-ux/settings.md`; `product/02-ux/ux-critic-findings.md`.

---
**2026-08-05 — Known gap: `merchant-user-tester` agent proposal unrecoverable**
*Context:* Product Owner referenced a previously-discussed proposal for a
new `merchant-user-tester` agent (prerequisites reportedly including an
Approved Merchant Experience Kit, Playwright MCP, and a public Figma
Present-mode URL).
*Discovery:* Exhaustive repository search (agent files, every `company/`/
`product/` doc, `product/99-rfc/`, saved plans, full git history) found no
trace of it. Genuinely lost, not stale — the proposal was never persisted.
*Impact:* Cannot be sequenced or built until restated by the Product Owner.
One partial breadcrumb: `playwright` MCP is already configured
project-side (`.mcp.json`, `.claude/settings.local.json`) with no written
justification — plausibly a prerequisite, unconfirmed.
*References:* none — that is the point of this entry. Once restated,
persist immediately (likely `product/99-rfc/`) and update this entry.

---
**2026-08-05 — `merchant-user-tester` agent Approved; Experience Validation formalized as governance**
*Context:* Restated by the Product Owner (the original proposal was lost —
see the entry above) with a changed academic objective — continue
learning/refining before real-merchant sessions, not rush to them — plus
repeated evidence that Product-Owner-led walkthroughs kept finding
experience problems (broken emotional continuity, disconnected screens,
weak NFC journey, underwhelming Results, unclear system state) that
`ux-critic`/`ux-designer`/`reviewer`/Experience Review hadn't caught,
because none of those roles' mandate is "be a first-time merchant," only
"check against spec." Refined over several rounds: single Ana persona;
hard knowledge isolation via tool scoping, not just instructions; report
structured around the Merchant Experience Kit's own dimensions (confidence,
trust, perceived value, expectation-vs-reality), with an explicit
anti-quota rule so positive and negative findings both get equal rigor
rather than manufactured balance; adoption signal captured only as a felt,
in-the-moment reaction, never a business forecast.
*Decision:* Approved. `.claude/agents/merchant-user-tester.md` is the agent;
`company/CLAUDE.md`'s new "Experience Validation" section is its lifecycle
and governance — when it runs (after Medium-Fidelity's `ux-critic`/
`reviewer` cycle, before human-moderated User Validation), how its reports
become `experience-review-*.md` documents and re-enter the standard UX
Remediation cycle, and the hard boundary that its findings never substitute
for real merchants validating business assumptions and product value.
*Impact:* New standing role in the pipeline. Before its findings are
trusted as part of that standard cycle, its first execution must be a
**Qualification Run** against the already-known Journey 2 regression (see
the entry above) — deliberately run before that regression is fixed, so a
successful find is evidence the agent works, not evidence the prototype
does. Concrete blocker before that run can execute: a public Figma
Present-mode URL for the original three-journey prototype (Journey 2
specifically) — likely a manual Figma sharing-settings action, not
something available through current tools.
*References:* `.claude/agents/merchant-user-tester.md`;
`company/CLAUDE.md` ("Experience Validation", "Delegation" sections).

---
**2026-08-05 — ID009 discovered: new agent files aren't dispatchable within their own creation session**
*Context:* First attempt to dispatch `merchant-user-tester` (created earlier
this session) failed twice, identically — the harness didn't recognize the
agent name at all. Traced via file-timestamp comparison: every agent
successfully dispatched this session (`marketing`, `ux-critic`,
`ui-designer`, `reviewer`) had a file predating this session's start; the
one new file was the one that failed. The standard workaround for a similar
prior issue (ID002 — dispatch via `general-purpose` with the persona pasted
in) was deliberately not applied here, since `general-purpose` has
unrestricted tool access and would have silently defeated
`merchant-user-tester`'s core design property (knowledge isolation
enforced by tool scoping, not just instruction).
*Decision:* Documented as `company/infrastructure-decisions.md` ID009.
Agreed with the Product Owner to end this session and run the Qualification
Run in a fresh one instead of accepting a weaker, instruction-only
substitute — the run's whole purpose is validating the architecture, not
just the prompt.
*Impact:* This session boundary now doubles as the first live test of the
Session Recovery Protocol itself. The next session should: run recovery,
confirm `merchant-user-tester` is now discoverable, then execute the
Qualification Run exactly as already agreed — task text, URL, and
instructions are final, already recorded below, not to be re-litigated.
*References:* `company/infrastructure-decisions.md` ID009.

---
**Handoff for next session — Qualification Run, ready to execute once recovery confirms the agent is available:**
- Public prototype URL (seamless demo, correct entry point confirmed via
  live `get_metadata` on node `162:320`, "Bienvenida + Elegir cómo
  empezar"): `https://www.figma.com/proto/DPRnGD5JWjfoNBSlAFoVG4/Nahui-%E2%80%94-Medium-Fidelity-UI?node-id=162-320&p=f&viewport=574%2C625%2C0.02&t=qPfTobaiUEx4GEvv-1&scaling=min-zoom&content-scaling=fixed&starting-point-node-id=162%3A320&show-proto-sidebar=1&page-id=160%3A2`
  — also recorded in `company/active-artifacts.md` (update that row once
  confirmed working).
- Task (verbatim, Product-Owner-approved, do not alter): "You just heard
  about an app called Nahui from another vendor at a bazaar. She said it
  helps you keep track of your sales without it slowing you down with
  customers. You're curious whether it's worth trying. Open it, see what
  it's about, and get as far as you naturally would."
- No journey names, frame names, node IDs, expected paths, or the Journey 2
  regression should be mentioned in the dispatch. Record the complete
  navigation path, preserve screenshots/browser evidence, do not intervene
  if the agent gets confused or chooses an unexpected route.

---
**2026-08-05 — Qualification Run executed and confirmed; `merchant-user-tester` enters standard governance**
*Context:* First real execution, in a fresh session (confirming ID009's
diagnosis — the agent was immediately available once the process
restarted). Ran against the seamless demo prototype's true entry point
("Bienvenida"), with only the approved task text and no journey/frame/node
hints, exactly as scoped.
*Discovery:* The agent independently found and triple-verified a dead CTA
("Empezar" on the "Todo listo" screen) that `product/02b-medium-fidelity/CLAUDE.md`
already documents as a known, deliberate, disclosed exception — with zero
access to that document. It also added a genuinely new angle the original
disclosure lacked: the emotional cost of *where* the dead end lands, right
at the peak of built-up curiosity. Separately, it had to enable Figma's
screenreader accessibility mode to interact with the canvas-rendered
prototype at all, which incidentally exposed internal frame names/spec
citations — disclosed proactively, and independently verified by Main to
not have leaked into its actual findings.
*Decision:* Product Owner confirmed the run as a pass. Two refinements
applied to `.claude/agents/merchant-user-tester.md`: tool-mechanics
narration must stay in a separate voice from Ana's from the first moment,
not just in the final compiled report; and implementation metadata exposed
by tooling (as opposed to Main's dispatch prompt) is now explicit
"environmental noise" — reasoned from never, ignored and continued past,
only ever disclosed if it materially broke the run. Explicitly decided
*against* changing internal frame-naming conventions to reduce this
exposure — those names stay, valuable to the design team and governance;
the agent got more resilient instead. Documented as
`company/infrastructure-decisions.md` ID010.
*Impact:* `merchant-user-tester` is qualified and enters the standard
Experience Validation cycle described in `company/CLAUDE.md`. The
"Empezar" dead-end finding itself is not yet persisted as a formal
`experience-review-*.md` or routed to `ui-designer` for remediation — a
natural next step, not done as part of this entry.
*References:* `.claude/agents/merchant-user-tester.md`;
`company/infrastructure-decisions.md` ID009, ID010;
`product/02b-medium-fidelity/CLAUDE.md` (the pre-existing, now
independently-confirmed "Empezar" exception).

---
**2026-08-06 — Qualification Run confirmed successful; verification-status governance added; `merchant-user-tester` fully operational**
*Context:* The Product Owner questioned whether a real multi-screen
walkthrough had actually occurred (having only observed the browser open,
not the automated interaction). Rather than trust the agent's own report,
Main independently reproduced the entire path — same URL, same clicks —
using its own Playwright access.
*Discovery/Decision:* All three real screens the agent reported (Bienvenida
→ "Ver un ejemplo" confirmation → "Todo listo") matched word-for-word, and
the core finding — "Empezar" on "Todo listo" does nothing — was confirmed
directly by Main clicking it and observing no URL/screen change. Main's own
first click attempt hit the identical wrong-screen mistake the agent had
disclosed (a real ~1.176x screenshot/viewport scale mismatch), further
corroborating the agent's tooling disclosures as accurate, not fabricated.
Product Owner confirmed: the Qualification Run's real value wasn't finding
a broken flow — it was proving the full chain (agent finds issue unaided →
Main independently reproduces it → tooling artifacts stay separated from
product findings) holds together. New standing governance added: every
future `experience-review-*.md` from `merchant-user-tester` must tag each
finding **Independently Verified / Partially Verified / Pending
Verification / Tooling Artifact** (`company/CLAUDE.md`, Experience
Validation section).
*Impact:* First official agent-generated Experience Review persisted:
`product/02-ux/experience-review-2026-08-06.md`. The "Empezar" dead-end
finding routed to `ux-designer` (spec-level question — what should honestly
happen there without fabricating business data — precedes any Figma fix).
`merchant-user-tester` is now fully operational governance, not just
qualified in principle.
*References:* `product/02-ux/experience-review-2026-08-06.md`;
`company/CLAUDE.md` (Experience Validation section, verification-status
rule).

---
**2026-08-06 — `events.md` Medium-Fidelity workstream closed; task #33 completed**
*Context:* Closing the loop on the 2026-08-05 Session Recovery incident's
concrete damage (see that entry above). Product Owner's explicit direction:
don't revert the redundant review's legitimate fixes just because the
review itself was unnecessary — restore only what actually regressed.
*Decision:* Journey 2 fully restored (`ui-designer`, new clone IDs
`324:540`/`324:559`/`324:563`, fresh reaction-chain readback confirmed).
Final `ux-critic` pass: 0 Blockers, 0 unresolved Majors, both the
restoration and all 10 kept fixes (5 Major + 5 Minor) independently
re-verified, full 20-frame sweep clean. `reviewer` pass: 0 Blockers, one
Important finding — the second cycle's 10 findings/fixes had never actually
been recorded anywhere, meaning nobody could independently confirm they
were legitimate. Closed by adding the missing dated record to
`product/02b-medium-fidelity/events.md` §3, in the same style as the
document's existing findings log.
*Impact:* `events.md`'s Medium-Fidelity status is `product/02b-medium-fidelity/CLAUDE.md`-confirmed
done again, with full, honest history intact rather than smoothed over.
Task #33 closed. `company/active-artifacts.md`'s stale regression note
corrected.
*References:* `product/02b-medium-fidelity/events.md` §3;
`product/02b-medium-fidelity/CLAUDE.md`; `company/active-artifacts.md`.

---
**2026-08-06 — Root cause found: Playwright MCP was missing `--caps=vision`; the "canvas can't be clicked" problem was a config gap, not a ceiling**
*Context:* A `merchant-user-tester` re-walk of the "Empezar" fix produced no valid interaction evidence — every accessibility-ref-based click attempt failed, and the run wrongly substituted the prototype's own Next/Previous frame stepper to keep advancing, narrating the result as if it carried interaction evidence. Caught and stopped by the Product Owner before being trusted; the agent itself honestly confirmed, on direct challenge, that it had used the stepper and produced no valid findings.
*Discovery:* The Product Owner identified the actual root cause: Playwright MCP's default capability set only exposes accessibility-tree-based tools, which a canvas-rendered Figma prototype doesn't support — but Playwright MCP has an official, purpose-built `--caps=vision` opt-in exposing coordinate-based tools (`browser_mouse_click_xy` etc.) designed for exactly this case. This project's `.mcp.json` never enabled it. Every prior workaround (the `ID010` accessibility-accommodation trick, Main's own manual clicking via the unsafe code-execution tool) was routing around a missing flag, not evidence of a real limitation.
*Decision:* `.mcp.json` now launches Playwright with `--caps=vision`. `merchant-user-tester`'s tool list replaced entirely with the Vision coordinate tools; every accessibility-ref tool dropped; the unsafe code-execution tool deliberately never granted. Documented as `company/infrastructure-decisions.md` ID011, explicitly corrected to record this as a configuration gap, not an inherent impossibility.
*Impact:* Requires a session restart before the next Qualification Run (both the MCP config change and the agent's tool-list change need one, per `ID009`). A minimal smoke test (navigate, screenshot, click one button at corrected coordinates, confirm the screen changed) should run before trusting a full walkthrough again.
*References:* `company/infrastructure-decisions.md` ID009, ID010, ID011; `.mcp.json`; `.claude/agents/merchant-user-tester.md`.

---
**2026-08-06 — `--caps=vision` fix proved insufficient: genuine, unexplained click-reliability intermittency, not a Button-vs-text pattern**
*Context:* Post-fix, the "Empezar" re-walk showed a real 401 auth-endpoint error that Main initially (and incorrectly) blamed for click failures — retracted once the same error was found present during successful clicks too. Product Owner directly observed successful real-world clicks Main's automation couldn't reproduce. Extensive elimination followed: CDP trust level (already trusted, confirmed via research), natural multi-step movement/hover-settle timing, coordinate precision (verified against live Figma node geometry), an orphaned pre-fix `playwright-mcp` process (found running without `--caps=vision`, killed) — none explained the gap. A resumed re-walk agent produced genuine, node-ID-verified proof one of its clicks worked; a subsequent fresh dispatch then failed on all four taps, including the previously-100%-reliable plain-text link — ruling out even the Button-vs-text-link theory. Net: real, currently unexplained intermittency in Playwright-driven clicks against this Figma canvas, not a fixed, characterizable pattern.
*Decision:* Product Owner declined to accept this as a permanent limitation or hand the investigation to `merchant-user-tester` — reliable, unbiased first-time-user automation is essential precisely because the Product Owner already knows the intended paths. Reclassified as a Main-owned infrastructure research workstream: compare genuinely different automation stacks (official `chrome-devtools-mcp`, Playwright `connectOverCDP`, native macOS pointer input via a narrowly-scoped MCP, other computer-use MCPs, a web-exported prototype as fallback-only), each evaluated for reliability, knowledge-isolation impact, security boundary, and effort — with a real, repeated (3x from clean state) proof-of-concept per viable candidate, not research alone.
*Impact:* `chrome-devtools-mcp` (official, Google-maintained) installed via `claude mcp add`, pending restart to test. Native macOS automation was blocked on macOS Accessibility permission (`osascript` denied assistive access, `-1719`) — Product Owner has now granted it. `connectOverCDP` deprioritized for a full test cycle based on strong existing negative community evidence (documented reliability regressions vs. normal Playwright `connect()`), noted as evidence-based, not directly disproven. Next session: confirm `chrome-devtools-mcp` tools are available, run controlled 3x-from-clean PoC tests for both new candidates, produce the full decision matrix requested (recommended solution, fallback, rejected alternatives with evidence, security boundaries, setup steps, reliability results, rollback plan).
*References:* `company/infrastructure-decisions.md` ID011; `.mcp.json`; `~/.claude.json` (`chrome-devtools` server entry).

---
**2026-08-06 — Infrastructure investigation resolved: `chrome-devtools-mcp` adopted, 9/9 clean, `merchant-user-tester` switched over**
*Context:* Completed the comparative investigation from the entry above. Native macOS automation, after the Accessibility permission was granted, revealed a different, more fundamental block: the automation browser doesn't render to any physically-visible screen in this hosting environment (`System Events` reports zero windows; a full-screen capture shows only desktop wallpaper) — rejected as environment-specific, not a permissions problem. Playwright `connectOverCDP` deprioritized on existing negative community evidence, not directly tested. `chrome-devtools-mcp` tested properly: the same three controls, each clicked 3× from a fresh navigation, 9/9 successful, every click confirmed against the correct destination node via the tool's own explicit navigation report.
*Decision:* `chrome-devtools-mcp` adopted as `merchant-user-tester`'s primary interaction mechanism, replacing Playwright Vision entirely. `.claude/agents/merchant-user-tester.md` updated: new tool list (`navigate_page`/`take_snapshot`/`click`/`take_screenshot`/`wait_for`), interaction instructions rewritten around snapshot→element→click rather than coordinate math, and the "enable screen-reader accommodation" step is now a standing, documented instruction rather than left to ad hoc rediscovery. `.mcp.json`'s Playwright `--caps=vision` config left in place, untouched, as an immediate same-day rollback path.
*Impact:* Full decision matrix delivered (recommended/fallback/rejected alternatives with evidence/security boundaries/setup steps/rollback plan) — see `infrastructure-decisions.md` ID011's final entries for the complete record. Restart required before the next `merchant-user-tester` dispatch, per `ID009`. A fresh Qualification-style run on the new stack is the next step, to confirm it holds up under the agent's own full workflow, not just Main's direct testing.
*References:* `company/infrastructure-decisions.md` ID011 (full resolution); `.claude/agents/merchant-user-tester.md`.

---
**2026-08-06 — First `chrome-devtools-mcp` run: narration-separation gap found and fixed; 2 real findings confirmed and routed**
*Context:* First full `merchant-user-tester` run on the new stack completed successfully (20+ real taps, no workarounds needed). Product Owner caught a real regression while reviewing it live: technical tool language (click confirmations, navigation results) was leaking into Ana's first-person narration — a weaker standard than previously set ("labeled and switched" instead of genuinely separate channels).
*Decision:* Strengthened `.claude/agents/merchant-user-tester.md` to a hard structural split: all technical detail goes exclusively into a Tool Diagnostics/Execution Evidence channel, never into the live narration, not even labeled — narration contains only what Ana sees/thinks/feels/decides. Applies to all future runs; this run's own report predates the fix.
*Discovery:* Main independently verified the run's two most concrete findings by retracing the exact path. (1) Settings reached from the NFC-seeded demo Home shows the free-plan/buttons variant — confirmed via frame names on both ends (`nfc-twin` Home → `plan gratis` Settings), traced to an already-disclosed build simplification (exception #6, `product/02b-medium-fidelity/CLAUDE.md`). (2) The tab bar (Inventario/Eventos/Resultados) is genuinely unwired on that same Home frame — confirmed structurally via `take_snapshot` (plain `StaticText`, not `link`). Three other reported findings (stale daily total, apparent state reset, empty Eventos tab) left as Pending Verification, not yet traced.
*Impact:* `product/02-ux/experience-review-2026-08-06-b.md` persisted with verification-status tags per finding. Both confirmed findings routed to `ui-designer` as one remediation batch.
*References:* `product/02-ux/experience-review-2026-08-06-b.md`; `.claude/agents/merchant-user-tester.md`; `product/02b-medium-fidelity/CLAUDE.md` (exception #6).

---
**2026-08-06 (overnight) — NFC Settings/tab-bar fix verified end-to-end; 2 Pending findings resolved; 1 new finding surfaced**
*Context:* Continuing overnight per explicit Product Owner authorization
("continue the work... capture the findings of merchant-user-tester as
higher priority because it's my zero user"). `ui-designer` had completed the
Findings 1+2 remediation batch from the entry above (Settings routing clone
`360:1117`, tab-bar wiring across all 5 nfc-mode frames) and reported it
ready for verification.
*Discovery/Decision:* `ux-critic` reviewed structurally: clean, 0
Blockers/Majors, confirmed clone fidelity and target-frame content by
screenshot, and caught a real error in `ui-designer`'s own disclosed-items
list (a claimed stale "← Eventos" label that, checked against the render,
already correctly reads "← Hoy" — corrected, not carried forward). `ux-critic`
also flagged that reaction-level click correctness is structurally outside
its Figma-inspection tools (`infrastructure-decisions.md` ID004). Main
closed that exact gap directly via `chrome-devtools-mcp`: every hop of both
fixes (▾ → clone → Configuración → correct paid/nfc Settings → back-link →
origin Home; all three tabs → their destinations) confirmed by live click,
not inference. While doing so, Main also independently traced the two
previously-Pending findings from the prior entry: Finding 5 (empty Eventos
tab) and Finding 6 (tag queue resolving in one tap) are both now
Independently Verified, with root causes identified (5 is the same
canonical-shared-frame pattern as Finding 1; 6 is a disclosed static-
prototype content gap, same class as the accepted daily-total limitation).
A new issue, Finding 7, was surfaced only by fixing Finding 2: the tab bar
now works, but all three destinations are generic canonical frames
inconsistent with the nfc-twin's actual mid-session narrative — logged as a
Product Owner proportionality question (same class as exception #6), not
resolved unilaterally.
*Impact:* `product/02-ux/experience-review-2026-08-06-b.md` fully updated —
every finding (1-7) now carries a verification status and, where
applicable, a fix record. `product/02b-medium-fidelity/CLAUDE.md`'s demo-page
tracking has a new dated subsection recording both fixes. `company/active-artifacts.md`'s
demo-prototype URL row corrected — it had been left marked "Not yet obtained"
for days after the URL was actually acquired and in active use, a stale-
documentation gap fixed in passing. `reviewer` and a `merchant-user-tester`
re-walk (task: check Settings and today's sales/inventory mid-Día-2) were
both dispatched to close the loop — results not yet in as of this entry.
*References:* `product/02-ux/experience-review-2026-08-06-b.md`;
`product/02b-medium-fidelity/CLAUDE.md`; `company/active-artifacts.md`;
`company/infrastructure-decisions.md` ID004.

---
**2026-08-06 (overnight) — `product/03-build` readiness confirmed; `settings.md` §2.1's last disclosed gap closed; forward-looking backlog prep done**
*Context:* With Medium-Fidelity delivery stable and the NFC fix above being verified, Main kept `architect`/`planner` productive per `company/CLAUDE.md`'s standing governance rather than leaving them idle overnight.
*Discovery/Decision:* `architect`'s build-readiness review found the domain model fully implementation-ready for sale registration, event/session, inventory+NFC, and settings/capability toggles — no open `decision-log.md` entry blocks a build start. It found two documentation-hygiene gaps, both closed same night: `product/02-ux/CLAUDE.md`'s status section had a stale "full remediation cycle pending" line for `onboarding.md`/`settings.md`'s already-completed D27 cycle (corrected; a pointer-only entry also added to `ux-critic-findings.md`, honestly noting the original Major/Minor IDs were never captured at the time rather than fabricating them), and `settings.md` §2.1's Home entry-point wiring had never received its formal Medium-Fidelity `ux-critic` pass despite two separate self-disclosures in `home.md`'s own tracking. That formal pass ran and came back clean (0 Blockers/Majors/Minors, all 8 in-scope frames + 4 correctly-excluded frames verified) — the last open item in that lineage is now closed.
`planner`'s backlog prep recommends Home (Hoy), paired with a thin Onboarding and thin Inventario slice, as the first `product/03-build` target — grounded directly in `company/backlog.md`'s own stated gating ("neither stage starts before backlog #1 clears its bar") and a sharp observation from `company/usability-testing-plan.md`: neither the Figma click-through nor a moderated session can ever produce a real "<3 second" registration-speed measurement — only real code in real merchants' hands can close backlog #1 at all.
*Impact:* No blockers remain for a clean `03-build` start on Home/Onboarding/Inventario, whenever that phase begins. One genuine Product Owner-level call surfaced, correctly not resolved by `planner` itself: whether to continue prototype-stage validation testing or move to real `03-build` now — a strategic sequencing decision with no already-established ordering, per Decision Ownership.
*References:* `product/02-ux/CLAUDE.md`; `product/02-ux/ux-critic-findings.md`; `product/02b-medium-fidelity/home.md`; `company/backlog.md`; `company/usability-testing-plan.md`.

---
**2026-08-06 (overnight) — Real dead-end trap found by `merchant-user-tester` re-walk; Blocker routed for urgent fix**
*Context:* The `merchant-user-tester` re-walk dispatched to close tonight's NFC Settings/tab-bar remediation loop (see the entry above) found something more serious than the expected confirmation pass. Ana followed a fully realistic path — active session, checked Settings (worked, felt trustworthy), returned to selling, tapped "Inventario" expecting to see her stock — and got genuinely stuck, with no working control left to get back to her event. She correctly stopped and reported the wall factually rather than diagnosing it or improvising a workaround, exactly per her design ("merchant-user-tester experiences, Main investigates").
*Discovery:* Main reproduced Ana's exact path via `chrome-devtools-mcp`, click for click. Root cause: `162:1661`'s ("Registrar mercancía" form) own "← Inventario" back-link is wired to `162:1485` (Home's literal Journey-1 Cold-start frame) — correct for Journey 1, its original and only caller until tonight, but never reachable from the NFC-mode family before tonight's Finding 2 tab-bar fix made `284:3534`→`162:1661` reachable for the first time. Once on `162:1485`: tab bar entirely unwired (a pre-existing, untouched legacy limitation of that specific frame), and the only working control (header "▾") opens a sheet whose own Inventario link loops back to `162:1661` — never to the live session. A genuine closed loop, not merely a narrative mismatch like Finding 7 from earlier tonight.
*Decision:* Classified as a Blocker, not a proportionality question — this one carries a real risk of a merchant believing her sales were lost, the exact core validated friction the product exists to protect against. Routed to `ui-designer` immediately, ahead of Finding 7: clone `162:1661` for nfc-context entry (repointing the 5 nfc-mode frames' Inventario tabs to the clone), give the clone's back-link a real destination in the live nfc session, and leave `162:1661`/`162:1485` themselves untouched so Journey 1's own correct use of them isn't disturbed.
*Impact:* `product/02-ux/experience-review-2026-08-06-b.md` updated with the full finding (Finding 8) and root-cause trace. Fix dispatched; not yet verified as of this entry. This is the clearest example yet of the Experience Validation layer's actual value: a re-walk meant to confirm a fix instead found a worse problem the fix itself had newly exposed — precisely the reason this gate exists.
*References:* `product/02-ux/experience-review-2026-08-06-b.md` (Finding 8).

---
**2026-08-06 (overnight) — Finding 8 fix verified complete; a second, sibling dead-end (Finding 9) found immediately after**
*Context:* Follow-up to the entry above, whose Impact line said "fix dispatched; not yet verified" — `reviewer` flagged that line as stale once verification actually completed, correctly per the Session Recovery failure this file already documents; this entry closes that gap rather than editing the original (append-only convention).
*Decision/Discovery:* Finding 8's fix (5 self-referencing Inventario clones + the `360:1117` leak fix) is complete: `ux-critic` reviewed clean with elevated scrutiny, `reviewer` confirmed the self-referencing-clone design was a legitimate, disclosed engineering judgment call (not overstepping into invented product behavior), and Main independently verified two full round trips by live click via `chrome-devtools-mcp`. A second `merchant-user-tester` re-walk, dispatched specifically to confirm this fix felt right to Ana, found it did — her "← Inventario" tap worked exactly as intended — but she then tapped the header "▾" (a reasonable next move) and hit a second, sibling dead-end: 4 of the 5 nfc-mode frames' own "▾" trigger still opens the *original, unfixed* session-controls sheet (`162:1526`), whose tab bar is inert and whose "Configuración" reproduces **Finding 1's exact original bug** (free-plan Settings shown to the paid/nfc-seeded business) — plus puts "Cerrar sesión" one tap away with no warning, a real accidental-logout risk mid-sale. Main reproduced and traced this directly. Root cause identical to Finding 8's: tonight's original Fix 1 only ever rewired `284:3698`'s own "▾" trigger (the one frame Finding 1's original report demonstrated the bug from); Finding 2's tab-bar fix made the other 4 frames reachable for the first time, exposing that their own local triggers were never touched.
*Impact:* Logged as Finding 9 in `product/02-ux/experience-review-2026-08-06-b.md`, classified as a Blocker (same severity as Finding 8). Routed to `ui-designer` for an urgent fix, extending Finding 8's self-referencing-clone pattern to the remaining 4 frames. Not yet verified as of this entry.
*References:* `product/02-ux/experience-review-2026-08-06-b.md` (Finding 9).

---
**2026-08-06 (overnight) — Finding 9's own fix caught a fresh regression before reaching `reviewer`; third fix round dispatched**
*Context:* `ui-designer` fixed Finding 9 (correctly identifying along the way that only 3 origins needed it, not the reported 4 — `284:3552` deliberately has no "▾" per its receipt spec). Main live-click-verified 2 of the 3 chains, both landing correctly with session state intact. Routine next step: `ux-critic`'s structural pass.
*Discovery:* `ux-critic` caught a real regression Main's own click-through had missed — the 3 new sheet clones were built from the wrong template (`home.md` §3.6c's no-Session shape: generic header, one "Configuración" button) instead of the correct active-Session shape (§3.7a: real session header, **two** rows — "Cerrar sesión" and "Configuración," always reachable per §2's Session-controls interlock). Checking Main's own transcript confirmed it directly: both live-tested sheets showed only "Configuración." The old, buggy `162:1526` sheet these replaced had actually gotten this part right — fixing the wrong-Settings-tier bug silently dropped "Cerrar sesión" from three mid-selling screens.
*Decision:* Classified as a third Blocker in this same lineage, routed back to `ui-designer` with precise instructions: rebuild the 3 sheet clones from `162:1526`'s own shape (the correct template), keep the 3 already-correct content clones untouched, wire "Cerrar sesión" to a real working destination.
*Impact:* This is the third consecutive round where verifying one fix surfaced a new, genuine problem one layer deeper — not oscillation (each round found something the previous fix's own change newly exposed, converging closer each time), but worth naming as a pattern: this specific nfc-mode session-controls affordance had accumulated more latent, never-exercised wiring debt than any other single surface touched tonight. Fix verified since this entry was written: `ux-critic` re-review clean, Main live-click-confirmed the full chain on the no-items origin (`284:3534`) including a real, working "Cerrar sesión" destination.
*References:* `product/02-ux/experience-review-2026-08-06-b.md` (Finding 9, regression addendum).

---
**2026-08-06 (overnight) — `reviewer`'s closing pass caught a fourth, more severe Blocker: the Session-controls interlock silently bypassed for an in-progress Sale**
*Context:* Dispatched as the closing Foundation-consistency review for the whole Finding 8/9 chain, expecting a routine clean pass given `ux-critic` and Main had both already verified the latest round.
*Discovery:* `reviewer` found that Main's own verification methodology had a real gap: the live click-through only tested `284:3534` (the no-items nfc origin) and assumed `284:3540`/`284:3547` (with-items / mid-save) matched "by identical pattern." That assumption inverted the actual requirement — `home.md` §2's Session-controls interlock exists specifically to make the with-items case *diverge*: "Cerrar sesión" with 1+ items in the open Sale must route to §3.11a's blocking notice, never to §3.11's direct close confirmation. The rebuilt fix wired all 3 origins to the same §3.11 destination, meaning a merchant with an unfinished 2-item sale could tap through to "Sí, cerrar" and silently lose it — more severe than Findings 8/9 (those were dead-ends; this is silent data loss presented as the normal flow), and the exact core validated friction (`company/CLAUDE.md`) this product exists to prevent.
*Decision:* Classified as a fourth Blocker in this lineage. `41:573` (the correct §3.11a frame) already exists in the production build — routed to `ui-designer` to clone it for the demo page rather than inventing new content, and to explicitly verify/decide `284:3547`'s correct behavior rather than assuming it too.
*Impact:* A genuine lesson for Main's own verification discipline, not just `ui-designer`'s build: "identical pattern" is not a safe assumption when the underlying spec requires divergence between similar-looking states — the specific states that most need independent testing are exactly the ones a fix might have wrongly treated as interchangeable. **Fix now complete and verified**: `ui-designer` cloned the production §3.11a frame for both with-items origins, `ux-critic` reviewed with elevated scrutiny (clean, independently re-confirmed the §3.8c "still counts as unfinished" reasoning against `home.md` directly rather than trusting the build report), Main live-click-verified the full round trip. Fourth Blocker in this one-night lineage, fourth clean close.
*References:* `product/02-ux/experience-review-2026-08-06-b.md` (Finding 9, second regression); `product/02-ux/home.md` §2/§3.11/§3.11a.

---
**2026-08-06 (overnight) — Finding 10: a fifth instance of the same pattern, one layer deeper (the "Elegir producto" picker sheet)**
*Context:* A closing `merchant-user-tester` re-walk, dispatched to confirm the whole Finding 8/9 chain felt right end-to-end, instead found a fifth dead-end: from inside Finding 8's own (correctly fixed) registration-form clone, tapping "Elegir producto" opens a separate, shared, un-cloned picker-sheet node whose own back-link still targets the original `162:1485` trap. Main reproduced and confirmed directly.
*Decision:* Given this is the fifth distinct discovery of the identical shared-node-wrong-back-link pattern in one night, Main judged that another narrow one-off patch was unlikely to converge and requested a comprehensive sweep instead — audit everything transitively reachable from the 5 nfc-mode session frames for any remaining reference to the pre-fix frame family, fix all findings from that sweep in one batch. First dispatch hit a genuine, disclosed tool-access gap (`ReadMcpResourceTool` unavailable) and correctly refused to guess rather than proceed unsafely — retried once per the standing `ID001` practice for this exact intermittent failure mode.
*Impact:* Sweep in progress as of this entry.
*References:* `product/02-ux/experience-review-2026-08-06-b.md` (Finding 10); `company/infrastructure-decisions.md` ID001.

---
**2026-08-06 (overnight) — Finding 10 sweep complete; six-round NFC remediation saga closed**
*Context:* Closing the loop on the entry above — the comprehensive sweep it left "in progress."
*Decision/Discovery:* Sweep found 6 real leak instances beyond the picker sheet itself, all the same causal family (shared nodes whose back-link/completion still targeted the pre-fix trap): a second picker-sheet state, the registration form's "committed lines" screen, a destructive action's own completion (Descartar), and two more instances of Finding 1's exact original bug via new side doors (an Activar-clientes-frecuentes confirmation and its completion). Fixed with 28 new clones, one per instance per applicable nfc origin. Two deeper legacy nodes deliberately left unfixed — a disclosed proportionality question, same class as the already-accepted exception #6 — `reviewer`'s closing pass independently verified this classification directly against the actual nodes (both have working back-links, not the inert-tab-bar trap pattern) rather than accepting the analogy on faith. `ux-critic` reviewed with elevated scrutiny: clean, independently derived and spot-checked the clones since none were given by ID, confirmed sources undisturbed. Main live-click-verified the original confirmed instance end-to-end.
*Impact:* Six rounds, six genuine Blockers, one night: dead-end trap, four unfixed Settings triggers, a wrong sheet template dropping "Cerrar sesión," a Session-controls-interlock bypass risking silent data loss, a picker-sheet leak, and this six-instance sweep. `reviewer`'s closing Foundation pass found the underlying work sound with zero Blockers — only three documentation-hygiene gaps (this entry, `product/02b-medium-fidelity/CLAUDE.md`'s missing Finding 10 subsection, and `experience-review-2026-08-06-b.md`'s stale Recommendation section), all closed alongside this entry. This is the clearest demonstration yet of why Experience Validation is its own gate, separate from `ux-critic`/`reviewer`'s spec-compliance checks: every one of these six Blockers was found by verifying — not trusting — the previous round, exactly the chain this project's governance exists to run.
*References:* `product/02-ux/experience-review-2026-08-06-b.md` (Finding 10, full sweep record); `product/02b-medium-fidelity/CLAUDE.md`.

---
**2026-08-06 (overnight) — Finding 11, the seventh and final item: two never-wired terminal screens; investigation deliberately closed for the night**
*Context:* A final closing `merchant-user-tester` re-walk, this time completing the full registration+tagging flow rather than backing out of it, found a genuinely new trap: `284:535` (the tagging-complete confirmation) and, one hop further, `162:1804` (Eventos cold-start) both had entirely unwired tab bars — not a wrong-destination bug like Findings 8-10, but screens that simply never had lateral navigation built at all, since both were built as terminal/leaf screens in their own original journeys. Tonight's nfc-mode wiring work made both reachable as journey-crossing points for the first time.
*Decision:* Fixed and verified — `284:535` (single caller) edited directly; `162:1804` (13 legitimate callers, several Journey-1-native) cloned rather than edited, preserving every other caller's behavior. Main live-click-verified the full two-hop escape chain both ways. Given this was the seventh distinct navigation gap found through six rounds of fix→verify→re-walk in one night, Main made a deliberate decision to close the investigation here rather than continue chasing further leaves — the underlying reachable graph (all of Inventario's registration+tagging flow, Eventos, Resultados, Settings' deeper sub-flows) is larger than what one night's remediation can exhaustively audit with confidence, and returns were diminishing round to round.
*Impact:* Seven Blockers found and fixed in one night, every one surfaced by verifying — never trusting — the previous round: a dead-end trap, four unfixed Settings triggers, a wrong sheet template silently dropping "Cerrar sesión," a Session-controls-interlock bypass risking silent data loss, a picker-sheet leak (closed via a 6-instance/28-clone sweep), and two never-wired terminal screens. A dedicated, systematic full-graph audit of the nfc-mode reachable surface is flagged as good candidate work for a future session — not urgent (the specific traps found tonight are all closed), but the underlying pattern (Journey-1-built shared content silently reused by nfc-mode without re-checking every downstream link) is now well-understood enough to search for proactively rather than only reactively.
*References:* `product/02-ux/experience-review-2026-08-06-b.md` (Finding 11, closing the night's full record).

---
**2026-08-06 — Correction: the entry above miscounts "Seven Blockers"**
*Context:* Caught during fact-checking for an external progress report — the entry above's Impact line says "Seven Blockers found and fixed in one night" but its own enumeration lists only six items (dead-end trap; unfixed Settings triggers; wrong sheet template; interlock bypass; picker-sheet leak/sweep; two never-wired terminal screens). The "seven" was carried over from Finding 11's own self-description as "the seventh distinct navigation gap" — a cumulative count of every finding in the night's sequence (including non-Blocker findings like 5, 6, 7), not a count of Blocker-severity items specifically.
*Correction:* Six genuine Blockers were found and fixed in one night, not seven — matching the entry earlier in this same log ("Six rounds, six genuine Blockers, one night"). Per this file's append-only convention, the original entry above is left unedited; this is the correction of record.
*References:* `product/02-ux/experience-review-2026-08-06-b.md` (Finding 11's own "seventh distinct navigation gap" language, which is the actual source of the miscount).

---
**2026-08-06 — Two external Spanish deliverables produced: progress report (DOCX) and presentation, both brand-designed**
*Context:* Product Owner requested a progress report and a ~4-minute presentation, both in natural Mexican Spanish in her own voice, sourced strictly from real project artifacts, with the explicit division of labor: Marketing improves storytelling, Reviewer verifies facts, Architect verifies technical accuracy, UX Designer verifies the product narrative, Main coordinates and delivers.
*Decision:* Built an English master draft first (internal reasoning stays English per standing instruction), dispatched all four specialist reviews in parallel. All four found real, correctable issues, not rubber-stamped: Marketing found an unstated but true connection between Ana's founding fear and the mid-session data-loss bug found overnight; Reviewer caught a genuine arithmetic error (this file's own "seven Blockers" line contradicted its own six-item enumeration — corrected at the source, see the entry above) plus an entry-point-count error (five Settings screens claimed, actually four); Architect caught a reintroduced-and-already-corrected phrasing about the Loyalty/customer-privacy boundary (D21); UX Designer caught a real factual conflation between two different Figma prototype artifacts, and — most importantly — that the flagship privacy-fix story had been implicitly misattributed to `merchant-user-tester`, when it was actually found by a Product-Owner-led walkthrough before that agent existed. All four corrections applied before translation.
*Impact:* Three files delivered to `evidence/2026-08-06-progress-report/`: the Spanish DOCX progress report, a Spanish HTML presentation built with Nahui's actual brand tokens (Coral AA+, Fredoka/Inter, the real four-pillar mark, real Figma screenshots — not a generic template), and a companion DOCX with the slide outline, full speaker notes, and a coverage checklist. Published as a private artifact for the Product Owner to review before any sharing.
*References:* `evidence/2026-08-06-progress-report/`; `product/00-foundation/decision-log.md` D21; `company/brand/brand-guide.md`.

---
**2026-08-06 — Both deliverables reframed: from "what happened" to "why the process itself evolved"**
*Context:* Product Owner reviewed the first version and asked for a genuine reframing, not a polish — both pieces described the project instead of explaining why it's fundamentally different. New explicit requirement: the report (capped at 3 pages) must naturally answer why specialized agents, governance, and Experience Validation each became necessary, how the project's artifacts support each other, and why the project is ready for High Fidelity — without enumerating artifacts as a list, and without spending space on individual UX findings. The presentation must tell that same evolution as its own story, not summarize the report.
*Decision:* No new facts were introduced — this was a narrative restructuring of already-verified material, done directly rather than redispatching the four specialist reviews (no claims changed, only the framing). Rewrote the report as a continuous narrative: the validation-prototype question → the Foundation (why decisions needed one permanent home) → why specialized agents became necessary (the real story: a boundary was crossed once, and that's exactly why it got hardened) → why governance became necessary (the Session Recovery incident, told as cause and effect) → why Experience Validation exists (the privacy catch, spec-compliance vs. lived experience) → how MCP tooling let agents touch the real prototype instead of only reading about it → why the project is ready for High Fidelity (the method is proven, not just the design finished). Rewrote the presentation (11 slides) around the identical arc, kept "The path to what's next." in English throughout per explicit instruction, reused the same brand system and real screenshots.
*Impact:* All three files in `evidence/2026-08-06-progress-report/` updated in place (same artifact URL preserved for the presentation). The thesis both pieces now carry: Nahui's biggest achievement so far isn't only the application design — it's the AI-native methodology built to design, validate, and govern it with evidence rather than assumption.
*References:* `evidence/2026-08-06-progress-report/`.

---
**2026-08-06 — Native .pptx generated alongside the HTML deck**
*Context:* Product Owner confirmed she wanted the editable PowerPoint file offered earlier, and separately flagged that the deliverables weren't visible where she expected — traced to the files living one folder level deeper (`evidence/2026-08-06-progress-report/`) than she'd checked, not a real gap; confirmed all files were present and pointed to the exact path.
*Decision:* Built `Nahui - Presentacion.pptx` via `python-pptx`, matching the HTML deck's 13-slide content, Nahui's real colors, the logo mark (rasterized to PNG since PowerPoint doesn't render SVG), and the same real Figma screenshots. Disclosed one honest limitation: brand fonts (Fredoka/Inter) are referenced by name, not embedded, so a machine without them installed will see a fallback font — color/logo/layout/screenshots are unaffected.
*Impact:* Four final files in `evidence/2026-08-06-progress-report/`: the DOCX report, the DOCX speaker-notes/checklist companion, the branded HTML presentation (also live as a published artifact), and now the native `.pptx`.
*References:* `evidence/2026-08-06-progress-report/`.

---
**2026-08-06 — Executive Review pass: product visibility restored, method reconnected to Ana, evidence sharpened**
*Context:* Product Owner requested a final executive-quality review against 9 criteria (product-vs-method balance, product visibility, cause-and-effect clarity, evidence quality, executive-level term introductions, one-idea-per-slide, visual consistency, speaker-note naturalness, and the 3-message exit test) — explicitly not a rewrite, and explicitly without growing either deliverable.
*Discovery:* Two real gaps survived the earlier reframe: (1) the product itself had disappeared — no screenshot or concrete description of what using Nahui actually looks like remained anywhere in either deliverable after the process-narrative rewrite; (2) two paragraphs/slides ("why agents," "why governance") were purely internal-process reasoning with zero reconnection back to Ana or the merchant problem. The appendix timeline diagram was also found more decorative than evidentiary — generic milestone labels rather than explicit cause→effect pairs.
*Decision:* Report: added one grounding sentence describing what Ana's day with Nahui actually looks like, reframed the endings of the "why agents" and "why governance" paragraphs to name Ana as the reason each exists, tightened other phrasing to net the report 32 words *shorter* than the prior version despite the additions. Rebuilt the timeline diagram with explicit cause→effect label pairs instead of generic milestones. Presentation: added one new slide ("Esto es Nahui," the real Home/selling screenshot) right after the origin-story slide, simplified two overly-dense slides (why agents, why governance) to one idea each by cutting their redundant lede paragraphs, and added an explicit Ana callback to the closing slide. Swapped a duplicate screenshot in the evidence-grid slide so no image repeats. All changes verified word-count-neutral or negative; no change to the core 8-beat narrative arc.
*Impact:* All four files in `evidence/2026-08-06-progress-report/` updated in place (report and companion notes DOCX, presentation HTML republished at the same artifact URL, `.pptx` rebuilt to match — now 14 slides). A full change log was given directly to the Product Owner in-conversation, organized by the same 9 review criteria.
*References:* `evidence/2026-08-06-progress-report/`.

---
**2026-08-06 — Pure language pass: em dashes removed from every deliverable**
*Context:* Product Owner requested a final writing-style-only review across all four files, explicitly not touching structure, narrative, or visuals: no em dashes as a narrative device, short direct sentences, avoid "not only... but also" constructions, and every paragraph should sound like something she would actually say, not something an author would write.
*Decision:* Rewrote every sentence containing an em dash across the report body, the four appendix diagram images (regenerated, since two captions were baked directly into the matplotlib output, not just doc text), every presentation slide (HTML and `.pptx`), and the full 14-slide speaker-notes script, replacing each with a short sentence, a comma, or a colon depending on what the spoken version would actually use. Also caught and fixed dashes in structural elements that aren't narrative prose but are still part of the artifacts: the HTML page `<title>`, an image `alt` attribute, and the speaker-notes doc's own slide-number labels. Consolidated the previously fragmented speaker-notes doc (earlier versions only recorded deltas between passes) into one complete, single-source script covering all 14 slides.
*Impact:* Verified zero em dashes remain in any of the four files via direct text extraction, not visual spot-check. Report word count and presentation slide count unchanged; only sentence-level phrasing changed.
*References:* `evidence/2026-08-06-progress-report/`.

---
**2026-08-06 — Narrative voice pass: first-person singular for Product Owner decisions, plural reserved for the agent system**
*Context:* Product Owner clarified this is an individual project and asked for a final consistency pass: her own decisions, designs, and discoveries needed first-person singular ("decidí," "diseñé," "introduje," "documenté," "descubrí," "validé"), with plural/team language reserved strictly for describing the AI agent system's own coordinated execution, never blending the two as "the AI and I" or presenting agents as co-authors.
*Decision:* Reclassified every sentence across all four files. Moments of her own design/discovery (freezing the Foundation, documenting the Decision Log, introducing the Merchant Experience Kit and Experience Validation, defining governance rules, personally finding the payment-privacy issue) converted to "yo." Moments describing the agent system's own coordinated behavior (a session-recovery gap during Main's coordination, the verification loop itself) kept in third person ("el sistema," "quien coordina") rather than "nosotros," to avoid any reading that blends her with the agents. Caught one significant miss on the first sweep: the presentation's own title, "Cómo construimos Nahui," used exactly the plural framing being corrected — fixed to "Cómo construí Nahui" across the HTML title, cover slide, `.pptx` cover, and every cross-reference to that title in the notes doc.
*Impact:* Verified zero incorrect first-person-plural verb forms remain in any of the four files via direct text extraction. Added a short explanatory note to the speaker-notes companion doc stating the voice convention explicitly, so it's documented, not just applied silently. Presentation republished at the same artifact URL under its corrected title.
*References:* `evidence/2026-08-06-progress-report/`.

---
**2026-08-06 — Final refinement: inline concept glossary, visual appendix, two new presentation slides**
*Context:* Product Owner confirmed the reframed narrative was right and wanted one more pass, not a rewrite: readers with no project context needed each concept (Fundación, Registro de Decisiones, Kit de Experiencia del Vendedor, Validación de Experiencia, Protocolo de Recuperación de Sesión, Bitácora, MCP) briefly introduced the first time it appears, without turning the report into a glossary or breaking the 3-page cap.
*Decision:* Rewrote the report narrative with one-clause definitions woven inline at each concept's first mention (word count held to ~1,240, still comfortably within 3 pages). Added a visual appendix (explicitly outside the page limit) with four diagrams — agent architecture, artifact relationships, the validation lifecycle, a project timeline — built with `matplotlib` in Nahui's actual brand colors, not text tables. Added two new presentation slides without touching the existing 11-slide story: a responsibilities-not-technology flow (Product Owner → Main → agentes especializados → herramientas MCP → Validación de Experiencia → Alta Fidelidad) and a real-artifact evidence grid, reusing actual Figma screenshots already captured rather than illustrations.
*Impact:* All three files in `evidence/2026-08-06-progress-report/` updated in place; presentation republished at the same artifact URL. Product Owner separately asked whether the presentation could have been built as an editable file (PowerPoint/Keynote); clarified the current deliverable is a branded HTML page (the closest equivalent to a "Canvas" in this environment) and that a native `.pptx` is technically achievable the same way the Word report was generated — offered as a follow-up, not yet requested.
*References:* `evidence/2026-08-06-progress-report/`.

---
**2026-08-06 — Experience Validation Coverage Matrix created**
*Context:* Product Owner requested a coverage matrix cross-referencing `information-architecture.md`'s 5 canonical User journeys against actual `merchant-user-tester` (Ana) test coverage, distinct from spec-review or Main's own manual verification.
*Discovery:* Every `merchant-user-tester` run to date has entered through Onboarding or an already-active NFC Session — none has walked Event scheduling, Event close, or Resultados/Review at all, and even within Selling, Ana has never personally completed Finalizar Venta or Cerrar Sesión through to their terminal states (Main performed those specific taps during independent verification, not the persona). Not a fidelity gap — all relevant screens are done at Medium-Fidelity — a dispatch-scope gap: every run so far has focused on the NFC-mode Home/Settings/Inventario area where the night's findings originated.
*Decision:* New standing artifact created: `product/02-ux/experience-validation-coverage.md`. Explicitly excludes `experience-review-2026-08-04.md` from coverage credit — that was a Product-Owner-led walkthrough, not a `merchant-user-tester` run, a different verification mechanism.
*Impact:* Recommended next-dispatch priority, in Core Thesis order: (1) Selling, completed end-to-end by the persona, not just navigated around; (2) Review/Resultados, zero coverage today; (3) Event scheduling; (4) Event close, lowest urgency.
*References:* `product/02-ux/experience-validation-coverage.md`; `product/00-foundation/information-architecture.md`.

---
**2026-08-06 — Knowledge Mentor introduced: a shared, consultation-only knowledge layer, plus a reusable Consultation Pattern**
*Context:* Product Owner wanted to introduce a long-term Research capability — evidence-gathering across a curated local `Knowledge/` repository, general model knowledge, and external sources — without it ever making decisions, modifying artifacts, or acting as a reviewer. Refined through several rounds: first scoped as a plain search function, then reframed as an organizational "knowledge mentor" consulted by `ux-critic`/`ux-designer`/`ui-designer`/`architect` (reducing duplicated reading and duplicated token cost across those four), then corrected on one architectural point — the specialist requesting a consultation, not Main, must be the one who determines whether its own objective trigger is met, keeping Main a pure orchestrator rather than a reasoning supervisor.
*Decision:* Created `.claude/agents/knowledge-mentor.md` — `Read`/`Glob`/`Grep`/`WebSearch`/`WebFetch` only, no `Write`, strict origin-tagging (Local Knowledge / Model Knowledge / External Sources) on every claim, never classifies findings, never overrides Foundation, never replaces the requesting specialist's judgment. Documented the interaction shape as a new, explicitly reusable **Consultation Pattern** in `company/CLAUDE.md` (specialist determines the need → specialist requests and stops → Main orchestrates only → specialist retains full responsibility), generalized from the escalation mechanism `ux-critic`/`reviewer`/`ux-designer` already used with `architect` — not a new governance shape, a named and generalized version of an existing one. Added objective, task-shape consultation triggers (never "if you have doubts") to `architect.md`, `ux-critic.md`, `ux-designer.md`, and `ui-designer.md`; explicitly excluded `reviewer` (internal-consistency-only mandate) and `merchant-user-tester` (knowledge isolation is load-bearing to its entire purpose) by design, with `planner`/`marketing` flagged as natural future consumers once relevant `Knowledge/` domains exist.
*Impact:* First `Knowledge/` domain (`Knowledge/UX-UI/index.md`) already points at an external, uncopied course repository; future domains (Software Architecture, Payments, Security, Cloud, etc.) can be added the same way with zero edits to the agent itself, since it reads `Knowledge/`'s structure at consultation time rather than hardcoding a domain list. The Consultation Pattern is explicitly documented as the standard shape for any future consultative agent, not something unique to Knowledge Mentor.
*References:* `.claude/agents/knowledge-mentor.md`; `company/CLAUDE.md` (Delegation, Consultation Pattern, Knowledge Mentor sections); `Knowledge/UX-UI/index.md`; `.claude/agents/architect.md`, `ux-critic.md`, `ux-designer.md`, `ui-designer.md`.

---
**2026-08-06 — Review-architecture governance changes: Horizontal Journey Review, wiring-escalation, mandatory coverage gate, decision-log re-audit**
*Context:* A Product-Owner-led walkthrough of "Empezar gratis" (`product/02-ux/experience-review-2026-08-04.md`'s successor conversation, not yet its own persisted review doc) found four real defects the existing review pipeline never caught: a repeated "empezar" action verb across consecutive onboarding screens, apparent NFC-oriented content reachable from a free-tier path, a registration flow that felt like repeating the same step, and inventory content the merchant never entered. A responsibility analysis (not a fix pass) traced each to one of three structural causes, not agent incompetence: a narrative-continuity heuristic `ux-critic` was already mandated to check but had never operationalized; a documented, accepted tooling limitation (`company/infrastructure-decisions.md` ID004 — `ux-critic`/`reviewer` cannot see Figma reaction/wiring data) that leaves capability- and state-gated boundaries verifiable only by empirical click-through; and an unowned seam between independently-approved `product/02-ux/*.md` documents, since the folder's own "one experience reviewed before the next" rule was never paired with a review of the transition between two already-approved ones. Verification during the analysis found first-party confirmation: the "Empezar gratis" Todo-listo variant (`71:955`) has zero destination-wiring documentation anywhere in `product/02b-medium-fidelity/onboarding.md`, unlike its sibling "Ver un ejemplo" variant, whose wiring bug was found and fixed months earlier — and `product/02-ux/CLAUDE.md`'s own status log was caught self-contradicting a correction it claimed to have already made.
*Decision:* Four governance changes added to `company/CLAUDE.md`: a new **Horizontal Journey Review** (`ux-critic` reviews the full concatenated screen sequence of a journey's approved documents, not each one independently, before Medium-Fidelity build starts and again before that journey's build is "done"); a **wiring-dependent-findings** rule (neither `ux-critic` nor `reviewer` may treat a capability/state-gated boundary as verified from content inspection alone — it must be named, then Main reproduces it directly or dispatches `merchant-user-tester`); **coverage is a gate, not a queue** (a journey's Medium-Fidelity "done" status now requires `merchant-user-tester` Fully Tested coverage per `experience-validation-coverage.md`, or a logged, named exception — closing the exact dispatch-priority gap that left "Empezar gratis" untested); and a new **Decision-Log Re-Audit** duty (a `decision-log.md` entry that tightens an earlier rule triggers `reviewer` to cross-reference every other Approved artifact referencing that rule, not only the one being amended). Matching instructions added to `ux-critic.md` (narrative-continuity heuristic, a distinct Horizontal Journey Review mode, wiring-dependent-findings instruction) and `reviewer.md` (capability/state-boundary verification limit, decision-log re-audit duty). `product/02-ux/CLAUDE.md`'s per-document approval rule got a companion sentence pointing at the new seam review; `experience-validation-coverage.md` got a sentence stating it's now an enforced gate, not passive tracking. No change made to `merchant-user-tester.md` itself — full-journey dispatch scope is Main's own dispatch discipline, not agent behavior, so it belongs in `company/CLAUDE.md`'s gate language rather than in agent instructions the agent has no control over.
*Impact:* None of this fixes the four original findings — that remains explicitly out of scope, unstarted. What changed is structural: the next time a similar defect exists in an untested seam or an unverifiable wiring boundary, the gates above are designed to surface it before a human has to find it by hand again.
*References:* `company/CLAUDE.md` (Horizontal Journey Review, Decision-Log Re-Audit, Experience Validation sections); `.claude/agents/ux-critic.md`, `reviewer.md`; `product/02-ux/CLAUDE.md`; `product/02-ux/experience-validation-coverage.md`; `company/infrastructure-decisions.md` ID004; `product/00-foundation/decision-log.md` D27.

---
**2026-08-06 — The four onboarding findings: all fixed, exercising the review-architecture gates the same day they were built**
*Context:* Following up on the responsibility analysis above, the Product Owner asked to actually fix the four findings from her "Empezar gratis" walkthrough — redundant "Empezar" copy, apparent NFC content on the free path, a "register, register" repeated-step feeling, and phantom inventory. Each was routed to its owning specialist per the new governance, not fixed by Main directly.
*Decision:* **Finding 1** (redundant verb) — `ux-designer` renamed Todo listo's CTA "Empezar"→"Entrar" across all three variants in `onboarding.md`; `ux-critic` verified clean. **Finding 3** (repeated steps) — `ux-designer` traced it outside `onboarding.md` entirely, to a `home.md`/`inventory.md` seam; `ux-critic`'s new Horizontal Journey Review mode classified it as Major (`HJR-INV-M1`); `ux-designer` fixed it (`inventory.md`'s heading "Registrar mercancía"→"Registro de mercancía"); `ux-critic` and `reviewer` both verified clean, though `reviewer` caught the Figma build hadn't been re-synced — closed by a follow-up `ui-designer` dispatch (30 heading nodes, full-file search, not just the 4 originally suspected). **Findings 2 & 4** (NFC content, phantom inventory) — `ui-designer`'s first static reaction trace found the Onboarding→Home wiring clean, contradicting the Product Owner's own report; rather than accept that as closed, she directed continued investigation from the exact path she'd walked. A `merchant-user-tester` live re-walk (after Main fixed a browser-session tooling gap blocking the first attempt) reproduced both findings for real, with diagnostics pinpointing the actual cause one hop downstream: Inventario's own "Guardar mercancía," on the buttons-only path, was silently routing into an NFC tag-assignment queue. Root cause: the 2026-08-05 NFC-activation-chain build had hijacked a shared "saving" node — the same "shared node reused across the wrong context" pattern already seen six times the night before, now found a seventh time in a new location. `ui-designer` fixed it (cloned the node for the NFC leg's own use, restored the original's correct destination, rewired 10 total affected callers) — independently confirmed by Main via live click-through, and structurally corroborated by `ux-critic`.
*Discovery, distinct from the fix itself:* live-verifying the fix surfaced two content-fidelity questions (a static confirmation screen showing seeded quantities instead of what was entered; a product picker that always shows "Pijama" regardless of which row is tapped). The Product Owner's ruling on both, stated as a general standard: at Medium-Fidelity, static content is acceptable *unless it contradicts the selected journey or creates business-rule confusion* — the NFC-routing bug crossed that line (D27 violation), these two don't. Saved as a durable memory (`feedback_medium_fidelity_static_content.md`) since it's a reusable standard, not a one-off ruling.
*Impact:* All four original findings closed. Two new, previously-unknown defects found along the way (a product-picker binding gap, now logged as a disclosed limitation matching an existing production-page precedent) and one new documentation-consistency gap caught and fixed (a tracking file claiming "0 Important findings" that hadn't been updated after a real one was found). The review-architecture governance built earlier the same day — Horizontal Journey Review, the wiring-escalation protocol, mandatory `merchant-user-tester` coverage — wasn't theoretical by the end of this session; every piece of it fired at least once on real, current work.
*References:* `product/02-ux/onboarding.md`, `home.md`, `inventory.md`, `ux-critic-findings.md`; `product/02b-medium-fidelity/inventory.md`, `CLAUDE.md`; `product/02-ux/experience-review-2026-08-06-c.md`; `product/02-ux/experience-validation-coverage.md`; `product/00-foundation/decision-log.md` D19/D27.

---
**2026-08-07 — Where wiring belongs: the Consultation Pattern's first real test, and a genuine "no" to the Product Owner's own hypothesis**
*Context:* After fixing the day's wiring bugs, the Product Owner asked an architectural question and explicitly required it be answered through the new consultation architecture rather than by Main's own reasoning: should wireflows/navigation be part of the design definition itself, authored at Low-Fidelity, rather than something that emerges during Medium-Fidelity/Figma work? Stated explicitly as a hypothesis not to be assumed correct — evidence was to drive the conclusion.
*Decision:* `architect` was dispatched first and determined the question wasn't its own domain (no aggregate/IA/ubiquitous-language implication), naming `ux-designer`/`ui-designer` as the actual owners — while still gathering real evidence en route (`onboarding.md` §4 was already an exhaustive node-level flow; the "undocumented destination" case that motivated the question turned out to be a tracking gap, not a missing-spec gap). Both named specialists independently verified that evidence rather than taking it on faith, and both requested their own `knowledge-mentor` consultation — Nahui's first real use of the capability — rather than reasoning from impression. `knowledge-mentor` grounded both against the curated `Knowledge/UX-UI/` course material plus external sources (NN/Group, Figma's own documentation/forum on the identical shared-screen problem), tagging every claim by origin as designed. Verdict, converging independently from both specialists: the Product Owner's hypothesis holds for *naming* (which destination each branch/error/interruption resolves to — genuinely belongs upstream, and was informal before, now an enforced rule) but not for *wiring* (making a transition clickable, resolving Figma's one-destination-per-reaction constraint — conventionally and correctly resolved downstream; no source found supports moving it earlier, and today's specific bugs weren't preventable by doing so, since the violated constraint doesn't exist as a concept before a prototyping tool is involved).
*Impact:* `product/02-ux/CLAUDE.md` §4 gained an enforced naming rule (named destination or explicit "Not yet resolved" marker, no silent gaps), a shared-state ID-reference convention, and an explicit "wiring originates in the spec, never in Figma" clause. `ui-designer.md` gained a mandatory caller/destination audit before rewiring any shared node. `reviewer`'s consistency check on the new rule found zero Blockers but one real, pre-existing drift it caught as a side effect: `ux-designer.md`'s own tool grant and description still referenced Figma MCP tools and visual design work — leftover from before D24 split `ui-designer` out as its own agent, never corrected until this review. Fixed: `ux-designer.md` rewritten to match its actual scope (`Read`/`Glob`/`Grep` only, ASCII/text, implementation-independent, hands off to `ui-designer` not `builder`). Logged as `decision-log.md` D31.
*References:* `product/00-foundation/decision-log.md` D31; `product/02-ux/CLAUDE.md` §4; `.claude/agents/architect.md`, `ux-designer.md`, `ui-designer.md`, `knowledge-mentor.md`; `company/CLAUDE.md` (Consultation Pattern).

---
**2026-08-07 — §4 rule wording tightened: dropped an overclaim about where "wireflows" universally sit**
*Context:* The same `ux-designer` dispatch that finalized D31's §4 rule kept self-refining after its first "final" answer landed, catching its own overreach: the persisted text asserted Nahui's Low-Fidelity docs sit at "the abstract flow-diagram / user-flow stage, not the wireflow stage" — a categorical claim about UX methodology the consultation evidence itself didn't support (even the single local source consulted used "wireflow" inconsistently across its own decks).
*Decision:* Reworded §4 to state the exhaustive-destination-naming bar as Nahui's own process decision for this document, not a claim about universal convention — "what matters here is what this project requires of this document, independent of the label." Also renamed the "naming requirement" framing to "navigation-definition requirement," a more accurate description of what §4 actually asks for (canonical destinations, not just labels).
*Impact:* `product/02-ux/CLAUDE.md` §4 updated in place. No change to the underlying conclusion (naming stays upstream, wiring stays downstream) — this was a precision correction to the rule's own justification, not a reversal.
*References:* `product/02-ux/CLAUDE.md` §4; `product/00-foundation/decision-log.md` D31.

---
**2026-08-07 — First full execution of the Governance Rollout Cascade, D31/§4: found and fixed a real defect the rule change was meant to catch**
*Context:* Product Owner asked for the D31/§4 rollout to run as the standard cascade rather than a one-off review, and to keep applying it as the new standard for future governance changes. Full five-step run: update the rule (already done) → audit all 6 Low-Fidelity docs → fix deviations → verify Medium-Fidelity realizes what's now defined → report gaps → only then consider adopted.
*Decision:* **Step 2** — `reviewer` audited all 6 Approved docs; found a real, repeated pattern (3 of 6 dropped the decline branch of a confirm/decline dialog from their own §4, independently, in `home.md`/`inventory.md`/`events.md`). `ux-designer` fixed all three plus an optional `reports.md` citation improvement in one batch; `reviewer` re-verified clean. **Step 3** — `ui-designer` traced the newly-defined branches against the actual Figma build and found the hypothesis confirmed directly: 6 of 7 were unwired, the one exception being the one branch that was already unambiguous before today's rule existed. Fixed across three follow-up rounds (missing reactions, a genuinely unbuilt local-override content state — correctly not invented on the spot, built properly with Design System components — and inbound triggers into two confirmation frames that had zero live callers at all). **Step 4** — `ux-critic`'s structural pass on this round caught its own earlier scope gap (a first pass had only checked new-frame content, not link destinations) and, in redoing it properly, surfaced a genuine Blocker `reviewer` confirmed: a capability-revoked screen's "Ir a Configuración" routed to a frame asserting an active Paid plan — impossible given the very condition (D27) that reaches that screen. Fixed (routed to the correct, already-built Free-tier frame); `ux-critic` re-verified the fix's content directly rather than trusting the report; Main live-click-verified all three wiring-dependent boundaries this round touched (`ux-critic`/`reviewer`'s tools structurally can't see reaction data, ID004) via the established cross-page URL-fallback tab-opening pattern (ID003). `reviewer` gave final sign-off: all three documents Foundation-consistent, no Blockers, no Important findings.
*Impact:* This is the cascade's first full run, and it justified itself directly — not administrative overhead. The pattern `ui-designer` predicted held exactly: every branch left unspecified before today's rule was also unwired in Figma, and the deepest issue found (a D27 contradiction) was hiding specifically in a spot no prior review had ever actually checked, surfaced only because this round's own process discipline (re-scoping an incomplete review rather than accepting it) caught it. Also demonstrated the wiring-dependent-findings protocol working exactly as designed three separate times in one round: `ux-critic` naming an unverified boundary instead of assuming, Main closing it via live click-through, `reviewer` treating "verified how" as part of what it checks before signing off.
*References:* `company/CLAUDE.md` (Governance Rollout Cascade); `product/02-ux/home.md`, `inventory.md`, `events.md`, `reports.md`; `product/02b-medium-fidelity/home.md`, `inventory.md`, `events.md`; `product/00-foundation/decision-log.md` D27, D31; `company/infrastructure-decisions.md` ID003, ID004.

---
**2026-08-07 — D32: is a canonical-journey layer missing, or is this a Medium-Fidelity classification gap? A second architectural investigation the same day, from a fresh real observation**
*Context:* After the full D31 remediation round closed, the Product Owner walked "Empezar gratis" again and still reached the NFC experience — a real, reproduced observation, not theory. She asked whether this meant `product/02-ux/`'s experience-organized specs structurally permit journey leakage, and whether a first-class canonical-journey artifact is missing — explicit constraint: the single seamless Figma prototype stays, no fragmentation, no predefined solution.
*Decision:* `architect` split the question correctly rather than absorbing or fully declining it: `information-architecture.md` is sufficient (grounded in D28 precedent — IA's journeys are deliberately loose, not exhaustive contracts), but whether a journey artifact should exist is `ux-designer`'s/`ui-designer`'s call, not architect's. Both verified independently rather than ratifying architect's framing: `ux-designer` traced every cited incident to Figma-layer node reuse against specs that were already correct — proving it by finding `inventory.md`'s own spec already branching the exact state that caused the Guardar-mercancía hijack, unprompted — and declined a `knowledge-mentor` consultation with a stated reason (direct repo evidence was stronger than an abstract citation would be). `ui-designer` confirmed the mechanism via direct screenshot comparison across multiple incidents (shared content, diverging navigation) and surfaced a second risk axis from Finding 9 (a correctly-triggered clone can still use the wrong template). One notable process moment: a first attempt to have `ui-designer` "finalize its own dispatch's" recommendation correctly refused — a fresh, stateless subagent instance has no real memory of an earlier dispatch, and it correctly declined to author text for its own governing config file based on a claim of continuity it couldn't verify. Redispatched with honest framing (Main relaying an earlier finding, asking for independent judgment, not self-authorization) and it proceeded correctly. A final `knowledge-mentor` consultation (a narrow follow-up, not a fresh topic) found Figma's more "elegant" caller-owned mechanisms (instance overrides, Variables) both carry documented silent-failure modes reproducing the exact bug — confirming cloning as the right mechanism for this tool, not the merely expedient one.
*Impact:* No canonical-journey artifact, no Foundation change, no further `product/02-ux/CLAUDE.md` §4 extension — both evaluated and rejected on evidence, not assumed unnecessary. `.claude/agents/ui-designer.md`'s caller-audit rule sharpened: a two-axis (content/navigation) classification now triggers proactively, at node-identification time, not only reactively before a rewrite. Logged as `decision-log.md` D32.
*References:* `product/00-foundation/decision-log.md` D32 (and D28, D31 it builds on); `.claude/agents/architect.md`, `ux-designer.md`, `ui-designer.md`; `product/02b-medium-fidelity/CLAUDE.md`.

---
**2026-08-07 — "Empezar gratis" demo-safety audit: the actual leak found and fixed, and a real regression caught before it shipped**
*Context:* Right after D32 concluded the architecture was sound, the Product Owner reframed the same underlying concern as a demo-quality question, not an architecture one: she wanted every reachable path from "Empezar gratis" traced and every unintended transition into NFC or another journey eliminated, with the single seamless prototype kept intact — safe to hand to a user without having to explain away a wrong screen.
*Decision:* Four parallel, read-only trace passes (one per surface: Home, Inventario, Eventos, Selling+Resultados) found one real, confirmed, multi-hop cross-journey leak — Eventos' silent-save screen (`162:1896`) auto-continuing into the NFC-twin confirmation instead of its own buttons-only twin, chaining three hops deep into NFC session/settings/inventory content via that screen's own tab bar. Almost certainly the exact bug the Product Owner had personally hit. Also found: a same-journey Configuración back-link trap (returned to Cold Start regardless of origin — turned out to have 9 real origins, not the 3 assumed) and a pervasive dead tab bar across nearly every frame in the journey, explicitly included in scope per the Product Owner's own framing that non-functional navigation is part of demo quality. All three fixed: the leak by a one-line repoint, the trap by cloning 9+8 nodes on the navigation axis (a residual, deeper instance left explicitly unfixed as out of scope), the tab bar by restoring ~90 dead reactions to already-established destinations only, no new navigation invented. **Before closing the batch, `ux-critic`'s structural review caught a real regression the fix itself had introduced**: repointing the shared save-node away from the NFC path plausibly orphaned the 2026-08-05 Connected NFC activation-to-sale chain, since no dedicated NFC clone of that node existed. Verified true, investigated properly (the assumed fix pattern didn't fit the actual structure, so `ui-designer` adapted rather than forcing it), and fixed with two clones instead of one. Main live-click-verified both the original leak's fix and the regression's fix, end to end, on the actual prototype.
*Impact:* Both journeys now demonstrably stay within their own boundaries on the seamless prototype — verified by direct click-through, not inference. Three genuine, separate gaps were found and deliberately left open rather than absorbed into this pass: buttons-mode has no session-close block state, "Cancelar evento" has no reachable entry point in this journey, and Configuración's trap has a residual two-levels-deep instance. All three logged for `ux-designer`/a future pass, not silently fixed or silently dropped. Also worth naming as process evidence: a fresh `ui-designer` dispatch correctly refused a request framed as "finalize your own last dispatch" since it had no real memory of proposing anything — caught a false-continuity error on Main's part, not a flaw in the agent.
*References:* `product/02b-medium-fidelity/CLAUDE.md` (2026-08-07 entry, full node-level record); `product/00-foundation/decision-log.md` D31, D32; `.claude/agents/ui-designer.md`; `company/infrastructure-decisions.md` ID001, ID004.

---
**2026-08-07 — Full demo-quality remediation closed: both journeys clean, seven manual observations resolved without a single new decision**
*Context:* Direct follow-up to the same day's leak fix — the Product Owner asked for a complete pass: close the three deferred gaps, fully revalidate the Premium/NFC journey without breaking the now-correct Free/buttons one, and investigate four things she'd personally observed (NFC falling back to buttons, stale "Activar plan de pago" copy after paying, plan/mode state inconsistency, "Clientes frecuentes" activation model) plus the post-payment/NFC-kit transition — explicit instruction not to guess business intent, escalate through Decision Ownership if genuinely unresolved.
*Decision:* `architect` grounded all seven items against `decision-log.md`/`domain-model.md`/the approved specs before any Figma work — every one resolved to **Already Decided** or **Build Defect**, zero new Product/Business/Architecture Decisions needed. Notably: the "paid but no kit yet, keep selling with buttons, activate NFC later" experience the Product Owner described is *already the designed behavior* (D27, `home.md` §3.6a's fourth variant); "Clientes frecuentes" being independently activatable regardless of plan tier is *correct*, not a bug (D22/D25 — only the Resultados display needs paid tier); mid-Session mode reversion is never sanctioned, cross-Session reversion via NFC Readiness is. Both previously-deferred "missing" states (buttons-mode session-close block, "Cancelar evento") turned out to already be fully defined in approved specs, just unbuilt. The Premium/NFC trace confirmed the Product Owner's own observation directly — a real, reproducible leak (`188:1574`, reached from the NFC selling screen's own Cancelar link) explains the "falls back to buttons" report exactly, plus five more leaks in the same pass, including one inside that same day's own earlier NFC-chain fix. All fixed: 8 NFC-surface defects, the session-close block (reusing the approved production frame verbatim), "Cancelar evento" (reusing approved content, with one disclosed content-reuse determination), and the Configuración trap's full 27-node depth (traced properly rather than assuming the smaller scope first guessed). Closing `ux-critic`/`reviewer` passes both clean. One Major finding from `ux-critic` — a newly-connected "Cancelar evento" sequence appearing to silently erase unrelated events — was investigated against the spec rather than fixed or dismissed: `ux-designer` found a direct structural sibling section (§3.10) establishing this project's own existing convention for "ambient, returns to list" screens is minimal illustrative content, not an exhaustive snapshot — resolved as not-a-defect, correctly consistent with prior practice, no fix applied.
*Impact:* Both the Free/Buttons and Premium/NFC journeys are now clean — verified structurally, against the Foundation, and by direct live click-through on the highest-risk paths, not asserted from reports. Zero new decisions were required anywhere in this pass; every open question had an existing, correct answer once actually checked against what's already approved.
*References:* `product/02b-medium-fidelity/CLAUDE.md` (2026-08-07 entries, full record); `product/02-ux/home.md` §3.6a/§3.11/§3.11a; `product/02-ux/events.md` §3.10/§3.11-§3.13; `product/00-foundation/decision-log.md` D22, D23, D25, D27, D31, D32.

---
**2026-08-07 — Stale plan-tier display after activation fixed and verified; ID012 tooling gap found along the way**
*Context:* Product Owner personally hit a repro: activate the paid plan, briefly see "Tu plan: Pago," tap back, reopen Configuración, land back on a frozen "Tu plan: Gratis." `ui-designer` traced it one hop past the initial hypothesis — the activation confirmation chain itself was already correct; the defect was on the *return* path, where a heavily-shared plain Home node's session-controls sheet routed into Configuración via a hardcoded pre-activation reaction, affecting all five "just changed a paid-tier setting" confirmations that share that node, not only plan activation.
*Decision:* Fixed with a narrowly-scoped nav-diverge clone (`579:2538` Home, `579:2545` its session sheet), closing the loop back onto the paid confirmation screen without recloning the whole shared Home node. Main live-verified the fix directly via `chrome-devtools-mcp`, walking the real path (Home clone → sheet → Configuración → paid `184:1665` → Back → Home clone again) rather than trusting the build report alone. First verification attempt produced a false negative — clicking the Back link (`184:1666`, a Figma "Back"-type reaction) via synthetic `click` reported success but never navigated, reproduced identically on an unrelated, untouched sibling back-link, then resolved cleanly via `press_key({key:"Enter"})` on the same focused element. Documented as `company/infrastructure-decisions.md` ID012 so a future session doesn't misread the same false negative as a real regression.
*Impact:* The fix is confirmed correct end-to-end, not just reported. `merchant-user-tester`/Main's live-verification protocol for Back-type reactions is now: click to focus, then `Enter` to trigger — a click alone is not sufficient evidence of breakage.
*References:* `company/infrastructure-decisions.md` ID012; `product/02b-medium-fidelity/home.md`.

---
**2026-08-07 — Pending-tags task-priority refinement: from a Product Owner observation to an Approved spec amendment, via a two-round independent-investigation discipline**
*Context:* Product Owner personally noticed that returning to Inventario after deferring NFC tag assignment seemed to push her toward "Registrar mercancía" (register new merchandise) rather than resuming the pending tagging queue — and asked whether Nahui was unintentionally mixing two business processes (reception vs. tagging), with an explicit instruction not to assume she was right and to investigate via independent agent perspectives rather than Main's own reasoning.
*Discovery/Decision, round 1 (process-mixing question):* `architect` and `ux-designer`, dispatched independently with no cross-visibility, both concluded no — reception and tagging are one Inventory-context process (`domain-model.md`'s bounded-context table, one shared verb set), and the Foundation's own completion semantics already distinguish the two capability tiers honestly (`inventory.md` §3.12 "registrada" vs. §3.13 "lista para vender"). `merchant-user-tester`'s parallel live-walk attempt surfaced two separate, real tooling/build problems instead of reaching the question: a zero-open-browser-pages tooling gap (`merchant-user-tester` has no `list_pages`/recovery tool, fixed by Main opening a fresh page and resuming the agent) and a genuine, previously-undocumented dead tab bar on the post-plan-activation Home clone (`184:1503`, "H1 clone of 36:29") — independently reproduced by Main, who also ruled out two false leads (an open Figma "Flows" sidebar, and the same-day ID012 click-vs-Enter quirk) before concluding it's a real, separate defect. Left unfixed pending the Product Owner's explicit sequencing instruction.
*Discovery/Decision, round 2 (task-priority question):* Product Owner narrowed the question to whether the Foundation implies *priority* (not just process ownership) between "Registrar mercancía" and "Continuar etiquetando." `architect`: the Foundation is silent on priority specifically — it guarantees Registrar Mercancía stays reachable (a floor), never that it stays visually primary — leaving the question open to UX judgment. `ux-designer`, building on that opening: the current build's persistent-bottom-button treatment conflates "always available" with "primary," and the spec's own internal reasoning (§7's physical-continuity logic, §2 step 3's auto-continuation default, §3.13's "lista para vender" wording) already favors "Continuar etiquetando" in this specific mid-task state — faithful to the letter of the old spec, drifted from its own priority logic.
*Decision:* Product Owner ruled this a UX refinement, not a new Product Decision, and directed `ux-designer` to propose an actual fix. `ux-designer` proposed (and Main applied) a full amendment to `product/02-ux/inventory.md` (§2, §3.5, §3.17, §4, §7, §9, new §10 entry): "Continuar etiquetando" becomes the primary action (unboxed status line + CTA, mirroring `home.md` §3.6's shape) in the pending-tags state; "Registrar mercancía" stays in its existing position with unmodified reachability, now explicit as secondary in that one state only. `ux-critic`: clean pass, 2 non-blocking Suggestions (INV-S4/S5, logged). `reviewer`: clean pass, 1 Important documentation-hygiene finding (a stale, contradicting §10 bullet) — fixed by Main. Folded back into Approved.
*Impact:* `inventory.md` and `product/02-ux/CLAUDE.md`/`ux-critic-findings.md` all updated and cross-referenced. Two real, disclosed defects remain deliberately unfixed pending Product Owner authorization: the dead Home→Inventario tab bar (`184:1503`) and the already-known dead "Terminar después" button on the demo page (`284:526`, disclosed at build time, 2026-08-05) — both block `merchant-user-tester` from validating this refinement end-to-end on the live prototype, which is the explicitly agreed next step now that the design itself is settled.
*References:* `product/02-ux/inventory.md` §3.5/§3.17/§10; `product/02-ux/CLAUDE.md`; `product/02-ux/ux-critic-findings.md` (INV-S4, INV-S5); `product/02b-medium-fidelity/CLAUDE.md` (dead "Terminar después" disclosure).

---
**2026-08-07 — Correction: the "dead Home tab bar" finding above was a tooling artifact, not a real build defect; both authorized wiring fixes built and live-verified**
*Context:* `ui-designer` built the pending-tags CTA hierarchy into Medium-Fidelity and investigated the two wiring gaps authorized above. For the dead-tab-bar item, its own `node.reactions` readback found the wiring already correct and flagged the contradiction back to Main rather than "fixing" data that wasn't broken.
*Discovery:* Main re-tested live and found the earlier reproduction was itself flawed — a contaminated test sequence (multiple clicks before a single `Enter`, so `Enter` fired on stale prior focus, not the intended target) plus, once isolated properly, a genuine but page-specific automation artifact: a long-lived `chrome-devtools-mcp` browser tab, reused across dozens of navigations this session, had stopped delivering clicks correctly on that one frame. A brand-new page at the identical URL worked immediately, landing exactly on the destination `ui-designer`'s data-layer check had predicted. Documented as `company/infrastructure-decisions.md` ID013.
*Correction:* The "Defect A" dead-tab-bar finding is retracted — it was never a real product defect. No design or wiring change was needed or made for it.
*Decision/Impact:* The other authorized item (the disclosed dead "Terminar después" on the demo page) was real and is now fixed: `ui-designer` cloned the rebuilt §3.5/§3.17 pair onto the demo page (`593:526`/`593:542`) and a navigation-axis clone of the NFC Registrar-Mercancía entry (`594:2659`), wiring the full loop. Main live-verified the entire new chain end-to-end on a fresh page: `284:526` ("Terminar después") → `593:542` (new hierarchy, screenshot-confirmed — "Continuar etiquetando" reads as clearly primary, "Registrar mercancía" clearly secondary) → "Continuar etiquetando" correctly resumes to `284:526`; separately, "Registrar mercancía" correctly reaches `594:2659`. The production master frames (`47:38`/`47:65`) were also rebuilt with the same hierarchy and a Secondary-variant swap on "Registrar mercancía" (closing `ux-critic-findings.md`'s INV-S5). Next: `ux-critic` review of the build, then `merchant-user-tester` validates the actual experience — using a fresh page per ID013's lesson.
*References:* `company/infrastructure-decisions.md` ID013; `product/02b-medium-fidelity/inventory.md` (frame table pending update with new IDs: `47:38`/`47:65` rebuilt, `593:526`/`593:542`/`594:2659` new demo clones).

---
**2026-08-07 — Pending-tags fix closed: `ux-critic` clean, `merchant-user-tester` validates the core question, one same-day defect found and fixed**
*Context:* Closing out the full pipeline from the Product Owner's original observation through to a validated, live experience.
*Discovery/Decision:* `ux-critic`'s Medium-Fidelity review: clean pass (one non-blocking Minor, a button-width inconsistency, `INV-MF-MIN1`) — confirmed the built hierarchy genuinely reads as primary/secondary, not just structurally, closing `INV-S5`. `merchant-user-tester` then ran the actual validation task (activate → register → tag → defer → navigate away → return) and, at the exact moment being tested, reported: "'Continuar etiquetando'... [was] exactly the reassurance I needed," confidence "rose the most" there, and named the state-preservation as the one thing she'd tell another vendor about — no trace of "repeating work." Main independently corroborated via direct click-through immediately prior. The run also surfaced a real, same-day defect: `593:526`/`593:542`'s bottom nav bar was completely unwired (created after the earlier tab-bar restoration sweep, never inherited it) — confirmed structurally by Main, fixed by `ui-designer` (sourcing destinations from the nearest correct sibling, `284:535`, not guessing), and live-verified by Main on a fresh page. Five further findings surfaced outside this task's scope (plan-activation not visibly sticking on first attempt, an unexplained Home state jump, an unresponsive Cantidad field, plus two likely-already-accepted static-content limitations) — logged as Pending Verification, not yet investigated, Product Owner to prioritize.
*Impact:* The original Product Owner observation is fully closed: root-caused (not a business-process conflation), redesigned (Foundation-consistent UX refinement), built, reviewed, and now behaviorally validated by a naive first-time-merchant proxy — the complete pipeline this project's governance describes, exercised end-to-end in one day.
*References:* `product/02-ux/experience-review-2026-08-07.md`; `product/02-ux/ux-critic-findings.md` (INV-MF-MIN1); `product/02b-medium-fidelity/CLAUDE.md`.

---
**2026-08-07 — Marketing foundational research: competitive/behavioral landscape and Jobs To Be Done, both evidence-tiered**
*Context:* Before resuming Marketing work, Product Owner asked for a full status re-establishment (completed/approved, in-progress, missing, open decisions, blocked-vs-independent, prioritized roadmap — all via `marketing`, read-only). From that roadmap, she chose to start with competitive/behavioral landscape research, specifically framed against the merchant's *current behavior*, not a generic competitor list — then, before any positioning work, a Jobs To Be Done analysis grounded in that research plus the existing interview/Foundation evidence.
*Discovery/Decision:* Competitive research (live `WebSearch`/`WebFetch`) found real, cited evidence across direct alternatives (Clip/Mercado Pago Point/SumUp — commission-based, registration is a payment byproduct, cash sales get no benefit; Loyverse — free/offline, closest functional analog, but zero evidence of adoption by this specific ICP; TingoMex — matching audience language, pre-launch, different job) and behavioral alternatives (notebook — the direct speed-friction incumbent; WhatsApp/Instagram — serves the relationship job, not registration; spreadsheets — reconciliation, not live capture). Its sharpest finding: no confirmed instance found anywhere of any alternative being used for live sale registration under real customer-flow pressure. The JTBD analysis built on this named the core functional job precisely (capture a sale, live, in seconds, without losing the next customer), what Nahui is explicitly not hired for, a job-mapped (not feature-mapped) competitive table, evidence-graded emotional jobs (explicitly excluding "embarrassment," for which no source supports it), a near-empty social-jobs evidence base (named as a gap, not filled in), and a friction ranking by evidenced severity, not assumed uniformity. Per explicit Product Owner instruction, the "job may be currently unserved" claim was **not promoted to fact** — persisted as an open hypothesis, distinguished explicitly from `market-validation.md`'s existing H1 (prevalence across other vendors) to avoid a label collision, since it's a different claim (competitive adequacy) that happens to be related.
*Impact:* New artifact `company/jobs-to-be-done.md` — Validated Findings / Supported Evidence / Open Hypotheses, each tier explicitly defined and cited. Cross-referenced from `market-validation.md`'s Open Dependencies section. This is now the durable analytical base future positioning/messaging/GTM work should build on, distinct from and complementary to the Merchant Experience Kit and the H1-H5 hypothesis-testing package.
*References:* `company/jobs-to-be-done.md`; `company/market-validation.md` (Open dependencies); `company/CLAUDE.md` (Core Thesis).

---
**2026-08-07 — Onboarding-native "Activar plan de pago" reconvergence: root-caused, fixed (two legs + one Blocker correction), documentation gap self-caught by `reviewer`**
*Context:* `merchant-user-tester`'s pending-tags validation run hit a real discrepancy — Configuración read "Tu plan: Gratis" after activation had already completed, and merchandise registration produced the buttons-only confirmation with no tagging step. Product Owner explicitly directed a root-cause investigation before any fix (three independent, no-cross-visibility dispatches — `architect`, `ux-designer`, `ui-designer`), then authorized the fix once confirmed, then authorized including a second, same-class instance `ui-designer` found but hadn't fixed, in the same pass.
*Discovery/Decision:* `architect`/`ux-designer` independently confirmed the Foundation and approved specs both expect immediate, everywhere-consistent capability visibility, with one narrow named exception (an already-open Selling Session). `ui-designer` traced the actual build and found the real defect: the Onboarding-native "Activar plan de pago" path correctly diverged from the Free path at its first hop, then silently reconverged one hop later onto the shared "Journey 1: buttons-only" subtree — a new instance of the same shared-node-reconvergence class already fixed repeatedly this project (Findings 8/9/10, the Guardar-mercancía hijack), just in a path none of those rounds had ever traced. Two legs fixed, both reusing existing paid-tier content rather than inventing any (per explicit instruction): the Inventario/Configuración leg (`184:1503`) and the "Iniciar Sesión Rápida" leg (`184:1507`). `ux-critic` gave a clean first pass; `reviewer` then caught a real Blocker neither Main's own live verification nor `ux-critic` had caught — one Configuración clone (`651:5396`) showed the wrong `defaultSellingMode` value ("Con tags" instead of "Botones"), a subtle but Foundation-grounded distinction (`onboarding.md` §2.3/D27: `defaultSellingMode` stays `buttons` unconditionally, a separate field from the `nfc` capability gate that correctly does flip immediately). Fixed in one node, plus a secondary label/destination mismatch `ui-designer` caught proactively in the same pass. `reviewer` also caught that neither fix had a tracking-file or bitácora entry before reaching review — closed by this entry and the corresponding `product/02b-medium-fidelity/CLAUDE.md` update.
*Impact:* Both legs now correctly Foundation-consistent, independently live-verified by Main on fresh pages after every fix. Re-review and a `merchant-user-tester` walk of the paid journey specifically are the remaining steps. One retracted false-positive along the way (a stale-browser-page tooling artifact, not a real defect) — see `infrastructure-decisions.md` ID013.
*References:* `product/02b-medium-fidelity/CLAUDE.md` (2026-08-07, "Onboarding-native 'Activar plan de pago' reconvergence" entry); `product/00-foundation/decision-log.md` D27, D31, D32; `product/02-ux/onboarding.md` §2.3; `company/infrastructure-decisions.md` ID013.

---
**2026-08-07 — Paid-journey reconvergence fix: workstream closed, one Blocker retracted as a tooling artifact, one new finding surfaced and left open by scope**
*Context:* Closing out the full pipeline on the reconvergence fix above — `reviewer` re-check, then `merchant-user-tester` walking the paid journey specifically.
*Discovery/Decision:* `reviewer`'s first re-check caught a real content Blocker (`651:5396`'s toggle button read "Cambiar a vender con botones," self-contradictory for a card already on "Botones") — but the finding was based on `get_metadata` alone, and Main had already independently live-verified the correct label three separate times. Sent back with the `infrastructure-decisions.md` ID004 Part 2 citation (stale instance-override leaf-text is a documented `get_metadata` failure mode); `reviewer` re-checked via `get_screenshot`, confirmed Main's read, and retracted its own Blocker. Logged as a third ID004 Part 2 recurrence, with a standing suggestion (flag instance-override text explicitly in build reports going forward) to make this easier to catch pre-emptively next time. `merchant-user-tester` then walked the paid journey and confirmed the actual target defect fixed, in its own words: Configuración's "Tu plan: Pago" was "the one moment that actually protected her sense that her payment mattered." It also surfaced one new, confirmed defect outside today's two fixes' scope — the "← Inventario" back-link on the new Registrar-Mercancía chain lands on Home (`184:1503`), not an Inventario catalog view, a label/destination mismatch introduced by today's retargeting, independently reproduced by Main — and reconciled a second observation (Resultados showing populated history against an empty Eventos tab) to the already-open, never-decided Finding 7 from 2026-08-06, not a new discovery.
*Impact:* The reconvergence fix itself is fully closed — built, reviewed, Foundation-checked, and behaviorally validated. Two items remain open, both explicitly out of today's scope rather than silently absorbed: the "← Inventario" mislabel (new) and Finding 7's generic-destination pattern (pre-existing, still undecided).
*References:* `product/02-ux/experience-review-2026-08-07-b.md`; `company/infrastructure-decisions.md` ID004 (third recurrence); `product/02b-medium-fidelity/CLAUDE.md` (Finding 7).

---
**2026-08-07 — Reconvergence workstream fully closed; Finding 7 resolved via Product-Owner-chosen Option C, in parallel**
*Context:* Two threads closed the same day, run in parallel per explicit Product Owner instruction: the regression `merchant-user-tester` found (a mislabeled "← Inventario" back-link, landing on Home instead of an Inventario view) treated as part of the reconvergence workstream rather than a separate item; Finding 7 (2026-08-06, the Resultados/Eventos content-mismatch pattern) taken up as its own Product Owner discussion.
*Discovery/Decision:* The regression fix went through two rounds — round 1 correctly retargeted the three genuine "← Inventario" links but mistakenly swept in the Descartar-confirmation button too; `ux-critic` caught it (should return to a blank form per `inventory.md` §3.9, not the Inventario catalog), round 2 corrected it. `reviewer`'s final sign-off: clean, closing the entire "Onboarding-native 'Activar plan de pago' reconvergence" workstream end-to-end. For Finding 7, walked through the trade-offs (full narrative-accuracy coverage vs. accept-as-is vs. a targeted fix) — Product Owner chose Option C: build one genuinely-empty Resultados state and route only demonstrably-fresh-account entry points to it, leaving established personas (the nfc-twin) on the existing populated destination, which is correct for them. `ui-designer` reused an already-approved empty state from the production `reports.md` build rather than inventing one, enumerated all 49 callers of the shared populated destination, and retargeted exactly the 34 confirmed fresh-account ones — Main independently live-verified both the fixed case (`184:1503`) and a preserved case (the nfc-twin) on fresh pages.
*Impact:* Both threads closed the same day the underlying defects were found. The reconvergence workstream's full closure record lives in `product/02b-medium-fidelity/CLAUDE.md`'s dated entry; Finding 7's resolution is pending final `ux-critic`/`reviewer` sign-off (in progress).
*References:* `product/02b-medium-fidelity/CLAUDE.md` (2026-08-07 entries); `product/02-ux/reports.md` §3.3; `product/02-ux/inventory.md` §3.9.

---
**2026-08-07 — Nahui Launch Kit persisted; Instagram account baseline corrected**
*Context:* While the Product pipeline ran in the background, Marketing produced a full "Nahui Launch Kit" (social account setup, landing page blueprint, launch readiness checklist, execution order) at the Product Owner's request — grounded strictly in approved Foundation/Vision/Branding/JTBD/Market-Validation artifacts, validation-stage framing throughout, no invented claims.
*Discovery/Decision:* An automated read-only check during drafting misread the existing `@nahui.app` Instagram account's state (reported a profile picture set and 7 follows). The Product Owner corrected this directly: the account is confirmed blank-slate (no photo, no posts, no stories/highlights) with exactly one intentional merchant follow (initial test follows already cleaned up). This correction is recorded as the new baseline for all future marketing/social-media work, superseding the automated check.
*Impact:* `company/nahui-launch-kit.md` persisted and published as an artifact; updated in place once the account-state correction landed, rather than left inconsistent with known-true facts. Facebook/TikTok account states remain unconfirmed — flagged as an open item, same class of risk that produced the Instagram misread.
*References:* `company/nahui-launch-kit.md`.

---
**2026-08-07 — Finding 7 fully closed: Option C built, two correction rounds, final Foundation-consistency sign-off clean**
*Context:* Closing the last open Product item from today — Finding 7 (2026-08-06, generic canonical tab destinations not matching a fresh account's narrative), taken up as its own Product Owner discussion per the trade-offs walkthrough (full coverage / accept-as-is / targeted fix).
*Discovery/Decision:* Product Owner chose Option C. `ui-designer` built a genuinely-empty Resultados state reusing already-approved production content, and retargeted 34 nodes classified as fresh-account. `ux-critic` caught two Major findings in that first pass — one confirmed content contradiction, one real misclassification that triggered a full content-based re-audit finding 18 of the 34 were wrong, not 2. Corrected. `ux-critic`'s re-verification then found a third issue, tracing one layer deeper to a pre-existing content-fidelity gap on `162:1900` (never actually matching its own approved spec, `events.md` §3.10) — Product Owner authorized a strictly-scoped conformance fix, two text edits only, no redesign. `284:3690` (confirmed to share the identical non-conformance) was checked and deliberately left untouched, named as a separate future item. `reviewer`'s final pass: clean, Foundation-consistent, contingent only on a tracking-file update — now done.
*Impact:* This closes the last Product item in today's queue. Full record in `product/02b-medium-fidelity/CLAUDE.md`'s "Finding 7 resolved" entry. Two deliberately-deferred items remain, both named and owned: `284:3690`'s identical non-conformance, and the broader exception-#6-class simplification Finding 7 was one instance of.
*References:* `product/02b-medium-fidelity/CLAUDE.md` (Finding 7 resolved entry); `product/02-ux/events.md` §3.10; `product/02-ux/reports.md` §3.3.

---
**2026-08-08 — "Day 0" discovery surfaces a real, load-bearing domain gap (pricing); Foundation reclassified from frozen to governed-evolution; `decision-log.md` D33 adopts an MVP pricing/event-cost operating model**
*Context:* A chain of Product Owner-led investigations, starting from a narrow question ("Activar clientes frecuentes") and widening deliberately at each step — first to whether a "Business/Merchant Profile" domain entity was missing (settled: no, `Business` already serves this role), then to whether the entire Merchant Identity Creation Journey and Day-0 onboarding were structurally missing. `architect`, dispatched to evaluate Day 0 from first principles, found something real underneath the framing: `Sale`/`SaleItem`/`Product` have no price field anywhere in the frozen domain model, despite `home.md`/`reports.md` already rendering real dollar totals with no traceable source — an ambient assumption no prior review pass had ever caught.
*Discovery/Decision:* An informal but first-hand conversation between the Product Owner and Ana added real field evidence mid-investigation: she varies prices by bazaar/venue, and treats event cost (~$2,500–$7,000, sometimes plus staffing) as a real input to her decisions — logged at Supported Evidence tier, explicitly distinguished from the still-unrun structured H6 interview (`company/market-validation.md` §1c/§1d/H6/H6a). The Product Owner then made an explicit, reasoned call: the *Foundation* stays disciplined and evidence-gated (no premature adoption), but a *prototype* necessarily embodies a hypothesis to be demonstrable at all — and directed that Foundation itself be reclassified from a permanently frozen artifact to one that evolves through the same governed `decision-log.md` mechanism it already used for D20/D22/D23, "representing the current operating model, not a permanently static one." `architect` confirmed all four resulting additions (`Product.defaultPrice`, `Event.bazaarCost`, an Event-owned Price Override internal entity, automatic `SaleItem.pricePaid` resolution) are plain, non-RFC-worthy decision-log entries — no new aggregate, no new bounded-context edge, no ubiquitous-language redefinition. One proposed mechanism (a per-selling-group, per-Event override) was independently pressure-tested twice against the merchant's real-world mental model, not the data model — first `ux-designer` wrongly rejected it by misapplied analogy (a live-selling friction finding stretched to an offline-planning moment) and proposed an unvalidated "blanket adjustment" alternative; the Product Owner caught both errors and had `architect` re-derive the answer from the literal evidence text and an existing Approved precedent (`events.md`'s own "Nuevo Evento" form already asks a comparable handful of values at the identical planning moment) — vindicating the original per-selling-group shape. Logged as a process lesson (`company/lessons.md`, 2026-08-08).
*Impact:* `decision-log.md` D33 persisted; `domain-model.md`/`ubiquitous-language.md` updated to match; both files' "frozen" status lines corrected project-wide (`domain-model.md`, `product/00-foundation/CLAUDE.md`) to describe governed evolution instead. This is the first time this project's Foundation has been amended after being explicitly labeled frozen — a real precedent for how future gaps of this kind get resolved, not just a one-off fix.
*References:* `product/00-foundation/decision-log.md` D33; `company/market-validation.md` §1c/§1d (H6/H6a); `company/lessons.md` (2026-08-08).

---
**2026-08-08 — D33 realized across all four merchant-facing Low-Fidelity documents; two remediation rounds; `reviewer` clean, folded back into Approved**
*Context:* Following D33's adoption (above), the Product Owner authorized the standard governance cascade: Low-Fidelity remediation across every document D33 touches, through the normal `ux-designer` → `ux-critic` → `reviewer` cycle, with an explicit hard scope boundary (no discounts, no point-of-sale haggling, no dynamic pricing, no profitability/margin calculations, no promotions).
*Discovery/Decision:* `ux-designer` amended `inventory.md` (price capture at Product creation, a Catalog-row price-edit affordance), `events.md` (event cost, a new per-Product Price Override mechanism), `home.md` and `reports.md` (grounding every previously-untraced dollar figure). `ux-critic`'s first pass found 2 Major findings: a wiring-completeness gap in `inventory.md` (two new screens fully specified but never threaded into the required-destination sections), and — more substantively — "Ajustar precios" reachable while Ana was actively selling at an Event, undercutting D33's own "deliberate, offline planning moment" premise and opening a path functionally adjacent to point-of-sale haggling. The Product Owner resolved this directly and precisely: the capability disappears from the Event UI entirely the moment an Event goes `active` — prices are finalized before activation and fixed for the Event's duration, no exceptions. Round 2 applied this exactly (removed from `events.md` §3.14/§3.15, confirmed as the only state offering it is §3.11) and closed the wiring gap; `ux-critic`'s re-verification found it converged cleanly, with two small Minor findings left open and tracked (`ux-critic-findings.md`). `reviewer`'s Foundation-consistency pass independently re-verified both Minors as correctly non-blocking, confirmed zero leakage of the scope boundary or of internal field names into merchant-facing copy anywhere across all four documents, and caught one unrelated pre-existing drift (`product/00-foundation/CLAUDE.md`'s stale "frozen" line) — fixed separately.
*Impact:* All four documents (`inventory.md`, `events.md`, `home.md`, `reports.md`) folded back into Approved status with D33 realized. Medium-Fidelity has not yet been touched — `events.md` carries the largest rebuild (a genuinely new screen pattern, the per-Product Price Override list, with no close existing Figma precedent), `inventory.md` and `home.md` need targeted updates, `reports.md`/`onboarding.md`/`settings.md` need review only or nothing.
*References:* `product/00-foundation/decision-log.md` D33; `product/02-ux/CLAUDE.md` (status log, all four documents); `product/02-ux/ux-critic-findings.md` (D33 entry, `EVT-D33-MIN-A`/`INV-D33-MIN-A`).

---
**2026-08-08 — "Define lo que vendes" moved into Onboarding (Product Owner decision); Marketing becomes a Knowledge Mentor consumer; first continuous merchant-validation pipeline persisted**
*Context:* Scoping Thursday's Medium-Fidelity demo journey surfaced a real UX-sequencing question: Selling-Group definition (name + `Product.defaultPrice`, D33) was only ever captured inline during Inventario's Registrar Mercancía flow — the Product Owner judged this doesn't match Ana's real mental model ("this is my business, this is what I sell, these are my prices," established before she ever thinks about physically receiving inventory) and directed the step move into Onboarding instead, as required on both real paths. In parallel, the Product Owner directed a second, independent change: Marketing now operates under the same `knowledge-mentor` consultation model already used by `architect`/`ux-critic`/`ux-designer`, superseding its prior "not a KM consumer" exclusion, and was tasked with building Nahui's first continuous merchant-validation pipeline as a background workstream.
*Discovery/Decision:* `architect` cleared the Onboarding move as architecturally clean (no RFC — the identical cross-context write-invocation shape D29 and this document's own demo-seed mechanism already establish) but caught a real, non-obvious consequence: `home.md`'s cold-start test conflated "Product ever registered" with "has anything sellable," an equivalence this change breaks (a freshly onboarded merchant now has named Products with zero stock). Folded the fix into the same pass. Two `ux-critic` remediation rounds followed — round 1 found a stale copy of the old test left behind in `home.md`'s own canonical wiring section (§4), and a genuinely new first-impression risk: a fresh merchant's own just-named products would render as "0 disponibles," indistinguishable from sold-out, on Inventario's Catalog view. Both fixed in round 2 (§4 corrected to match §2; a new "sin registrar" caption distinguishes never-stocked from sold-out, no schema change) and verified clean. Separately, Marketing's first validation-package pass named three specific methodology questions for `knowledge-mentor` (continuous-discovery cadence, risk-ranking framework, leading-question risk in interview design) — the consultation independently caught a real flaw in Marketing's own proposed interview probe (a binary "reviews all vs. some" framing carrying the same leading-question risk this project's own `market-validation.md` Q7 had already been corrected for once) and surfaced that Marketing's risk-ranking justification was partly redundant with the standard Evidence axis alone. Marketing reconciled both findings rather than defending its original draft.
*Impact:* `onboarding.md`/`home.md`/`inventory.md` pending `reviewer`'s Foundation-consistency pass (in progress) before folding back into Approved — `ux-critic` clean across both rounds. Two new artifacts persisted: `company/merchant-validation-pipeline.md` (trigger-driven cadence, explicit graduation criteria to a steady-state model) and `company/merchant-validation-backlog.md` (risk-sequenced queue); `market-validation.md` gained a new §8 of execution-ready assets, all still behind the standing Approval gate. `.claude/agents/marketing.md` and `company/CLAUDE.md` updated to reflect Marketing's new KM-consumer status.
*References:* `product/00-foundation/decision-log.md` D19, D33; `product/02-ux/onboarding.md` §2.2a, §3.5b–§3.5e (status header); `company/merchant-validation-pipeline.md`; `company/merchant-validation-backlog.md`; `company/market-validation.md` §8; `.claude/agents/marketing.md`; `company/CLAUDE.md` (Knowledge Mentor section).

---
**2026-08-08 — D33 realized in Medium-Fidelity across three Figma pages (Onboarding, Inventario, Eventos); business-identity capture fixed and cleared through `ux-critic`, pending `reviewer`**
*Context:* Following the Low-Fidelity D33 remediation and the "Define lo que vendes" Onboarding move (both above), the Product Owner authorized building the corresponding Medium-Fidelity UI for a demo journey covering Onboarding's new step, Inventario's price capture/editing, and Eventos' event cost/price-override work.
*Discovery/Decision:* `ui-designer` built all three in one combined dispatch (new Onboarding frames for "Define lo que vendes," `inventory.md`'s §3.4a Editar precio sheet and §3.8a new-product price capture with 12 new price tap targets, `events.md`'s Costo del evento field and its new §3.19/§3.20 Ajustar-precios states). `ux-critic`'s first round found 3 Major + 2 Minor: a self-contradictory picker typed-no-match state (self-caught and corrected by `ui-designer` before the round-1 report even landed), missing price display on 4 of 5 Catalog locations, and insufficient visual-weight distinction between `events.md`'s "Ajustar precios" (routine planning) and "Cancelar evento" (destructive) on the same screen. `ui-designer` fixed all three — the last fix (a button-variant swap) triggered a Figma quirk that silently reset the instance's text override, which `ui-designer` caught and corrected itself before reporting. `ux-critic`'s verification pass confirmed everything clean via direct screenshot inspection, but named 3 wiring-level claims as unverified boundaries outside its tool access (ID004) — Main closed all three via live `chrome-devtools-mcp` click-throughs rather than trusting static inspection. `reviewer`'s Foundation-consistency pass: 0 Blockers, 0 unresolved content findings — but one process gap: the three Medium-Fidelity tracking files and this Status section had zero record of any of this build, a Definition-of-Done gap per `company/CLAUDE.md`'s own governance rules, not a content defect. Closed by Main directly, cross-checking every claimed node ID against live Figma `get_metadata` reads rather than trusting the build report's own summary — this caught and corrected an imprecise node-to-state mapping in the original draft before it was persisted.

Separately, the parallel business-identity capture workstream (`onboarding.md` §2.2b/§3.9–§3.10a, "Tu negocio") hit a real process failure: `ux-critic`'s first fix-verification dispatch correctly found the previously-reported fix had never actually been written to disk — `ux-designer` has no Write access by design and had only produced replacement text in its report, which Main failed to persist before dispatching verification. Main applied the fix directly this time and redispatched; `ux-critic` confirmed clean (0 Blockers, 0 Major, 1 non-blocking documentation-precision Suggestion, applied). This section is **not yet built in Medium-Fidelity** — still Low-Fidelity only, `reviewer`'s Foundation-consistency pass in progress.
*Impact:* `onboarding.md`, `inventory.md`, `events.md` (Medium-Fidelity tracking) and their Status entries in `product/02b-medium-fidelity/CLAUDE.md` updated with the real node-ID record and review history. `product/02-ux/onboarding.md` §3.9–§3.10a corrected on disk (was previously only ever a report, not a file change).
*References:* `product/00-foundation/decision-log.md` D33; `product/02b-medium-fidelity/onboarding.md`, `inventory.md`, `events.md`; `product/02-ux/onboarding.md` §3.9–§3.10a.

---
**2026-08-08 — RFC 0004 promoted to `decision-log.md` D35; a related gating correction (D34) removes a second "Activate Frequent Customers" switch; backlog #1/#2 sequencing re-checked against a strictly post-sale QR placement**
*Context:* The Product Owner resolved RFC 0004's two named open items directly: (1) confirmed the new Loyalty Participation Record read edge stays Intelligence-only, Selling excluded — exactly as drafted; (2) reversed the assumption that Frequent Customers should require `loyaltyEnabled=true` as a second merchant activation step, stating the Product intent had evolved toward Frequent Customers existing as a capability rather than something separately switched on, and asked `architect` to re-evaluate D22/D27's gating accordingly. In the same message, the Product Owner asked for a re-check of `backlog.md` #1's registration-speed gate against a strictly post-sale QR placement (rendered only on the Digital Receipt, after the Sale write has already succeeded) — with an explicit instruction not to bypass existing governance, only to determine whether the gate still logically holds.
*Discovery/Decision:* `architect` confirmed Open Item 1 as Decided (no change) and found Open Item 2 needed a real but narrow correction, not a deletion: `loyaltyEnabled` isn't redundant with `subscriptionTier` (it lets a Free-tier merchant start collecting Claims before upgrading, a deliberate independent axis `settings.md`'s own already-Approved copy already designs for) — the actual fix is separating what it gates. Visibility of Customer Segmentation / the new Loyalty Participation Record now gates on `subscriptionTier=paid` alone (the same pattern already used for "Rendimiento por bazar"); `loyaltyEnabled` is kept, narrowed back to its original, `domain-model.md`-documented scope — whether Loyalty-claim actually collects Claims at all, never report visibility. On the backlog question, `architect` found the literal registration-speed metric doesn't apply to a strictly post-sale placement — `home.md` §3.8f (the Digital Receipt) is reached only after the Sale write has already succeeded, and `architecture-principles.md` #2 ties the <3s bar specifically to the Sale-write path — but named a distinct, separate concern in `backlog.md` #2's current wording (not wanting a second initiative competing for build capacity while #1 remains unmet) as a genuine open business/scope call, not settled by architectural analysis and left explicitly for the Product Owner/Planner.
*Impact:* `decision-log.md` gained D34 (the gating correction) and D35 (RFC 0004's promotion, Customer schematized as a Business-scoped aggregate root, new "Loyalty Participation Record" ubiquitous-language term). `domain-model.md`/`ubiquitous-language.md` updated to match both. RFC 0004's own status header updated to Accepted, retained as historical record per this project's RFC convention. `reports.md`/`settings.md`'s remediation is now complete: `ux-designer` dropped the joint-gate wording and reframed "Activar clientes frecuentes" as no longer a visibility precondition; `ux-critic` found 2 Major (a stale joint-gate statement left in `reports.md`'s "Out of scope" preamble; the collapsed "Tus clientes" empty state dropping all pointer language even where a case-agnostic, D34-compliant pointer would help) + 1 Minor, all fixed and re-verified clean; `reviewer` found 3 further Important findings, all documentation-persistence gaps (stale `product/02-ux/CLAUDE.md` status lines, a missing `ux-critic-findings.md` entry, D34's own Applied paragraph omitting `domain-model.md`), all closed by Main directly. Both documents folded back into Approved. Separately, a real process gap surfaced and was fixed mid-session: an earlier business-identity fix (`onboarding.md` §3.9-§3.10a) had been reported as done by `ux-designer` but never actually written to disk — `ux-designer` has no Write access and only produces replacement text in its report; Main is responsible for persisting it and initially failed to, caught only when `ux-critic`'s re-verification found the file unchanged. Applied directly on the second attempt; `ux-critic`/`reviewer` both since confirmed clean, closed via `decision-log.md` D36 (the `Business.name`/`logo`/`description` fields, previously undocumented in the Foundation despite being real). `backlog.md` #2's Stage 2 wording still needs the Product Owner's call on the scope/attention-discipline question the architectural finding didn't resolve — named, not yet acted on.
*References:* `product/00-foundation/decision-log.md` D34, D35; `product/99-rfc/0004-customer-loyalty-participation-record.md`; `product/00-foundation/domain-model.md`; `product/00-foundation/ubiquitous-language.md`; `company/backlog.md` #1/#2.

---
**2026-08-08 — `backlog.md` #2's Stage 2 gate corrected: no longer hard-blocked behind #1, but stays a lower implementation priority**
*Context:* The Product Owner asked `architect` to re-evaluate whether `backlog.md` #2's Stage 2 (Sale-level Claim Token/QR) should still remain sequenced behind #1's registration-speed bar, given that Frequent Customers had moved from an intentionally-deferred module (under the previous roadmap) to a real MVP-vision capability (D34/D35, above) — explicit instruction not to bypass governance, only to verify whether the existing dependency still represented current product direction.
*Discovery/Decision:* `architect` separated the gate's two components: the literal registration-speed technical constraint (already found, earlier the same day, not to apply to a strictly post-sale QR — `home.md` §3.8f only renders after the Sale write succeeds) versus a genuine business/attention-discipline concern about running two build initiatives at once, which `architect` correctly declined to resolve, naming it the Product Owner's own call. `architect` also flagged that D34/D35 elevated Frequent Customers' *architectural readiness*, not its *backlog priority* — `company/CLAUDE.md`'s core thesis still read "lower priority for now" for customer segmentation, unrevised, so the Product Owner's intent hadn't yet been recorded anywhere authoritative. The Product Owner then supplied the deciding Product fact `architect`'s review hadn't had: the Digital Receipt QR is optional and entirely merchant-controlled — Ana can decline to offer it during a busy stretch if checkout speed matters more than loyalty enrollment in that moment — so Stage 2 genuinely doesn't compete with the registration critical path, structurally or in practice. Resolution: Stage 2 is no longer hard-gated behind #1's success bar; it remains a lower implementation-priority item than #1 (#1 stays the top MVP priority), but UX design/specification work for Stage 2 (the QR/claim-flow screens, customer registration, the merchant-facing Loyalty Participation view) may proceed immediately rather than waiting on #1's bar to clear.
*Impact:* `company/backlog.md` #2 corrected — the previous "touches the same critical path" justification is named as factually inaccurate for the mechanism as actually specified (not merely superseded by changed priorities), the stale "illustrative placeholder" note on `reports.md`'s "Tus clientes" removed (it's been a real spec since D22/D34), and the gate rewritten to reflect the design-work-unblocked/implementation-still-sequenced resolution. `company/CLAUDE.md`'s core thesis wording was not touched — the Product Owner's request was scoped to the backlog gate specifically, not a restatement of Nahui's overall friction-priority ordering.
*References:* `company/backlog.md` #1/#2; `product/00-foundation/decision-log.md` D34, D35; `product/00-foundation/architecture-principles.md` #2; `product/02-ux/home.md` §3.8f.

---
**2026-08-08 — Frequent Customers Stage 2 Product specification finalized; RFC 0005 opened; `company/CLAUDE.md` thesis brought current**
*Context:* With `backlog.md` #2's design work unblocked (above), the Product Owner directed `architect` to finalize the remaining Product/domain specification for Frequent Customers Stage 2 so `ux-designer` could design the actual screens, and separately asked for a review of whether `company/CLAUDE.md`'s Core Thesis still accurately reflected the capability's new MVP standing.
*Discovery/Decision:* `architect` resolved four of five open items directly: (1) the Claim Token/QR displays passively on the Digital Receipt whenever `loyaltyEnabled=true`, no new merchant-facing toggle needed — the existing capability toggle already satisfies "skip during busy periods" at the granularity the Product Owner described; (2) a formal `Customer` dedup invariant, `(businessId, email)`; (4) a new `Business.loyaltyRewardThreshold` field; (5) the customer-facing flow's architectural boundary was already settled by the frozen Foundation, restated as concrete constraints. Item (3) — the merchant's manual "reward-confirm" action — surfaced a genuine RFC trigger: resetting `Customer.currentCycleProgress`/incrementing `completedCyclesCount` requires a new, narrow write edge (Intelligence → Loyalty-claim), since the two existing grants are explicitly read-only. `architect` drafted the RFC in full rather than leaving it abstract.
*Impact:* `decision-log.md` D37 (dedup invariant + reward threshold, both plain/additive) persisted; `domain-model.md`/`ubiquitous-language.md` updated. `product/99-rfc/0005-reward-cycle-confirmation-write-edge.md` persisted, Status: Proposed, pending Product Owner review. `company/CLAUDE.md`'s Core Thesis and Business Model Direction sections corrected — Frequent Customers now explicitly named as elevated to MVP-vision status (design priority, not implementation sequencing), and a stale gating description ("available once user has own sales history") removed. `ux-designer` then produced two full Low-Fidelity specs against this finalized spec: `product/02-ux/customer-loyalty-registration.md` (new — the customer-facing registration/QR-claim flow, explicitly outside the Merchant Application) and new §3.15–§3.18 in `product/02-ux/reports.md` (the merchant-facing Loyalty Participation view, appended additively — existing "Tus clientes" content untouched). The "Confirmar recompensa entregada" write action is designed but flagged everywhere as speculative, pending RFC 0005's approval; everything else depends only on the already-Accepted D35/D37. Both specs are now in `ux-critic` review.
*References:* `product/00-foundation/decision-log.md` D37; `product/99-rfc/0005-reward-cycle-confirmation-write-edge.md`; `company/CLAUDE.md` (Core Thesis, Business Model Direction); `product/02-ux-loyalty/customer-loyalty-registration.md`; `product/02-ux/reports.md` §3.15–§3.18.

---
**2026-08-08 — Brand Guardian created: a permanent specialist and new `/brand` documentation area for Nahui's identity**
*Context:* The Product Owner requested a new permanent specialist responsible for preserving Nahui's personality, voice, and emotional consistency across every touchpoint — explicitly not a Product, Architecture, or UX decision-maker. Proposed in full (mission, responsibilities, scope, non-goals, consultation triggers, collaboration model, governance responsibilities, documentation structure) before any implementation, per the Product Owner's own explicit instruction.
*Discovery/Decision:* The proposal surfaced one real structural question — a `company/brand/brand-guide.md` (tactical visual-execution guide, already referenced throughout `product/02-ux/`/`product/02b-medium-fidelity/`) already existed, distinct from the deeper identity layer being proposed. Product Owner resolved it directly: keep `company/brand/brand-guide.md` exactly where it is, avoid the churn of moving it, and make the new `/brand/visual-language.md` the strategic layer above it. Separately, the Product Owner corrected the proposed three-tier evidence model (Validated/Hypothesis/Aspiration) to four: **Decision** (deliberately adopted, binding, not evidence-backed) is distinct from **Validated** (confirmed by real external evidence) — the same evidence-vs-deliberate-choice discipline this project already holds everywhere else (`decision-log.md`'s reasoning-vs-proof distinction, `market-validation.md`'s Evidence tiers), now explicitly extended to brand governance rather than left to blur.
*Impact:* `.claude/agents/brand-guardian.md` created. New `/brand` folder: `CLAUDE.md`, `character-bible.md`, `brand-principles.md`, `tone-of-voice.md`, `storytelling.md`, `visual-language.md` — every claim in all five content documents tagged Decision or Hypothesis (nothing Validated yet, stated honestly). Consultation/delegation wired: `company/CLAUDE.md`'s Delegation and Knowledge Mentor sections updated; consultation triggers added to `.claude/agents/ux-designer.md`, `marketing.md`, `ui-designer.md`, `architect.md`; a scope-boundary note added to `.claude/agents/ux-critic.md` distinguishing its usability lens from Brand Guardian's voice lens. Root `CLAUDE.md`'s "Where things live" updated.
*References:* `.claude/agents/brand-guardian.md`; `brand/CLAUDE.md` and its five siblings; `company/CLAUDE.md` (Delegation, Knowledge Mentor sections); `CLAUDE.md`.

---
**2026-08-08 — Frequent Customers UX cleared through full remediation; `customer-loyalty-registration.md` relocated to a new `product/02-ux-loyalty/` folder (`decision-log.md` D38)**
*Context:* `ux-critic` and `reviewer` completed the full review cycle on both new Frequent Customers Low-Fidelity deliverables (`customer-loyalty-registration.md` and `reports.md` §3.15–§3.18). Two remediation rounds closed 2 Major + 3 Minor `ux-critic` findings (a self-contradictory "No, gracias" button-weight annotation; a missing decline affordance/trust-footer gap; a concurrent-first-claim race named as an open Architect item; and, surfaced during verification, a reward-confirm screen that reused a reversible-action template without disclosing its own irreversibility — closed by amending `product/99-rfc/0005-...` with an internal-only `Customer.lastRewardConfirmedAt` trace field, not a reversal mechanism, after `architect` checked precedent against Sale finalization's own deliberately-undo-free design).
*Discovery/Decision:* `reviewer`'s Foundation-consistency pass then found a real structural gap: `customer-loyalty-registration.md` had been placed inside `product/02-ux/` — a folder whose own charter scopes it to "each merchant-facing experience" — despite the document's own §0 stating plainly it is *not* part of the Merchant Application, with no Architect ruling ever authorizing that placement exception (unlike `onboarding.md`/`settings.md`, D13, or Medium-Fidelity, D24, both of which got one). `architect` ruled the document needs its own home, following D24's precedent rather than D13's (D13 only ever had to answer "does this need its own tab," never "is this even part of the app" — loyalty-claim fails that more basic threshold outright, since `domain-model.md` describes it as "likely a separate deploy target entirely"). `reviewer` also found one Blocker in the same document — implementation-specific Button-class names ("Secondary," "Tertiary") had leaked in while fixing an earlier finding, violating this folder's own unconditional "no component names" fidelity rule — closed by `ux-designer` using implementation-independent language the document already had, at zero cost to precision.
*Impact:* `decision-log.md` D38 persisted. New folder `product/02-ux-loyalty/` created (own `CLAUDE.md`, same pipeline stage and fidelity discipline as `product/02-ux/`, different deploy target); `customer-loyalty-registration.md` relocated there. `product/02-ux/CLAUDE.md`'s charter gained a one-sentence scope clarification (not a broadened scope); root `CLAUDE.md`'s "Where things live" updated. `product/02-ux/ux-critic-findings.md` gained a consolidated "Frequent Customers Stage 2" entry covering both documents' full finding record across all rounds.
*References:* `product/00-foundation/decision-log.md` D38; `product/02-ux-loyalty/CLAUDE.md`; `product/02-ux-loyalty/customer-loyalty-registration.md`; `product/02-ux/reports.md` §3.15–§3.18; `product/02-ux/ux-critic-findings.md` ("Frequent Customers Stage 2" entry); `product/99-rfc/0005-reward-cycle-confirmation-write-edge.md`.

---
**2026-08-08 — RFC 0005 accepted as drafted; `decision-log.md` D39: Product Owner explicitly accepts the residual reward-confirm mis-tap risk, no correction mechanism built for the MVP**
*Context:* With Frequent Customers Stage 2 fully specified and reviewer-clean, the Product Owner reviewed RFC 0005 (the reward-cycle confirmation write edge) directly — the one remaining open item blocking full closure of the arc.
*Discovery/Decision:* Accepted as drafted, no amendments. The RFC's own "Named Business Decision" section asked whether an MVP-stage merchant mis-tap (silently and permanently resetting a customer's earned reward-cycle progress) needs a correction mechanism — the Product Owner resolved it directly: the existing safeguards (customer-detail-only placement, explicit two-tap confirm, `lastRewardConfirmedAt`'s bare trace) are sufficient for this stage; no undo, editable reward history, reversal workflow, or support-mediated correction process is designed or built now. The remediation path stays deliberately open — `lastRewardConfirmedAt` exists specifically so a correction mechanism remains buildable later — but is explicitly gated on real evidence of material frequency/cost, not designed preemptively.
*Impact:* `decision-log.md` D39 persisted. `product/99-rfc/0005-...`'s status updated to Accepted. `domain-model.md`'s Intelligence bounded-context grant and Customer aggregate entry updated (`lastRewardConfirmedAt` field). `product/02-ux/reports.md` §3.16–§3.18's "speculative — pending RFC 0005" framing removed throughout (status header, §2, §3.17/§3.18 headings and bullets, §4, §5, §6, §10) — the confirm-reward action is now fully Approved. `product/02-ux/CLAUDE.md`'s `reports.md` status entry updated with the full Loyalty Participation view review record. This closes the entire Frequent Customers Stage 2 arc (D33 → D39) opened earlier today.
*References:* `product/00-foundation/decision-log.md` D39; `product/99-rfc/0005-reward-cycle-confirmation-write-edge.md`; `product/00-foundation/domain-model.md`; `product/02-ux/reports.md` §3.16–§3.18.

---
**2026-08-09 (overnight autonomous session) — Frequent Customers Stage 2 fully realized in Medium-Fidelity: Loyalty Participation view, "Tu negocio" business identity, and the customer-facing registration flow all close clean; a real Experience Validation coverage gap surfaced on wake**
*Context:* Per the Product Owner's 2026-08-08 standing "unattended operation" instruction (`company/CLAUDE.md`), Main continued the already-approved Medium-Fidelity build overnight with no new decisions pending: the Loyalty Participation view (`reports.md` §3.15–§3.18, D35/D37/D39), the "Tu negocio" business-identity step (`onboarding.md` §2.2b/§3.9–§3.10a, D36), and the customer-facing registration/QR-claim flow (`product/02-ux-loyalty/customer-loyalty-registration.md`, D34/D35/D37/D38/D39) — the last piece of Low-Fidelity spec left unbuilt at Medium Fidelity.
*Discovery/Decision:* All three completed full `ui-designer` → `ux-critic` → `reviewer` cycles, each converging to 0 Blockers/0 Important findings after one remediation round apiece (full detail in each document's own tracking file and `infrastructure-decisions.md` ID001's growing addendum — a 4th recorded instance of `ui-designer` proceeding without the mandatory `figma-use` skill loaded, again routed through `ux-critic` with elevated scrutiny, again confirmed clean). A `knowledge-mentor` consultation formalized `OptionChip` (age/gender single-select chips) as a canonical Design System component, now documented in `brand-guide.md`. Two more D34 Governance Rollout Cascade gaps were found and fixed unprompted on `reports.md`'s page (a stale retired-gate frame, stale conditional copy on 3 frames). **On the Product Owner's return, a real gap surfaced**: neither of the two new merchant-facing surfaces (Recompensas tab, Tu negocio) has been walked by `merchant-user-tester` (Ana) — and per `company/CLAUDE.md`'s "Coverage is a gate, not a queue" rule, that's required before a journey reads "done." The reason it didn't happen automatically: the only Present-mode prototype URL `merchant-user-tester` has access to (`company/active-artifacts.md`'s "Seamless demo" row, page `160:2`) is a frozen snapshot from 2026-08-04, built before either feature existed — even a same-night dispatch would have hit stale wiring. The "Production prototype" row (the six live pages these features actually live on) has read "Not yet obtained" since that file was created, named there as "a real blocker, not a placeholder" — and Main has no tool that generates a Figma Present-mode share link (that's a Figma-UI action, not exposed via the plugin/MCP surface Main or `ui-designer` can reach). All six documents' `product/02b-medium-fidelity/CLAUDE.md` Status bullets were still updated to "done" on the strength of the `ux-critic`/`reviewer` cycle (spec/build fidelity, a different kind of evidence) — but Experience Validation coverage for these two specific surfaces is genuinely outstanding, not silently skipped.
*Impact:* `product/02b-medium-fidelity/reports.md`, `onboarding.md`, `customer-loyalty-registration.md`, `CLAUDE.md` (Status section) all updated and closed. `product/02-ux-loyalty/`'s tracking file is the first to complete the Medium-Fidelity tier for that folder. Named, open follow-up: obtain a Present-mode URL for the production Figma pages (`58:2`, `67:2`) — either the Product Owner's own Figma "Present" action, or explicit authorization for a best-effort constructed URL — then dispatch `merchant-user-tester` against both new surfaces before their coverage rows in `product/02-ux/experience-validation-coverage.md` can move past their current status.
*References:* `product/02b-medium-fidelity/reports.md`, `onboarding.md`, `customer-loyalty-registration.md`, `CLAUDE.md`; `company/infrastructure-decisions.md` ID001 (4th instance); `company/brand/brand-guide.md` (OptionChip); `company/active-artifacts.md` ("Production prototype" row); `product/02-ux/experience-validation-coverage.md`.

---
**2026-08-09 — `decision-log.md` D40: `loyaltyEnabled` retired outright; Frequent Customers is now a Paid-tier-only capability, not a merchant toggle — Foundation, RFC 0006, five Low-Fidelity docs, and the Medium-Fidelity Figma build all corrected in one pass**
*Context:* The Product Owner independently caught a stale mental model surviving throughout the repo: Frequent Customers/Loyalty had been treated, in multiple places, as a feature Ana explicitly "activates" (`loyaltyEnabled`, "Activar/Desactivar clientes frecuentes" in `settings.md`). Directed a full cross-repository consistency sweep, with an explicit instruction to surface any conflict with an existing Decision before touching it, but otherwise propagate the correction without asking at every step.
*Discovery/Decision:* `architect`'s trace found this wasn't stale language — `decision-log.md` D22/D25/D27 originated and formalized `loyaltyEnabled` as a bidirectional self-service toggle, and D34 explicitly defended keeping it ("a deliberate, reasoned independent axis, not an oversight"). Architect's first proposed resolution (retire the toggle but make Claim Token generation unconditional across all tiers, so Free could "accumulate history") was itself corrected by the Product Owner: Frequent Customers is a **Paid-tier-only** capability — Free tier gets nothing, not even invisibly; Paid tier gets it automatically, with zero merchant configuration in either direction; entitlement (`subscriptionTier`) and per-Sale QR-offering (an unrelated, still-optional operational choice) are explicitly distinct concepts. This landed as `decision-log.md` D40 and `product/99-rfc/0006-retire-loyalty-enabled.md`, explicitly superseding the toggle-specific portions of D22/D25/D27/D29/D34 while leaving everything else in each entry intact (named per-entry in D40's own text, this project's standing "supersede, don't edit" convention).
*Impact:* Full pipeline run, twice (Low-Fidelity then Medium-Fidelity), each closing with a real finding caught and fixed — not a rubber-stamp pass. Low-Fidelity: `ux-designer` cascaded the correction into `settings.md` (six actions → four, the toggle screens removed entirely), `reports.md`, `onboarding.md`, `home.md` §3.8f (new Free-tier receipt variant, three elements not four), and `product/02-ux-loyalty/customer-loyalty-registration.md`; `ux-critic` caught one Major (a stray "clientes frecuentes is something Configuración manages" line surviving in two documents) — fixed and re-verified clean; `reviewer` caught one Blocker (`ubiquitous-language.md`'s own "Claim" term definition still cited `loyaltyEnabled` — the cascade had fixed the Capability list and Sale entry but missed this third site) — fixed directly by Main, re-verified. Medium-Fidelity: `ui-designer` cascaded into the Figma build across `settings.md`'s page (toggle frames deleted after a zero-caller audit), `reports.md`'s page (a stale Configuración-pointer text node removed), `home.md`'s page (new Free-tier receipt clone built), and — closing a second, unrelated gap noticed along the way — the "Demo — Journey 1 Seamless" page (`160:2`), which had never been synced with the prior night's Loyalty Participation/Tu negocio builds at all and carried its own independent 16-clone copy of the retired toggle sub-flow; both closed in one pass (28 frames deleted, new sync content built and wired). `ux-critic` verified clean; `reviewer` caught one further Important finding (two dead `CapabilityCard` component variants reproducing the retired toggle, still sitting in the shared Design System page though unreachable from any live screen — D40's own "not kept dormant" standard) — fixed, deleted outright, verified via fresh `get_metadata`. One boundary named rather than silently passed: whether the newly-synced demo content is actually tap-reachable rests on `ui-designer`'s own `node.reactions` readback (Main has no `chrome-devtools-mcp` access this session to independently click-verify) — a future `merchant-user-tester` walk is the natural closing step, not yet dispatched. Two pre-existing, unrelated gaps were found and explicitly disclosed rather than folded in: the Digital Receipt still renders the generic Nahui `BrandMark` instead of `Business.name`/logo (D36), and the demo page never synced "Define lo que vendes" (D33) either — both now tracked in `product/02b-medium-fidelity/home.md`/`CLAUDE.md`'s own Known Gaps, not fixed here.
*References:* `product/00-foundation/decision-log.md` D40; `product/99-rfc/0006-retire-loyalty-enabled.md`; `product/00-foundation/domain-model.md`, `ubiquitous-language.md`; `company/business-decisions.md` Q5 (Superseded); `product/02-ux/product-decisions.md` Q15 (new, open); `product/02-ux/settings.md`, `reports.md`, `onboarding.md`, `home.md`; `product/02-ux-loyalty/customer-loyalty-registration.md`; `product/02b-medium-fidelity/settings.md`, `reports.md`, `home.md`, `CLAUDE.md`; `company/backlog.md` #2; `brand/tone-of-voice.md`.

---
**2026-08-10 — `chrome-devtools-mcp` regression investigation: a suspected environment regression, resolved as an already-known Tooling Artifact — with a load-bearing correction to the investigation's own starting premise**
*Context:* A post-restart `merchant-user-tester` run's `press_key` fallback appeared to land in a different pattern than previously documented on Home node `184:1503`. The Product Owner, invoking the standing "don't accept current behavior as inherent without explaining why prior successful runs were possible" rule, ordered a full regression analysis rather than more click/Enter tuning — explicitly forbidding a "structural limitation" conclusion until the supposed prior successes were accounted for.
*Discovery/Decision:* `chrome-devtools-mcp` was found running unpinned (`@latest`) in two conflicting registrations, having silently jumped from a long-stable `1.6.0` to a brand-new, regression-affected `1.7.0` right around the session restart — a real environmental delta, fixed by pinning `.mcp.json` to `1.6.0` explicitly and removing the conflicting `--scope user` registration, verified live via `ps aux` showing the actual running process on `1.6.0`. But a controlled, properly-isolated reproduction on that verified-pinned environment (three independent fresh cycles, one tab tested per cycle) reproduced the exact same signature already documented in `infrastructure-decisions.md` ID014 before this session even started — same node, same wrong destination (`658:2766`), regardless of which tab was tapped. Before accepting "unrelated to the version bump" as final, Main searched the actual primary sources for the "184:1503 worked via plain taps on 2026-08-07" premise the whole investigation had been built on (both `08-07` experience-review files, every `184:1503` mention in this Log) — **and found no such success anywhere in the record.** The node was flagged dead, reclassified as a Tooling Artifact the same day once `ui-designer`'s ground-truth wiring read came back correct, and never successfully re-walked afterward. The "last known success" this entire detour was framed around was an inaccurate premise carried into the session, not a fact verified before acting on it. **Further correction, Product Owner-caught:** Main's first write-up of this conclusion still mischaracterized `press_key(Enter)` as a valid fallback for completing this specific navigation — it isn't; it activates a different, unrelated control, not the tapped tab, so using it here would produce a false pass rather than a friction-laden real one. Corrected in the same entry.
*Impact:* `chrome-devtools-mcp@1.6.0` now pinned explicitly project-wide, no `@latest` reference anywhere for this tool — sound testing-infrastructure hygiene, kept regardless of the outcome. `184:1503`'s Inventario/Eventos/Resultados tab-bar navigation is recorded plainly as unavailable to automated Experience Validation coverage with the current harness — plain tap fails, `press_key` is not a valid substitute here, wiring is independently verified correct three separate times. No further click/Enter tuning is warranted; this artifact is closed and settled. Lesson for future sessions: a carried-forward summary's claimed "prior success" is a claim to verify against primary sources before acting on it, not a fact on its own — this session's own regression-analysis mandate is exactly the discipline that caught it.
*References:* `company/infrastructure-decisions.md` ID014 (full write-up, "Session restart confirmed..." through "Standing resolution, corrected"); `.mcp.json`; `product/02-ux/experience-review-2026-08-07.md`, `experience-review-2026-08-07-b.md`.

---
**2026-08-10 — Demo sample-data replacement: real pilot-merchant identity/products swapped for fictional "Luna Mercado" data, across both fidelity layers**
*Context:* The Product Owner didn't want the public demo prototype to keep using the real pilot merchant's actual business name and product mix ("Ropa Ana," Pijama/Sudadera-Maxy/Calcetines) now that it will be shown to multiple potential users — she shouldn't feel her actual business became Nahui's public demo.
*Discovery/Decision:* Applied as a pure content substitution, explicitly not a Product/Foundation concept change: "Ropa Ana" → "Luna Mercado"; Pijama/Sudadera-Maxy/Calcetines → Bolsas ($350)/Accesorios ($180)/Playeras ($280). Low-Fidelity spec text (`product/02-ux/`, `product/02-ux-loyalty/`) updated first and directly by Main (mechanical swap, no design judgment, matching the Product Owner's own framing that this doesn't reopen approved decisions) — including two passages a blind find-replace had briefly broken (a case-variant example and a singular/plural distinct-Products example), both caught and fixed before moving on. Deliberately left untouched: Foundation's own illustrative-example mentions (`ubiquitous-language.md`, `decision-log.md` — internal domain-language prose, not demo-facing), the original `product/01-validation/registro.html` (a closed historical validation artifact tied to the real interview), and every `experience-review-*.md`/`ux-critic-findings.md` historical record (this project's own "never rewrite history" convention). `ui-designer` then propagated the identical swap into Medium-Fidelity Figma across all 6 production pages plus the Demo page (229 text nodes), hitting and resolving the standing ID001 Figma-skill-loading gap along the way via the *original* documented mechanism (Main reading the skill resource and inlining it) after a first attempt using a since-superseded fallback was correctly refused by `ui-designer` itself.
*Impact:* Zero remaining old-value references anywhere in live specs or Figma content, confirmed via fresh full-text sweeps on both layers. No `ux-critic`/`reviewer` cycle run, consistent with this project's existing static-content standard (2026-08-06 ruling) — pure substitution, no design decision.
*References:* `product/02b-medium-fidelity/CLAUDE.md` (2026-08-10 entry, full node list); `product/02-ux/home.md`, `onboarding.md`, `inventory.md`, `events.md`, `reports.md`; `product/02-ux-loyalty/customer-loyalty-registration.md`; `company/infrastructure-decisions.md` ID001.

---
**2026-08-10 — Paid-tier selling/receipt reconvergence found and fixed: a real defect the acceptance-test walk surfaced, same class as the 2026-08-07 Onboarding-native reconvergence**
*Context:* A full `merchant-user-tester` walk of the Paid acceptance journey completed a sale that landed on a receipt self-titled "...plan gratis... [Free-tier fix]" despite Paid activation at the start of the same session, with Configuración correspondingly showing "Tu plan: Gratis" and no QR anywhere on the receipt. Rather than accept this as the already-known click/focus tooling artifact, Main routed it to `ui-designer` for a ground-truth trace first — the same discipline the day's earlier chrome-devtools-mcp investigation had just re-established.
*Discovery/Decision:* The trace confirmed a real, distinct defect: `658:2766` (the Paid-tier "Iniciar Sesión Rápida" nav-diverge Home clone) had been correctly cloned for its own Configuración leg in the original 2026-08-07 reconvergence fix, but its selling flow was never cloned — its `ProductTile` silently pointed into the shared, original Free-tier chain (`162:1511`→`162:1521`→`825:2941`), whose every exit dumps back onto the Free hub, not back to `658:2766`. Everything else the trace checked (four other stuck points from the same walk) confirmed the already-known tooling artifact, wiring verified correct — this was the one genuine exception. `ui-designer` fixed it by cloning the chain for this leg's own use, composing the already-approved QR content from the existing Paid/NFC receipt (`284:3552`) rather than inventing anything new — the same clone-don't-share technique D31/D32 already established for this defect class. `ux-critic` found the content/visual work clean but correctly named the reaction-level wiring as an open boundary outside its tools, given this exact artifact's documented history of receipt-exit-wiring bugs; Main closed that boundary directly via a live `chrome-devtools-mcp` click-through on a fresh page, confirming the full chain end-to-end including the QR actually rendering and the exit correctly returning to the Paid hub with a genuinely empty tray. `reviewer` confirmed the fix correctly extends `decision-log.md` D40's existing "Multi-mechanism Claim resolution" rule (a Sale-level Claim Token generates at every Sale finalization for a Paid-tier Business "regardless of `registrationMode`") and caught one Important documentation gap — no tracking-file/bitácora record existed yet, the identical gap `reviewer` caught in the original 2026-08-07 reconvergence workstream — closed by this entry and `product/02b-medium-fidelity/CLAUDE.md`'s own dated addition.
*Impact:* Fully closed: built, `ux-critic`-clean, wiring-verified live by Main, `reviewer`-clean. A fresh `merchant-user-tester` walk of the complete Paid journey from the beginning is the standing next step, per this project's "rerun from the beginning, never resume from the stuck point" rule — not yet dispatched as of this entry.
*References:* `product/02b-medium-fidelity/CLAUDE.md` (2026-08-10 entry, "Paid-tier 'Iniciar Sesión Rápida' selling/receipt reconvergence"); `product/00-foundation/decision-log.md` D31, D32, D40; `product/02-ux/home.md` §3.8f.

---
**2026-08-12 — `184:1503` family: a fourth Catalog-routing instance plus two more reconvergence legs found and fixed on the primary onboarding path; a real concurrency bug found along the way**
*Context:* A clean, isolated `merchant-user-tester` Paid-journey rerun (the companion Free-journey rerun, dispatched concurrently, came back contaminated — see `company/infrastructure-decisions.md` ID015, a genuine new infrastructure finding: two `merchant-user-tester` dispatches sharing a browser session against the same Figma file can cross-contaminate, now a standing rule to never do again) went through `184:1503` — the primary "Activar plan de pago" onboarding Home landing, distinct from the `658:2766` family fixed two days earlier — and found three more real defects.
*Discovery/Decision:* A ground-truth trace confirmed all three, each a previously-unaudited leg of an already-known defect class, not a new class: (1) a fourth instance of the Inventario Catalog-routing violation (`inventory.md` §2 step 1), fixed with a new Catalog-view clone (`981:620`) plus a proactively-found sibling instance on the Configuración sheet; (2) the same missing-QR reconvergence pattern fixed for `658:2766` two days ago, this time on `184:1503`'s own Eventos-leg selling chain, fixed with a dedicated 4-node clone chain rather than reusing `658:2766` (investigated and rejected — it shows an already-open session, wrong for a just-scheduled-first-event moment); (3) the tag-queue chain bleeding into an unrelated, older demo persona (the 2026-08-05 Connected-NFC-chain build), a residual the 2026-08-07 fix had explicitly disclosed as reused-not-recloned at the time, now finally recloned. `ux-critic` (elevated scrutiny) found two Majors, both resolved without blocking the fix: one turned out to be a non-defect once Main live-click-verified the reaction works regardless of a misleading visual "disabled" style; the other (a first-time-context UX rough edge, a field appearing to answer itself) was accepted as a provisional, disclosed limitation with a named follow-up, matching this project's own established shipping-with-a-named-deferral pattern. `reviewer` found zero Foundation violations and one further real, valid content-fidelity gap — the new Catalog clone shows real quantities where `inventory.md` §3.4's own "sin registrar" convention should apply, since this leg's merchant has named Products but never registered a Lot — routed to `ui-designer` as its own small fix. `reviewer` also named, explicitly deferred, a deeper pre-existing question: whether `184:1503` sits on the correct baseline state at all per `onboarding.md` §4's routing — not chased today.
*Impact:* Every primary path live-click-verified end to end by Main on fresh pages, including the exact hop the original tester finding reported broken. Documentation persistence gap (a third recurrence in this same fix family, after 2026-08-07 and 2026-08-10) closed in the same pass this time, not caught after the fact. Standing next step: fold in the small `981:620` content fix, then a fresh, isolated `merchant-user-tester` rerun of the complete Paid journey from the beginning.
*References:* `product/02b-medium-fidelity/CLAUDE.md` (2026-08-12 entry, "`184:1503` family reconvergence"); `company/infrastructure-decisions.md` ID015; `product/00-foundation/decision-log.md` D2, D3, D31, D32; `product/02-ux/inventory.md` §3.4, `home.md` §2/§3.3, `onboarding.md` §4.

---
**2026-08-12 — `184:1503` family round 2: a self-inflicted regression plus a fifth reconvergence leg, both closed; a correction to how ID001's Figma-skill-loading workaround actually works**
*Context:* The isolated Paid-journey rerun that closed round 1 (above) surfaced three further items on a deeper walk: the round-1 "sin registrar" content fix, applied to `981:620`, turned out to have more real callers than round 1 knew about — some legitimately pre-registration, some legitimately post-registration — so the single-content fix broke the post-registration ones as a side effect of fixing the pre-registration ones. Separately, a fifth, never-audited reconvergence leg surfaced: the family's own Eventos EventCard fed into `185:699`, a node shared with the original, unrelated Eventos list, whose downstream chain is the Free-tier baseline (no QR, "Tu plan: Gratis") — wrong for this Paid-tier family.
*Discovery/Decision:* Fixed via the established D31/D32 clone-don't-share discipline: `981:620` split by content-diverge clone (`1014:3292` for the real-quantity callers, `981:620` itself kept for the sin-registrar callers). For the `185:699` leg, investigated reuse-vs-clone on both axes before building anything — found `658:2766` (already built and correct, for an unrelated leg fixed 2 days earlier) matched both content and navigation needs exactly, per `events.md`'s own explicit text calling "Continuar Día 2" "the identical CTA and underlying computed logic" as its Home equivalent, "a second, equally valid entry point into the same single global selling state, not a separate implementation." Cloned only the shared node itself (`185:699`→`1017:3313`, nav-diverge only) and rewired it into the existing `658:2766` chain rather than building a fourth parallel selling/receipt chain. `ux-critic`'s elevated-scrutiny review of this fix caught one further Major on its own: a third node in the same causal chain (`988:3236`) had been misclassified into the wrong content bucket by the first fix, one hop past a sibling NavBar that got it right — a real regression inside the regression's own fix, caught before it shipped. **Two agent dispatches attempting this last remediation were blocked** by the standing `ReadMcpResourceTool`/`ListMcpResourcesTool` gap (`infrastructure-decisions.md` ID001): the first correctly stopped rather than guess; the second correctly *refused* Main's own inlined-skill-content workaround, citing its own agent-file rule that relayed text isn't equivalent to a real resource load — a legitimate objection this project's own ID001 entry had not previously drawn this sharply. The fix succeeded on a third attempt using ID001's actual documented mechanism (the local plugin-cache file, readable via plain `Read`/`Glob`, no MCP resource tool needed) — ID001 corrected in place to reflect that inlining is not a reliably-accepted workaround, the local-cache path is.
*Impact:* All three round-2 items built, `ux-critic`-clean (two passes), `reviewer`-clean (zero Blockers, zero Important findings — confirmed the `658:2766` reuse is grounded in explicit approved-spec text, not just uncontradicted by it), wiring-verified live end to end by Main via direct `chrome-devtools-mcp` click-through on fresh pages (`988:3229`→Inventario→`981:620` "sin registrar" confirmed; `988:3229`→EventCard→`1017:3313`→"Continuar Día 2"→`658:2766` confirmed). Two pre-existing, out-of-scope findings surfaced along the way and logged rather than fixed: `INV-MF-MAJ1` (missing per-Product marker/zero-stock dimming) now confirmed present on the two new nodes too; `INV-MF-MAJ2` (new) — a narrative contradiction between an EventCard's "not yet started" label and its own destination's "already sold" content, pre-existing on the untouched original node, classified Major. Standing next step: a third isolated `merchant-user-tester` rerun of the complete Paid journey from the beginning, to confirm round 2 holds — not yet dispatched as of this entry.
*References:* `product/02b-medium-fidelity/CLAUDE.md` (2026-08-12 entry, "`184:1503` family reconvergence, round 2"); `company/infrastructure-decisions.md` ID001 (sixth recorded instance, correction), ID004, ID007, ID008; `product/02-ux/ux-critic-findings.md` INV-MF-MAJ1, INV-MF-MAJ2; `product/02-ux/events.md`; `product/00-foundation/decision-log.md` D31, D32, D33, D40.

---
**2026-08-12 — `184:1503` family round 3: the literal onboarding-entry path, walked end-to-end for the first time, surfaces a picker-overlay reconvergence and a wrong-sibling redirect; two more Blockers caught and closed same-day**
*Context:* Rounds 1 and 2 (above) both walked the family's own dispatched-shortcut nodes and closed clean. A fresh `merchant-user-tester` walk starting from the actual onboarding entry point (`node-id=162:320` — Bienvenida through "Activar plan de pago" to Home, never previously walked end-to-end this session) surfaced a severe cluster: a stale non-zero sales total, phantom unregistered products, Playeras flipping 20→0 disponibles, a sale that didn't move the day's total, and Configuración stuck on "Tu plan: Gratis" despite Paid activation moments earlier in the same walk.
*Discovery/Decision:* A ground-truth trace (two attempts — the first lost to an infrastructure/API-connection error mid-work, not a logical block; the retry found and completed the partially-created nodes rather than duplicating them) found the real root cause: `988:3202`'s Lugar field, which a merchant must actually tap since it starts unset, opened a shared, never-cloned picker overlay (`162:1829`) whose rows hardcoded navigation into the exact legacy Free-tier/unrelated-persona chain this family's other legs had already been fixed away from — a fresh instance of the same reconvergence defect class, just one level deeper (nested inside a form field, not a top-level tab/CTA) than any prior round had reason to check. Fixed via the established clone-don't-share pattern (new dedicated Lugar/Tipo picker clones feeding into the existing dedicated confirmation chain). `ux-critic` caught a real Blocker in the fix's own first pass — the new "Lugar seleccionado" clone inherited its source's raw content rather than reflecting that Tipo was already set, making a genuine prior selection appear silently discarded — fixed and re-verified clean. A second, independent cluster of dead buttons/nav in the merchandise-registration leg was investigated and fixed in parallel. `reviewer` then caught a second real Blocker: one of those fixes redirected to the wrong content-diverge sibling (a fully-registered Catalog view instead of the required pending-tag-work state), making 7 still-untagged units invisible with no discoverable way back to resume tagging — `inventory.md` §3.17's own text confirms this card is the sole path back. Fixed using an existing template already in the file, closing the loop.
*Impact:* Every reaction-level claim from every fix this round was independently live-verified by Main via direct `chrome-devtools-mcp` click-through on fresh pages — not left as an unverified boundary the way two of the three review passes (lacking reaction-read tools) had to leave them. Two real Blockers caught same-day by two different specialist reviews before either reached a confirmation rerun — the review pipeline doing exactly the job it exists for. One new pre-existing Minor logged (`INV-MF-MIN2`, a document-wide picker-snapshot limitation, confirmed not a regression).
*References:* `product/02b-medium-fidelity/CLAUDE.md` (2026-08-12 entry, "`184:1503` family reconvergence, round 3"); `company/infrastructure-decisions.md` ID001 (sixth recorded instance), ID004, ID007, ID008; `product/02-ux/ux-critic-findings.md` INV-MF-MIN2; `product/02-ux/inventory.md` §3.5, §3.17; `product/02-ux/onboarding.md` §3.5b; `product/00-foundation/decision-log.md` D16, D20, D31, D32.

---
**2026-08-12 — `184:1503` family round 4: a third `658:2766` entry point ("Iniciar Sesión Rápida," never audited before) reused established-history content instead of a genuine first-session empty state; a Low-Fidelity spec gap escalated and resolved properly**
*Context:* Round 3's own confirmation rerun verified all three of that round's fixes hold, then surfaced a new leg: `184:1503`'s own "Iniciar Sesión Rápida" button had never been checked before — it pointed at `658:2766`, the same shared node whose *other* entry point (a genuine Día-2 continuation) was already fixed and reviewed in round 2. A brand-new merchant tapping this button directly, on her actual first-ever session, saw an established persona's stale sales total, three products she never registered, and a real product wrongly shown out of stock.
*Discovery/Decision:* Ground-truth traced first: exactly two entry points into `658:2766` exist, and no dedicated "fresh Día 1" state existed anywhere in this family — one had to be built. Fixed with a new 5-node content/nav-diverge chain showing a genuinely empty session (no history, only Ana's real inventory). This surfaced a real Low-Fidelity spec gap along the way: `home.md` had never defined what the session header shows during an active session with no linked Event — the build's own first attempt defaulted to a blank header, which `ux-critic` correctly refused to accept as a Figma-layer judgment call, escalating it per `product/02-ux/CLAUDE.md` §4's own rule instead of letting it stand. `ux-designer` resolved it properly: the header reads "Sesión rápida," reusing vocabulary the spec already established elsewhere rather than inventing new copy, persisted as new `home.md` §3.7b with full citations. `ux-critic` separately caught a second real Blocker in the same round — a "Cancel sale" confirmation screen that, once reached, reproduced the exact original defect via an ordinary, always-visible tap — fixed with its own dedicated clone.
*Impact:* Two real Blockers and one Major (the spec-gap) caught and closed same-day, before either reached a confirmation rerun — both review layers doing exactly the job they exist for. Main independently live-click-verified the entire new chain end to end via direct `chrome-devtools-mcp` interaction, not left as a self-reported claim. `home.md` gains a small, permanent spec addition (§3.7b) that will apply automatically to any future active-Session screen with no linked Event, closing a gap that could otherwise have resurfaced on yet another unaudited leg.
*References:* `product/02b-medium-fidelity/home.md` (2026-08-12 entry, "`184:1503` family: a third `658:2766` entry point"); `product/02-ux/home.md` §3.7b; `product/02-ux/CLAUDE.md` §4; `company/infrastructure-decisions.md` ID004, ID007, ID008; `product/00-foundation/decision-log.md` D15, D23, D31, D32; `architecture-principles.md` #3.

---
**2026-08-12 — Multi-agent architecture refactor approved and begun: lower token usage, clearer agent/Skill boundaries, explicit review-cycle exit criteria**
*Context:* The `184:1503` reconvergence-defect hunt above ran 5+ full rounds this same night, each round 3-6 background dispatches of 15-80+ minutes, before the Product Owner paused work to ask for a checkpoint — a direct symptom of two structural issues: individual dispatches re-reading large `product/00-foundation/` documents in full every time regardless of relevance (a single `reviewer`/`architect` dispatch was reading an estimated 25,000-30,000 tokens of Foundation content before even looking at the artifact under review), and several agent files duplicating the same procedural instructions (Figma-inspection method, severity taxonomies, the Consultation Pattern, the D31/D32 clone-vs-share test) almost verbatim across multiple files, with Main additionally hand-typing that same clone-discipline test into `ui-designer` dispatch prompts an estimated 15+ times this session alone.
*Decision:* A full architecture-map/token-consumption analysis was produced and approved, per the Product Owner's own principle: agents stay where autonomous reasoning, persona, exploration, or judgment is load-bearing; Skills absorb reusable procedures, checklists, and repeated operational instructions. Outcome: `planner` retired outright (zero real dispatches this entire session; its procedure — read `company/CLAUDE.md`/`backlog.md`, apply the stated ordering rule — is now the `backlog-prioritization` Skill, invoked by Main directly). `merchant-user-tester` explicitly preserved unchanged — its persona/trust-trajectory/adoption-signal framework is the point of the agent, and it structurally cannot load Skills anyway (no `Read`/`Glob`/`Skill` tool, by design, for knowledge isolation). `ux-critic`, `reviewer`, `ui-designer`, `knowledge-mentor`, `marketing` lightened, backed by new shared Skills, with real judgment kept inline in each. `architect`, `ux-designer`, `brand-guardian`, `builder` kept as full agents with only minor Skill extraction. The Product Owner additionally approved, in the same conversation, a second major direction: Figma's Medium-Fidelity work is now treated as a sufficient blueprint, not a gate requiring an exhaustively executable prototype — `ui-designer`'s mandate is redefined from "Medium-Fidelity Figma builder" to "translate approved UX/Foundation/behavior/visual identity into coherent product UI, using the appropriate execution medium," with the already-installed `frontend-design` Skill adopted starting now rather than deferred to an undated future High-Fidelity phase. Explicit review-cycle exit criteria were also approved (core-loop vs. secondary-path severity distinction, a 3-round cap on reactive tester-discovery rounds before a mandatory checkpoint, an exhaustive-caller-audit as the one sanctioned exception allowed to run to full closure, a continuously-maintained Known Limitations list) — see `company/CLAUDE.md`'s UX Remediation section.
*Impact:* This is Step 1 of a 9-step migration (full plan: `/Users/boofalcon/.claude/plans/i-d-like-to-introduce-streamed-snail.md`), each step its own commit, each with a defined regression test, fully revertible independently. Step 1 (this commit): `planner` removed, `backlog-prioritization` Skill added, every current-tense reference to `planner` across `company/CLAUDE.md`, `global-principles.md`, `architect.md`, `reviewer.md`, `ux-critic.md`, `marketing.md` corrected — historical references in `decision-log.md`/`bitacora.md`/RFCs left untouched per this project's own never-rewrite-history convention. A mandatory 8-point checkpoint (before/after architecture, agents removed/kept/lightened, Skills created, measured token reduction, regression-test results, `ui-designer`'s final boundary, the approved React-transition workflow, and any further pipeline recommendations) is required once all 9 steps land — the React/TypeScript vertical-slice experiment itself is explicitly NOT approved to start until the Product Owner reviews that checkpoint.
*References:* `/Users/boofalcon/.claude/plans/i-d-like-to-introduce-streamed-snail.md` (full approved plan); `.claude/skills/backlog-prioritization/SKILL.md`; `company/CLAUDE.md` (Autonomous coordination, Delegation, Consultation Pattern sections).

---
**2026-08-12 — Refactor Step 8: `nahui-core-principles` digest drafted, A/B-validated, one real gap found and closed before rollout to `reviewer`**
*Context:* Step 8 of the 9-step migration above — the highest-value, highest-risk step, per the Product Owner's own explicit safeguard: *"Before promoting the digest to the default loading path, run the A/B validation you proposed... The acceptance bar remains: zero missed Blocker/Important findings compared with the full-Foundation version. If it misses something material, revise the digest rather than rolling it out."* Drafted a ~1,350-token digest (`global-principles.md`/`architecture-principles.md` near-verbatim, plus a current-state — not chronological — distillation of `decision-log.md`'s most load-bearing, most-superseded invariants, since a chronological summary would risk re-surfacing retired rules like `loyaltyEnabled` as if still live).
*Discovery/Decision:* Ran the A/B test for real: two live `reviewer` dispatches against the same constructed artifact (a Settings snippet reintroducing the retired `loyaltyEnabled`-style toggle D40 removed) — one digest-only, one full-Foundation baseline. The digest-only pass caught the primary Blocker (the toggle) and an Important finding, but **missed a second real Blocker** the baseline caught: "Claims," a `ubiquitous-language.md`-scoped internal term, leaking untranslated into Spanish merchant-facing copy — the digest's Product Language section stated the general rule but didn't call out watching for specific internal domain nouns leaking into copy as its own recurring pattern. Per the acceptance bar, this sent the digest back for revision, not into rollout: added an explicit "actively check for internal/bounded-context terms leaking into merchant-facing copy" bullet naming Claim/InventoryUnit/SaleItem/Lot as examples. Re-ran the identical digest-only test — both Blockers now caught, matching the baseline exactly, with the one genuine open question (Free-tier read-only visibility) correctly named as a digest gap rather than guessed past in both runs. A second, independent test case (a domain note proposing an independently-stored `Business.nfcEnabled` field, contradicting D27's derivation rule) also passed clean on the first attempt, no revision needed.
*Impact:* Digest validated against 2 independent real-pattern test cases (matching this session's actual `loyaltyEnabled`-retirement and NFC-derivation history), zero missed Blocker/Important findings after the one revision. Rolled out to `reviewer` only, per the plan's staged rollout (`reviewer.md` now loads the digest as its fast-path Foundation source, with an explicit escape hatch to the full doc the moment a question isn't clearly settled) — `architect`/`ux-critic`/`ux-designer`/`ui-designer` are deliberately not yet switched over; that extension waits on observing one real `reviewer` cycle against the digest first, per the approved plan, not bundled into this step.
*References:* `.claude/skills/nahui-core-principles/SKILL.md`; `.claude/agents/reviewer.md`; `product/00-foundation/decision-log.md` D27, D40; `product/00-foundation/global-principles.md` (Product Language section).

---
**2026-08-17 — Nahui adopted as a standing AI-engineering laboratory alongside being a product; new architecture tiebreaker principle**
*Context:* Product Owner directive: regardless of commercial outcome, every significant architecture decision should also maximize learning of modern AI-engineering practices (agentic AI, long-term memory, multi-agent collaboration, RAG, MCP, and twelve other named areas) — explicitly not a mandate to force AI where it adds no customer value.
*Decision:* `architect` was consulted to integrate this without eroding two existing rules — `global-principles.md`'s merchant-facing simplicity philosophy, and principle 5's precedent for how a deliberate departure from "build only what's validated" must be scoped (named, logged, exception-only, never a silent bias). Result: `architecture-principles.md` principle 8 — a strict three-step gate (genuine need already justified → candidates genuinely comparable in cost/complexity on their own merits → only then does learning value break the tie), explicitly walled off from merchant-facing UX/UI and from the separate, senior Product Backlog Ownership sequencing order.
*Impact:* `architecture-principles.md` (+principle 8), `company/CLAUDE.md` (+"Secondary strategic objective" section), `product/00-foundation/decision-log.md` (D49, full reasoning + three open Business Decision flags for the Product Owner — schedule/cost latitude, whether this extends to backlog sequencing (it doesn't, as scoped), whether "comparable cost" excludes new recurring paid services), and a new running log, `company/ai-lab-decisions.md`, for recording each decision made under this lens and its eventual lesson learned. No entries yet — activates at the first genuine Stage 7/infrastructure tie, `product/03-build` hasn't started.
*References:* `product/00-foundation/decision-log.md` D49; `product/00-foundation/architecture-principles.md` principle 8; `company/CLAUDE.md` "Secondary strategic objective"; `company/ai-lab-decisions.md`.

---
**2026-08-19 — Video-campaign deep-linking closed as disproportionate; guided-instruction entry strategy confirmed permanent, not a stopgap**
*Context:* `merchant-validation-strategy-v2.md` §7 had left one question open since 2026-08-18: whether a true in-app deep link (a URL dropping a demo participant directly into `Inventario > Asignar tags`, skipping Authentication) was architecturally sound, as an enhancement layered on top of the already-buildable guided-instruction baseline for the video-campaign roadmap's Wave 1. Triggered by the Product Owner's real-time question about why 96 Ads-Manager landing-page views on the live campaign had produced only 3 Google Form responses, which surfaced this as the actual blocker still sitting on the video-campaign roadmap (§10 item 2).
*Discovery/Decision:* `architect` ruled it disproportionate for this scope, not merely deferred: true deep-linking would require loosening `demo-mode.md`'s own "no domain write" invariant (load-bearing to every prior Architecture Review of that document), editing Merchant Application screen components (`App.tsx`, `InventoryScreen.tsx`) to accept externally-injected navigation state — a direct breach of `decision-log.md` D48's placement discipline — and a new URL-param-to-domain-object seeding layer duplicating invariants `domain-model.md` already governs elsewhere. The guided-instruction baseline (welcome-screen copy naming the flow to try, pointing to the already-Approved "Ver un ejemplo" seeded path) is confirmed the correct permanent answer for this scale of campaign work, closing §7/§10's open question rather than carrying it forward.
*Impact:* `merchant-validation-strategy-v2.md` §7 and §10 item 2 updated to record the ruling as resolved, unblocking roadmap item 3 (drafting Campaign A/B video scripts). Separately, the same real-time question prompted adding a count-only click event (`demo_questionnaire_cta_click`, via the already-mounted `@vercel/analytics`) to the reminder banner's questionnaire CTA (`product/02c-high-fidelity-prototype/src/screens/DemoMode/ReminderBanner.tsx`), per §13.5's own recommendation — the 96-views-to-3-responses funnel currently can't distinguish "never reached/tapped the CTA" from "opened the Form and bounced," and this is the cheapest available way to start telling those apart without any change to the Form or delivery mechanism (§12's Option A verdict untouched).
*References:* `company/merchant-validation-strategy-v2.md` §7, §10, §13.5; `product/00-foundation/decision-log.md` D48; `product/02-ux/demo-mode.md`; commit `1525e7a`.

---
**2026-08-19 — "Modo demo" banner label shipped; Campaign A/B video scripts drafted and cleared; H5 validation-process hypothesis added to track whether Demo Mode actually worked**
*Context:* Continuing the same day's Demo Mode work: the Product Owner asked to add a persistent "Modo demo" state indicator to the reminder banner (session-wide legibility that this is a test, not the live product), approved marketing's Campaign A/B video-script draft for the now-unblocked video wave, and asked for a new Learning Agenda hypothesis (H5) testing whether the cumulative Demo Mode changes actually reduced validation-environment confusion, as opposed to product friction.
*Discovery/Decision:* The "Modo demo" banner amendment went through four remediation rounds — not because the underlying design was wrong, but because closing one honest disclosure (a referent-ambiguity fix on "8-12 min") repeatedly required propagating the same fact to sibling passages describing row 2's wrap behavior; each round closed a genuine self-consistency gap the previous round's own fix had introduced, no design decision was ever reopened. Built into the live prototype (`ReminderBanner.tsx`), where `ui-designer` found and fixed a real latent bug in the same pass: the banner's height was a hardcoded CSS constant that would have caused the `position: fixed` banner to overlap the screen once row 2 could wrap taller — replaced with a `ResizeObserver` that measures real height. `ux-critic`'s code-level review found one further Minor (a stale `ResizeObserver` left un-disconnected on the restart-failure branch, fixed) and one open question (possible CTA truncation at 280px) that Main resolved directly via live browser verification on both localhost and production `demo.nahui.app` — no truncation, and the busiest active-session screen stays comfortably clear of the wrapped banner even at 280×568. Separately, `brand-guardian` reviewed the Campaign A/B video scripts and found a real Blocker across three rounds: "NFC" leaking into participant-facing copy (a video caption, a welcome-screen sentence, and a questionnaire checkbox row), each fixed to "tag" to match the product's own actual vocabulary — plus a live-verified button-label correction ("Iniciar Sesión Rápida" → the prototype's actual "Iniciar Venta Rápida"). Finally, `marketing` added Hypothesis E ("H5," Product Owner's own reference) to the Meta Ads Learning Agenda: an explicitly validation-process (not Product) hypothesis, with a composite evidence threshold grounded in the project's existing default threshold convention, and an explicit statement that it's unevaluable below 10 new Demo-Mode-era respondents (20 for confirmation) — the current n=2 respondents predate every Demo Mode amendment and serve only as its baseline.
*Impact:* `product/02-ux/demo-mode.md` Approved and built (commits `590e6fd`, `a1234b5`); `company/merchant-validation-campaign-videos.md` created and `brand-guardian`-clean (commit `4f13dea`); H5 added to `company/merchant-validation-campaign-meta-ads.md` §9 and cross-referenced from `merchant-validation-strategy-v2.md` (commit `83292b4`). Video scripts await Product Owner sign-off before recording; H5 is untested and tracked forward, not assumed true.
*References:* `product/02-ux/demo-mode.md`; `company/merchant-validation-campaign-videos.md`; `company/merchant-validation-campaign-meta-ads.md` §9 (Hypothesis E); `company/merchant-validation-strategy-v2.md` §0, §8, §13; commits `590e6fd`, `a1234b5`, `4f13dea`, `83292b4`.

---
**2026-08-27 — Concierge/DM pilot reframed from CP1-CP5 (funnel abandonment) to H1 (problem-fit) after a code/production audit showed the two questions need structurally different populations**

*Context:* The Product Owner traced a live Vercel Analytics dashboard down to an exact one-hour window (19:00 Aug 19 onward, when the three newest `demo_*` events actually shipped) and found 227 real, fully-instrumented post-deploy visitors produced only 2 pass-through events — both self-confirmed as the Product Owner's own test sessions, not external merchants. Combined with an independent code audit of every `demo_*` `track()` call (all six live in `ReminderBanner.tsx`, confirmed correctly queuing but three of the five original events not yet live for part of the original campaign window), this settled a question `reviewer`/`marketing`/`architect` had already flagged earlier the same night: the Concierge/DM pilot's original objective (diagnose why self-serve demo visitors abandon, CP1-CP5) cannot be answered by a DM instrument — a DM respondent is self-selected as willing to engage, categorically different from someone who bounces silently before any action. 225-of-227 never took a single action.

*Discovery/Decision:* Reframed `company/merchant-validation-concierge-pilot.md` around `market-validation.md`'s H1 (registration-friction generalization) instead, via a dated §7 amendment (not a silent edit — `reviewer` caught that the first pass of this edit deleted the original CP1-CP5 definitions rather than preserving them per the document's own locked-content rule; corrected by restoring them verbatim as a retained historical record, §7.6, matching the precedent `market-validation.md`'s own retired §7 already set). New two-loop design (§0): Loop 1 is the DM conversation itself, now explicitly scoped to H1/problem-fit signal (PF1-PF5, recoded from CP1-CP5's evidence-weighting/threshold mechanics, not reinvented); Loop 2 reuses the existing, *unmodified* self-serve demo and its instrumentation for merchants Loop 1 already qualified as genuinely interested — a materially different, more useful signal (where does an already-interested merchant struggle) than an anonymous bounce rate, and one that doesn't need any new tracking infrastructure (Pixel/backend), since the DM conversation itself is the identification/consent/handoff mechanism.

A related product proposal — the Product Owner's own idea that the demo should lead with a completed sale (a pre-loaded catalog) before any setup, for DM-qualified merchants — was evaluated in parallel by `architect` and `ux-designer`. Finding: no domain-model blocker (the write path already exists, proven via the "Ver un ejemplo" Onboarding path), but "Ver un ejemplo" is the wrong vehicle — it's explicitly non-convertible/disposable (`decision-log.md` D19), while this proposal wants a real, kept Business. `ux-designer` flagged a sharp cross-cutting risk: even "Ver un ejemplo" sits behind Authentication today, and skipping phone/OTP for this population (never stated as intended, but a real ambiguity in the original proposal) would foreclose ever learning whether phone-entry is a barrier from a population genuinely worth asking — directly undermining the pilot's own future usefulness. Logged as `product/02-ux/product-decisions.md` Q19, deferred by design: launch the two-loop pilot on the existing flow first, let Loop 2's own instrumentation show whether DM-qualified merchants actually stall in onboarding before building a new path speculatively.

*Impact:* `company/merchant-validation-concierge-pilot.md` substantially amended (supersession note, §0 two-loop design, §7 PF1-PF5 reframe with CP1-CP5 preserved at §7.6, front-loaded-incentive/respondent-led-exit rewrite of §6.1/§6.6, two skip-rule additions, stale video-asset reference corrected to `campaign-b-final-v3.mp4`). `product/02-ux/product-decisions.md` Q19 added; `product/02-ux/onboarding.md` §11 cross-referenced. Still design/proposal only — nothing authorized to launch. Two items explicitly not yet done, flagged for the Product Owner: §6.1/§6.6's new copy needs its own `brand-guardian` pass (the 2026-08-20 review predates this rewrite); neither file is committed yet.
*References:* `company/merchant-validation-concierge-pilot.md` (full document); `product/02-ux/product-decisions.md` Q19; `product/02-ux/onboarding.md` §11; `product/00-foundation/decision-log.md` D19; `company/market-validation.md` §1/§6 (H1); `company/merchant-validation-funnel-diagnosis.md`.

---
**2026-08-27 — Demo Day pitch deck rebuilt to 11 slides (Viabilidad + Gobernanza added, sourced verbatim from the already-submitted course deliverable), one review-fix cycle closed clean**

*Context:* The Product Owner, reviewing the guion against the already-submitted `Nahui - Documento Ejecutivo Demo Day.docx`, found ~6 minutes of unused pitch time and identified that Business Case/Risks (Word §4/§5) had no corresponding slide, plus a real visual redundancy — slides 3 and 4 of the existing deck use the same underlying screen (verified by inspecting the pptx's own embedded images directly: same "Venta rápida" layout, cards, and colors, differing only in transaction state).

*Discovery/Decision:* Guion (`evidence/2026-08-22-demo-day/guion-pitch-10min.md`) rewritten to 11 sections: slides 3+4 merged into one before/after diptych, a new instrumentation caveat added to the campaign slide (227-of-583 post-deploy detail, consistent with the concierge-pilot audit above but without the internal-testing detail per the Product Owner's explicit instruction — that stays in `bitacora.md`/internal docs only, not the slides), and two new sections (Viabilidad, Gobernanza) with every number sourced verbatim from the already-submitted Word document specifically to avoid any discrepancy between deliverables. `ui-designer` rebuilt the pptx to match; `ux-critic` found 2 Major (table text on the two new slides measurably under the deck's own 14pt floor; the new "Gobernanza" slide title colliding with the term's different, pre-existing meaning on slides 8/10) + 2 Minor (a risk-severity color using the brand's generic accent instead of its Error token; a contrast shortfall on the new campaign-slide caveat line) — all four fixed and re-verified via pixel-measurement against fresh renders in one remediation round.
*Impact:* `evidence/2026-08-22-demo-day/Nahui - Pitch Demo Day v2.pptx` now 11 slides, `ux-critic`-clean. Neither the guion nor the pptx is committed yet — pending the Product Owner's own review, per her explicit request to see both fresh in the morning.
*References:* `evidence/2026-08-22-demo-day/guion-pitch-10min.md`; `evidence/2026-08-22-demo-day/Nahui - Pitch Demo Day v2.pptx`; `evidence/2026-08-22-demo-day/Nahui - Documento Ejecutivo Demo Day.docx` §4/§5.

---
**2026-08-30 — Figma removed entirely from Nahui's toolset (D50), closing a gap between stated policy and actual usage**

*Context:* Reviewing which of the project's 12 Nahui-authored Skills were still needed surfaced a real question: D42 (React-first workflow) had demoted Figma to an optional medium (brainstorming, marketing assets, diagrams), but a repository check showed zero real Figma work had happened since D42 shipped — the last commit touching `product/02b-medium-fidelity/` was D42's own adoption commit itself. The Product Owner decided to close that gap by removing Figma entirely rather than leaving an unused optional allowance in place.
*Decision/Discovery:* `decision-log.md` D50 records the full change. Removed: Figma tool grants from `ui-designer`/`ux-critic`/`reviewer`; the two Nahui-authored Figma-specific Skills (`figma-clone-discipline`, `figma-review-methodology`), deleted outright per the same precedent set by `planner`'s own retirement; the Figma-specific trigger clause in `horizontal-journey-review`; and every active-governance reference in `company/CLAUDE.md`/`product/02-ux/CLAUDE.md` treating Figma as an available execution medium or wiring layer. Historical records (this log's own past entries, `decision-log.md`'s past entries, `company/business-decisions.md`'s closed items) were deliberately left untouched, per the project's standing non-rewrite-of-history discipline.
*Impact:* `ui-designer` now builds exclusively in React/TypeScript (`product/02c-high-fidelity-prototype/`), no Figma fallback. Two real follow-ups flagged, not silently actioned: the Merchant Experience Kit's persona content lived only as a FigJam board and needs migration to a plain document if still needed; `company/usability-testing-plan.md` (built around a Figma click-through prototype) is already superseded in practice by `merchant-user-tester`'s direct React-prototype testing, but hasn't been formally rewritten.
*References:* `product/00-foundation/decision-log.md` D50; `.claude/agents/ui-designer.md`, `ux-critic.md`, `reviewer.md`; `.claude/skills/horizontal-journey-review/SKILL.md`; `company/CLAUDE.md`; `product/02-ux/CLAUDE.md`; `company/active-artifacts.md`.

---
**2026-09-03/04 — Acceso DM shipped: value-first onboarding for the DM pilot, after a full-night reframe away from Q19-Q22's conversion-based design**

*Context:* A pre-pilot readiness audit found Q19/Q20/Q21 (a fourth, convertible Onboarding path for DM-qualified merchants) marked "Resolved" but never actually persisted into spec or code, and a stalled architect check on the auth-timing mechanism. The Product Owner authorized Main to resume that check and implement autonomously overnight. Mid-sequence, Main resolved a genuine conflict between `architect` (exclude the trial Sale from every Resultados aggregation) and `ux-designer` (show it with full parity) via a synthesis — logged as Q22 — flagged explicitly for review since it was made without the Product Owner present. The Product Owner then corrected the premise directly: the demo state never needs to convert into a real Business/account at all, which dissolved the entire `Sale.origin`/replay-at-conversion/synthetic-Session apparatus Q22 depended on. A further live-verified finding (Resultados requires the Session closed, not merely a Sale finalized — confirmed via a real Playwright walkthrough of "Ver un ejemplo") and a hard boundary (no change to real authentication architecture; a fixed demo-only credential instead) shaped the final, much smaller design.
*Discovery/Decision:* `product/02-ux/acceso-dm.md` (new, Approved) — a runtime `?acceso=dm` URL-detected route composing the existing `verifyOtp`/`completeOnboarding('demo')`/`setBusinessIdentity`/`commitLot` writes unmodified, landing a DM-qualified merchant directly on the same seeded "Ver un ejemplo" catalog with zero Authentication/Onboarding screens, plus a passive Home-only nudge explaining the session-close requirement. `decision-log.md` D51 records the full architecture reasoning and a reusable placement ruling (production-shipped-but-reachable-only-via-curated-link is a *third* documented failure mode of D13/D45's compound "genuinely part of the app" test, alongside D48's build-artifact clause). Full review pipeline — `architect` (auth/D44 boundary, a real `commitLot` retry-safety gap caught and fixed, the placement ruling), `ux-critic`, `brand-guardian` (one forbidden-imperative copy fix), `reviewer` — ran twice, once on the spec and once on the built code, both passes clean. Q19-Q22 marked Superseded in `product-decisions.md`, not deleted; Q20/Q21 decoupled and confirmed as independently real, unblocked product improvements.
*Impact:* Shipped and live-verified end-to-end via the dev server (fresh state → seeded sale → inventory change → session close → Resultados; reload-idempotency; production-bundle isolation from the demo-campaign build). Two items deliberately left open, not silently resolved: whether/how `merchant-validation-concierge-pilot.md`'s own DM handoff script gets pointed at this route (a real tradeoff — doing so loses `demo-mode.md`'s entire `demo_*` analytics suite unless separately re-instrumented) — a Product Owner call, not made here; and the fixed bypass credential's production-shipping risk, deferred to Backend Integration. A separate, unrelated real-merchant finding (request for concurrent multi-seller selling at one Event) was explicitly kept out of this workstream's scope and logged instead as a Product Discovery item in `company/backlog.md`.
*References:* `product/02-ux/acceso-dm.md`; `product/00-foundation/decision-log.md` D19, D44, D48, D51; `product/02-ux/product-decisions.md` Q19-Q22 (Superseded), Q20, Q21; `product/02-ux/onboarding.md` (capability-table cross-reference); `company/backlog.md` (Product Discovery section); `product/02c-high-fidelity-prototype/src/screens/AccesoDM/`.

---
**2026-09-04 — `demo_*` analytics suite confirmed live in production (previously undocumented), closing a false "instrumentation gap" flag**

*Context:* Preparing Ads Manager launch instructions, `marketing` searched `bitacora.md` for a record confirming all six `demo_*` events were actually delivering to the live Vercel Analytics dashboard (not just verified locally in dev). It found a record for only one (`demo_questionnaire_cta_click`, confirmed 2026-08-19) and flagged the other five as an unconfirmed instrumentation gap — a real, correct finding *given what was written down*, since no such confirmation had ever been logged, even though the Product Owner had, in fact, already checked the live dashboard herself at some earlier point.
*Discovery:* The Product Owner shared a screenshot of the live dashboard showing real, accumulated production counts — `demo_pass_through_reached` (11 visitors/27 total), `demo_onboarding_completed` (8/12), `demo_otp_completed` (8/12), `demo_sale_completed` (7/15), `demo_questionnaire_cta_click` (2/2) — clearly real campaign traffic, not a single test session. `demo_paid_plan_activated_midsession` wasn't visible in the screenshot; not yet confirmed either way.
*Why this happened, stated plainly:* not a memory-system failure — the persistent memory/bitácora system can only retain what someone actually writes down. This specific check (Product Owner reviewing the live dashboard) was never logged anywhere, so there was nothing for any future session or agent to recall. The actual gap is a coordination one: Main should proactively ask "what did the dashboard show, exactly?" and log it immediately whenever a live verification like this happens, rather than letting a real confirmation live only in conversation.
*Impact:* Five of six `demo_*` events reclassified from "unconfirmed" to "confirmed live in production" as of this entry. The Ads Manager guide's "before gastar un peso" flag is corrected accordingly. `demo_paid_plan_activated_midsession` remains genuinely unconfirmed pending a look at the rest of the dashboard.
*References:* `product/02-ux/demo-mode.md` §2.5 (event definitions); the Ads Manager setup artifact (this conversation).
