# 0010 — Event-scoped inventory allocation: commitment lifecycle correction

Status: Proposed. Corrects `product/99-rfc/0009-event-scoped-inventory-allocation.md` §3(b) specifically — the manual/quantity-mode allocation mechanism, and only that mechanism. Every other part of RFC 0009 (§1 `EventAllocation` as a new Selling-context aggregate root; §2 `AllocationMovement` as an internal-only ledger entity and its `counterpartEventAllocationId` correction; §3(a) NFC allocation-time exclusivity via `InventoryUnit`'s existing `available→reserved` conditional write; §4 the single-local-transaction reallocation mechanism; §5 `Session.eventId` resolution; §6 Selling-only bounded-context placement; §7 the sibling-RFC scope boundary; §8 ubiquitous-language additions) remains in force, unmodified, not rewritten here. `decision-log.md` D57 (RFC 0009's promotion record) is likewise unaffected in every respect this RFC doesn't name explicitly.

## The idea

RFC 0009 named two independent mechanisms enforcing the physical-location-exclusivity invariant: (a) NFC allocation-time exclusivity, a real conditional write flipping the specific scanned `InventoryUnit` from `available` to `reserved`; (b) manual/quantity-mode consumption exclusivity, a compare-and-swap on a disconnected counter, `EventAllocation.quantityRemaining`, that never touches `InventoryUnit.status` at all.

This asymmetry is a confirmed defect in the built Slice 12 prototype code (`product/02c-high-fidelity-prototype/`), not a hypothetical — reproduced by the Product Owner on the deployed prototype, not merely traced in the abstract. A merchant reserved 10 units for a future Event; the same-day Quick Sale sold into that reserved stock, because Quick Sale's FIFO logic reads `InventoryUnit.status = available` and had no reason to know a counter elsewhere claimed 10 of those units were spoken for. The counter only protects the one write path that explicitly checks it — it never actually removes committed units from the shared pool every other consumption path reads from. (This prototype is deliberately pre-backend per D41–D43 and `product/02c-high-fidelity-prototype/CLAUDE.md` — RFC 0009 itself names the real compare-and-swap as a Stage 7/Backend Integration requirement. The defect is real and confirmed at the prototype-code layer; this RFC's correction targets that same layer, not a claim about a live backend system that doesn't yet exist.)

The correction generalizes mechanism (a) to manual mode instead of leaving (b) as a counter: manual/quantity-mode commitment becomes a real, row-level `InventoryUnit` status flip, the same class of write NFC allocation already performs. This closes the exact class of bug the confirmed prototype defect exposed, and it does so without touching RFC 0009's aggregate boundary, bounded-context placement, or reallocation-transaction mechanism — none of which caused or need to change to fix this.

This RFC also separates soft planning intent from hard commitment (`quantityPlanned` vs. `commitAllocation()`), and states a standing modeling invariant (logical vs. physical identity) that the correction depends on but that outlives this RFC's own mechanics.

**§8's Event-close design has been reworked (Product Owner direction, 2026-09-09), superseding this document's own originally-proposed split-release design.** The original draft auto-released `fifo_assignment`-sourced units silently at Event close — architecturally defensible per §7's logical/physical-identity invariant, but rejected as a *product* decision: `events.md` §3.16's own "no silent auto-return to general inventory on Event close" is one unconditional principle, not one that bends by commitment mode. The corrected design (§8, below) introduces no auto-release for either mode, and — because manual mode never previously had any real reserved units to reconcile (the original counter-based mechanism made "reconciliation" structurally meaningless for it) — designs a genuinely new capability: a deliberately lightweight, quantity-based merchant reconciliation action for manual/untagged stock, symmetric in *requirement* (an explicit action is always needed) but not in *evidence* (quantity-confirmed, not unit-identified) to NFC's existing, unchanged two-button reconciliation.

## What it touches

- `domain-model.md`:
  - **`EventAllocation` schema** — gains `quantityPlanned` (new field). `quantityRemaining` is retired as a *stored* field — it becomes a read-time derivation, never written independently. `allocatedUnitIds` gains a second population source (manual/FIFO commitment, not NFC-scan-only as today). `quantityAllocated` is unchanged in shape and meaning. Gains a new operation, `commitAllocation()`.
  - **`AllocationMovement` schema (`domain-model.md`'s "Not aggregate roots" entry)** — `unitIds` becomes populated on every row, not only NFC-mode rows. Gains two new fields: `unitSource: scan | fifo_assignment`, and (Product Owner rework, 2026-09-09) `quantityExpected` (nullable integer, populated only on manual-mode reconciliation writes — see §6/§8/§11).
  - **`NFCTag`** — gains a new field, `assignedAt` (`ubiquitous-language.md`'s NFCTag entry and `domain-model.md`'s entity-relationships diagram annotation both need this addition; `domain-model.md` carries no separate NFCTag schema block today to diff against beyond that annotation).
  - **Key mechanisms** — "Physical-location-exclusivity invariant" is amended: mechanism (b) is rewritten from a `quantityRemaining` compare-and-swap to `commitAllocation()`'s real conditional row-flip, generalizing mechanism (a) rather than standing beside it as a structurally different kind of guard. Two new mechanisms are added: "Logical vs. physical identity" (a standing invariant, not RFC-specific) and "Event close: no auto-release, explicit reconciliation for both modes" (replacing the implicit "reconciliation reverses (a)" one-liner RFC 0009 left for the merchant-initiated path only — and, per the 2026-09-09 rework, replacing this RFC's own earlier, now-superseded split-release-by-`unitSource` draft of that same mechanism).
  - **Lifecycles** — `InventoryUnit`'s existing lifecycle comment (`available → reserved (in an open Sale, or held by an open EventAllocation) → sold`) is unaffected — this RFC introduces no new status value, only a new caller (`commitAllocation()`) of the existing conditional write, the same non-edge RFC 0009 §3(a) already established for the NFC path.
  - **Bounded contexts** — unaffected. No new context, no new dependency edge. `commitAllocation()` is a Selling-internal operation performing the identical class of write Selling already legitimately performs into `InventoryUnit.status` (FIFO consumption D5, Sale finalization, and RFC 0009 §3(a)'s own NFC allocation-time write) — a new *caller*, not a new edge, the same distinction RFC 0009 §6 already drew for its own NFC mechanism.
- `ubiquitous-language.md`: `EventAllocation` entry amended (`quantityPlanned` added; `quantityRemaining` restated as derived, not stored). `AllocationMovement` entry amended (`unitIds` now unconditional; `unitSource` added). `NFCTag` entry amended (`assignedAt` added; the logical/physical identity distinction referenced).
- **Not touched:** `architecture-principles.md` — no new numbered principle. Existing #2 (aggregate boundaries follow write-throughput, not just conceptual nesting) and #7 (idempotent/keyed retriable writes) remain the lens applied to `commitAllocation()`'s transaction shape and (per the 2026-09-09 rework) the new manual-mode reconciliation write, the same restraint RFC 0009 itself showed rather than manufacturing a new principle for a correction existing principles already cover. `information-architecture.md` (unaffected — no nav change). `events.md` (the Approved §3.16/§3.25 reconciliation UX for NFC-tagged rows is unchanged and stays exactly as designed; the manual/untagged-row behavior needs its own amendment because it now requires a new, deliberately lightweight, quantity-based merchant action — distinct from NFC's two-button pattern — rather than resolving automatically as this document previously (and incorrectly) assumed — routed to `ux-designer`, not resolved here; see Open Items). RFC 0009 §1/§2/§3(a)/§4/§5/§6/§7/§8 (all reused unmodified, per the header above).

## Why

### 1. Root cause, restated precisely: a disconnected counter, not a row-level reservation

RFC 0009 §3(b) reads `EventAllocation.quantityRemaining` as "the actual selling gate," decremented via `UPDATE EventAllocation SET quantityRemaining = quantityRemaining - 1 WHERE id=? AND quantityRemaining > 0`. This write is a genuine, correct compare-and-swap — on the wrong object. It gates future reads of `quantityRemaining` itself, but it never writes `InventoryUnit.status`, so every other consumption path (Quick Sale's FIFO, another Event's own manual commitment) that reads `InventoryUnit.status = available` directly sees ten fully "available" units the counter alone believes are spoken for. A counter only protects paths that explicitly check it; it was never capable of removing committed units from the shared pool other paths read from. This is the confirmed, reproduced failure mode this RFC exists to close, not a hypothetical hardening pass.

### 2. `quantityPlanned` (new field) vs. hard commitment — `commitAllocation()`

`EventAllocation` gains `quantityPlanned` (integer, optional, default `0`) — soft intent, freely settable or adjustable at any time, with zero effect on the shared `InventoryUnit` pool. An Owner planning six months ahead for a future Event can state "I expect to bring 20 Hoodies" without locking a single physical unit — nothing about that number touches `InventoryUnit.status`, and it participates in no read-time gate, no FIFO exclusion, no compare-and-swap. It exists purely so the Owner can record and revise intent before committing.

Actual hard commitment is a new operation, `commitAllocation(eventAllocationId, quantity)`. It performs a FIFO-ordered conditional flip of `N` `InventoryUnit` rows matching `available AND untagged` (i.e., `status='available' AND tagId IS NULL`, ordered by the owning Lot's receipt date — the same FIFO ordering D5 already establishes) to `reserved`, inside one local transaction that also appends the specific flipped unit IDs to `EventAllocation.allocatedUnitIds`, increments `quantityAllocated` by the actual number flipped (which may be less than `N` if available stock is insufficient — the same partial-fulfillment shape ordinary FIFO consumption already tolerates), and writes one `AllocationMovement` row (`unitSource = fifo_assignment`, `unitIds` = the flipped set). The `untagged` filter is deliberate, not incidental: it keeps manual/FIFO commitment out of the pool NFC allocation earmarks for scan-based consumption specifically — RFC 0009 §3(a)'s own "the tagged/allocated units are earmarked for scan-based consumption specifically, the untagged units remain FIFO-eligible" reasoning, reused unmodified, now also protecting `commitAllocation()`'s own pool from double-claiming a tagged unit.

This is a direct generalization of mechanism (a), not a parallel new mechanism. RFC 0009 §3(a) already established the pattern (a conditional `available→reserved` write, zero-rows-affected meaning "already sold, mid-Sale, or allocated elsewhere," no new field or status value on `InventoryUnit`, Inventory's own schema staying completely unaware of *why* a unit is `reserved`); `commitAllocation()` is that identical pattern with a new caller and a FIFO `LIMIT N` instead of a single scanned ID.

### 3. Retiring `quantityRemaining` as a stored field

`quantityRemaining` stops being written at all. It becomes a read-time derivation: the count of `EventAllocation.allocatedUnitIds` entries whose referenced `InventoryUnit.status` is still `reserved`. This closes the exact class of bug the confirmed prototype defect exposed — a cached counter that can drift from the real committed set, because nothing forces it to stay in sync with the row-level truth once more than one write path exists. Once `allocatedUnitIds` is the one real committed set (§4, below) and every unit in it is `reserved` for exactly as long as it's genuinely held, `quantityRemaining` is always correct by construction — there is no longer a second, independently-writable number that can disagree with reality.

This also simplifies what "the selling gate" means: it is no longer a number checked by a bespoke compare-and-swap at all. The gate is the ordinary `available`-status filter every consumption path (FIFO, NFC scan) already applies — the same filter mechanism (a) always used. `commitAllocation()`'s job is entirely upstream of that: making sure committed units genuinely stop being `available` the moment they're committed, so the existing filter does the rest correctly, for every consumer, not just the one that remembers to check a counter.

### 4. Extending `allocatedUnitIds` to both modes

Today only the NFC path populates real unit IDs into `allocatedUnitIds`; manual/FIFO commitment must populate the identical array with the specific units `commitAllocation()` flipped. This is what makes §3's derivation well-defined and what makes both modes share one real, row-level committed set per `EventAllocation` — not two structurally different bookkeeping mechanisms wearing the same aggregate. Two modes composing on the same Product row (RFC 0009's own "the two compose on the same Product row, never a forced choice," `ubiquitous-language.md`'s existing `EventAllocation` entry) now also means they compose on the same underlying array of real committed units, not a shared row alongside an independent, disconnected counter.

### 5. `NFCTag.assignedAt`

A new field on `NFCTag`, recording when a tag's physical attachment window to its `InventoryUnit` began. `ubiquitous-language.md`'s current NFCTag definition ("a physical tag's UID, attached 1:1 to an InventoryUnit... Consumable — it leaves with the customer after the sale") carries no timestamp today; this RFC adds the one this correction's own invariant (§6, below) depends on. In the currently-Approved UX, tags are consumable and not detached/reassigned — `assignedAt` is written once, at the scan that attaches the tag, and stays fixed for that tag's lifetime under this RFC's own scope. The field is stated generally (through "detachment or reassignment") because it names a standing invariant, not a mechanism scoped only to what's built today; no detachment/reassignment write path is introduced or assumed built by this RFC.

### 6. `AllocationMovement` correction — `unitIds` unconditional, `unitSource` added

RFC 0009 §2's schema carries `unitIds` only for "NFC-mode movements," `null` for quantity-based ones. Since §4 above makes both modes populate real unit IDs, every `AllocationMovement` row must always carry the specific `unitIds` it affected — a manual/FIFO `commitAllocation()` write is no longer a quantity delta with no row-level trace; it's the same kind of discrete, unit-identified action an NFC scan already produces. `quantityDelta` is retained as a convenience/audit figure (its sign and magnitude derivable from `unitIds.length` and the movement `type`), not removed, but it stops being the only record of what a manual-mode movement actually did.

A new field, `unitSource: scan | fifo_assignment` (closed set), records how each specific unit entered `allocatedUnitIds` via that movement. This is the field the Event-close release logic (§8, below) filters on. Without it, a single `EventAllocation` row that has accumulated both scanned and FIFO-assigned units into one shared `allocatedUnitIds` array (exactly the outcome §4 intentionally produces) has no way to release only the FIFO-assigned portion — `EventAllocation` itself carries no per-unit provenance, only the flat array; `AllocationMovement`'s own ledger, one row per discrete write action (RFC 0009 §2's own "written unconditionally, one row per discrete write action" discipline, unmodified), is where that provenance actually lives. Since each unit enters a given `EventAllocation`'s `allocatedUnitIds` via exactly one originating movement (the `initial_allocation`/`replenish`/`reallocate_in` write that added it), the close-time release logic resolves a unit's `unitSource` by looking up its originating `AllocationMovement` row — a join against the ledger, not a new field on `EventAllocation` or on `InventoryUnit` itself. This keeps `InventoryUnit`'s schema exactly as untouched as RFC 0009 §3(a) already insisted on ("Inventory's own schema stays completely unaware of *why* a unit is `reserved`") — provenance is Selling-internal ledger data, never leaked onto the Inventory-owned row.

**Revision (Product Owner direction, 2026-09-09): one further field, `quantityExpected`.** §8's reworked Event-close mechanism needs the ledger to distinguish a reconciliation-triggered release from an ordinary mid-Event `adjustment` stepper decrease, and to record what the merchant was actually asked to confirm against, not merely what she confirmed. Two changes, both minimal: (1) the manual-mode reconciliation write reuses the *existing* `type = return_to_general` value (no new enum member — the identical value NFC reconciliation already writes, now also written by a second, structurally different underlying mechanism, the same pattern already established for `initial_allocation`/`replenish`/`adjustment` being written by both a scan and a `commitAllocation()`/`releaseAllocation()` call depending on mode); (2) `AllocationMovement` gains `quantityExpected` (nullable integer; populated only when `type = return_to_general AND unitSource = fifo_assignment` — the manual-mode reconciliation call site specifically; null on every other movement, including NFC's own `return_to_general` rows, which carry no expected/confirmed split since NFC reconciliation always resolves the full, already-known remaining set by construction). A row reading `type=return_to_general, unitSource=fifo_assignment, quantityExpected=3, quantityDelta=-2` is legible on its own — "she was asked to confirm 3, confirmed 2" — without joining against subsequent live state. This is the smallest fix that satisfies the requirement: no new entity, no new `type` value, one nullable field reusing the exact convenience/audit role `quantityDelta` already established for `adjustment`.

### 7. Logical vs. physical identity invariant

Every `InventoryUnit` has logical identity unconditionally, from the moment it's received — this is already true today (the `Lot → InventoryEntry → InventoryUnit` traceability chain, `domain-model.md`'s existing "Key point"), not a new mechanism this RFC introduces. Verified *physical* identity — the system's ability to say with certainty "this exact physical garment was in the merchant's hand at this moment" — exists only during an `NFCTag`'s attachment window: from `assignedAt` (§5, above) through whenever that tag is later detached or reassigned, if ever.

This is stated here as a standing modeling invariant, not merely a fact this RFC's own mechanics happen to rely on. Any movement or allocation that occurred *before* a unit was tagged must never be presented, to the merchant or to any downstream reasoning, as physically verified — a unit's logical existence in inventory is never sufficient grounds to claim the system knows where it physically is. This is precisely what makes §8's split release behavior, below, correct rather than arbitrary: a FIFO-assigned (untagged) unit never carried a physical claim in the first place, so releasing it asserts nothing false; a scanned (tagged) unit did carry one, so releasing it without the merchant's own confirmation would assert something the system cannot actually verify has become true again.

### 8. Event close: no auto-release for either mode — explicit reconciliation required, evidence differs by mode (revised 2026-09-09; supersedes this RFC's own originally-proposed split-release design)

**This section replaces the original draft in full, per explicit Product Owner direction — it is not an amendment layered on top of it.** The original draft auto-released every `fifo_assignment`-sourced unit silently at Event close, reasoning from §7's logical/physical-identity invariant that a FIFO-assigned unit carries no verified physical claim, so releasing it asserts nothing false. That reasoning is still true and still stated in §7 — but it was never itself a reason the release has to happen *automatically and silently*; that step was this RFC's own design choice, not an entailment of §7, and it's the one now overturned. The Product Owner's ruling: `events.md` §3.16's "no silent auto-return to general inventory on Event close" is one unconditional principle Nahui should apply without a manual/NFC exception — Nahui must never assume unsold merchandise physically returned to general inventory just because the Event's calendar window ended, regardless of commitment mode.

**The corrected rule: `Event.status`'s automatic `active → closed` transition (unchanged, date-driven) writes nothing to `EventAllocation` or `InventoryUnit`, for either mode.** Any `EventAllocation` with 1+ unit still `reserved` in `allocatedUnitIds` — regardless of whether that unit's originating movement carries `unitSource = scan` or `fifo_assignment` — stays `status = open` straight through the close transition, exactly the behavior RFC 0009/D57 already established for the aggregate before this RFC's earlier draft carved out an exception for the manual case. `events.md`'s persistent reconciliation section (§3.16) keeps rendering for **both** modes until each is explicitly resolved — never a dismiss-and-forget banner, never a close-triggered write of any kind, for either mode.

**What differs by mode is the reconciliation *evidence*, not whether reconciliation is *required*.** Both modes require an explicit merchant-initiated action; the shape of that action differs because what the system can already verify differs (§7):

**NFC-tagged entries (`unitSource = scan`) — unchanged, exactly as `events.md` §3.16/§3.25 already specify today.** "Regresar a inventario general" (flips the known `allocatedUnitIds` units back to `available`, writes `type = return_to_general`) or "Mover a otro evento" (§3.24, writes `type = reallocate_out` via the reallocation transaction). No re-scan, no stepper — the specific units were already unit-identified at allocation time, and the exclusivity invariant guarantees nothing since could have touched them, so the merchant's tap is a bookkeeping confirmation of an already-fully-known fact. Not redesigned by this rework.

**Manual/untagged entries (`unitSource = fifo_assignment`) — new, genuinely lightweight, quantity-based reconciliation action.** Unlike NFC, the system never verified *which specific physical units* are still out — only that some quantity is. Requiring per-unit resolution the way NFC's two-button pattern does would be real friction for no accuracy gain, since there is no specific-unit identity to confirm in the first place (§7). The mechanism:

- **"Expected quantity" is a live derivation, never a stored/cached number.** Whenever the reconciliation prompt is computed (on any render of the closed-Event detail screen), the system counts `EventAllocation.allocatedUnitIds` entries whose `InventoryUnit.status` is currently `reserved` **and** whose originating `AllocationMovement` (resolved via the same per-unit ledger lookup §6/§11 already establish) carries `unitSource = fifo_assignment`. This is not a new query — it is the identical candidate-selection query `releaseAllocation()` (§11) already performs to find its own candidate pool; "expected" is simply that pool's size, computed fresh every time, the same "no independently-writable, driftable number" discipline §3's retirement of stored `quantityRemaining` already established for the aggregate as a whole.
- **The one-tap happy path: confirming "sí, regresaron los N" calls `releaseAllocation(eventAllocationId, quantity=N)`, where `N` is that live-expected count** — the default/ceiling argument *is* the full expected amount, with no number for her to type or verify. This is `releaseAllocation()` doing exactly what it already does (most-recently-committed-first, conditional `reserved→available` flip, one transaction, tolerant of a shrunk-mid-flight candidate pool) — no new write mechanism, only a new caller passing the system's own already-known ceiling as the default quantity. This is what makes the happy path minimal-interaction at the write-mechanism level: one tap supplies an argument the system already computed, rather than asking her to state a number back to it (`global-principles.md`'s "never ask twice").
- **A confirmed quantity lower than expected (fewer actually came back) calls the same operation with `quantity = <confirmed>` instead.** Only that many units release, most-recently-committed-first — the same ordering rule §11 already establishes for the mid-Event stepper decrease, reused unmodified, not re-derived for this call site. The `(expected − confirmed)` remainder stays exactly where it already was: `reserved`, still a member of `allocatedUnitIds`, still fully excluded from every other consumption path (FIFO, NFC scan) by the ordinary `available`-status filter every path already applies — genuinely still committed, not merely "still tracked" as a passive record. She is never offered a quantity above `N`: `releaseAllocation()`'s own candidate pool *is* that ceiling, the same partial-fulfillment tolerance §2/§11 already build in, not a new validation rule.
- **Ledger legibility — the confirmed/expected split is recorded explicitly, not merely reconstructable from live state.** The reconciliation call writes `type = return_to_general` (an existing, unchanged closed-set value, reused — not added) rather than `type = adjustment`, which is what distinguishes a reconciliation-triggered release from an ordinary mid-Event stepper decrease at the ledger-row level (see §11's revision, below). `AllocationMovement` gains `quantityExpected` (§6, above) — a row reading `type=return_to_general, unitSource=fifo_assignment, quantityExpected=3, quantityDelta=-2` is legible on its own, with no join required, as "she was asked to confirm 3, confirmed 2 came back."
- **`EventAllocation.status` transition rule is unchanged, and now correctly symmetric across both modes.** Per the existing `ubiquitous-language.md` `EventAllocation` entry (RFC 0009), `status` flips to `reconciled` only as a consequence of an explicit reconciliation-typed write (`return_to_general` or `reallocate_out`) that leaves zero `reserved` units of *either* `unitSource` remaining in `allocatedUnitIds` — never as a side effect of an ordinary `adjustment`, and never automatically at Event close. A manual-mode reconciliation call that fully resolves its own `fifo_assignment` remainder, with no `scan`-sourced units outstanding either, flips `status` to `reconciled` by this same existing rule; a shortfall, or any remaining NFC-tagged stock, leaves it `open`. No new status value and no per-mode status distinction are introduced — "how much is still outstanding, and for which mode" is answered entirely by the live derivation above, not by status granularity.
- **Idempotency.** Same discipline `architecture-principles.md` #7 already requires of every client-retriable write in this document family (`events.md` §3.23's own convention) — this write carries a stable idempotency key, generated once per attempt and reused on retry. Noted here as the write-mechanism-level requirement; the actual saving/error/ambient-confirmation UX shape follows the established §3.9/§3.23 pattern and is `ux-designer`'s to design (see Open Items), not reinvented here.

**Why this doesn't reintroduce the friction the original design tried to avoid.** The one-tap default — release everything currently expected, nothing to type — is the actual common-case path, per the Product Owner's own framing ("if 3 units are expected back, confirming that all 3 returned should require minimal interaction"). What this design adds over the now-rejected silent-auto-release draft is exactly one explicit tap in that common case, plus a genuine (not merely theoretical) path for the case where reality diverges from expectation — the same shape NFC reconciliation has always had, at the coarser grain (quantity, not per-unit identity) manual mode can actually verify.

### 9. Row-level reservation over counter-based CAS — the standing lesson

Flipping actual shared `InventoryUnit` rows is strictly more correct under concurrent multi-path consumption than a counter guard, because a counter only protects paths that explicitly check it — every other path that reads the same underlying resource by a different route stays completely unaware the counter exists. This is not a hypothetical tradeoff argued in the abstract: it is the exact, concrete mechanism the confirmed prototype defect proved. `EventAllocation.quantityRemaining`'s compare-and-swap was itself internally correct — the write genuinely couldn't go below zero — but it protected only the one call site written to check it; Quick Sale's FIFO logic, reading `InventoryUnit.status` directly, had no path through which that protection could ever reach it. A row-level flip has no such blind spot, because every consumer of that resource already reads the same field the flip writes — there is no second, parallel channel for the guarantee to fail to reach. This RFC's correction (§2, generalizing mechanism (a) to manual mode) is the applied form of this lesson; this section states it as the general principle, not scoped to `EventAllocation` alone, for future aggregate-boundary or concurrency-guard decisions to weigh against a counter-based shortcut before choosing one.

### 10. Slice A / Slice B implementation split

**Slice A** — the pure correctness fix: the schema changes above (§2–§7), the FIFO/reservation rewrite (`commitAllocation()`), and the symmetric decrease/reconciliation operation (§11, below, now serving two call sites per §8's 2026-09-09 revision). **Correction (Product Owner direction, 2026-09-09): no longer zero new merchant-facing UX.** The original draft's Event-close design (silent auto-release) genuinely required no new screen; the corrected design (§8) does — a new, deliberately lightweight, quantity-based reconciliation action for manual-mode rows on the closed-Event detail screen, distinct from NFC's existing two-button pattern. The schema/write-mechanism pieces (`quantityExpected`, the `return_to_general`-typed `releaseAllocation()` call site, the corrected no-auto-release invariant) remain implementable independent of any specific screen design and stay "Slice A" in the sense of closing the confirmed correctness defect without inventing a new capability's full scope — but this one piece cannot ship to Ana without `ux-designer` designing the actual interaction first (see Open Items). Narrower than a Slice B-scale feature (no planned/committed two-state visual distinction, no new navigation) — but no longer screen-free either.

**Slice B** — the fuller planned/committed two-state UX: surfacing the `quantityPlanned` vs. hard-committed distinction (§2, above) to the merchant visually, so an Owner can actually see and act on the difference between "I'm planning to bring 20" and "12 are actually locked." This needs `ux-designer` first — out of scope for this RFC's own implementation, named here as the next step, not designed here.

### 11. The symmetric decrease operation — `releaseAllocation()`

§2 specified `commitAllocation()` for the increase path but left the decrease path unaddressed. This is a real gap, not a deferred nicety: `events.md` §3.21's already-Approved stepper is bidirectional (floor 0, ceiling "Disponible en general"), and its own copy states plainly, "Lo que no asignes se queda disponible para tus otros eventos." Under §3's correction, `quantityRemaining` is a pure derivation from real `reserved` units — so a "Guardar cambios" commit that lowers the staged manual quantity has no defined write to actually free the corresponding units. Left unaddressed, decreasing an allocation would leave those units silently stuck `reserved`, reproducing — via the one write path this RFC didn't yet touch — the exact class of phantom-commitment defect §1 exists to close.

**`releaseAllocation(eventAllocationId, quantity)`.** Candidate units are drawn only from `EventAllocation.allocatedUnitIds` entries whose `InventoryUnit.status` is currently `reserved` **and** whose originating `AllocationMovement` (resolved via the same per-unit ledger lookup §6 established) carries `unitSource = fifo_assignment`. Scan-sourced units are categorically excluded from this operation's candidate pool — not conditionally filtered, never reachable by it at all. This is the same split §8 already applies at Event close, for the same reason stated in §7's logical/physical identity invariant: a `scan`-sourced unit carries a verified physical claim the system cannot re-assert was relinquished without the merchant's own explicit tap; only the existing, unmodified `events.md` §3.25 "Regresar a inventario general" / "Mover a otro evento" mechanism may release one. A manual quantity-decrease via the stepper must never touch an NFC-tagged commitment — the same boundary `commitAllocation()`'s own `untagged` filter draws on the way in, drawn here on the way out.

**Ordering: most-recently-committed-first, not commit's own FIFO-forward order.** `commitAllocation()`'s oldest-Lot-first ordering exists to serve D5's fairness/aging concern over the *shared, cross-Event* pool of `available` stock — it answers "which units should this claim take from everyone else's pool." Release answers a different question: "which of *this EventAllocation's own* already-committed units should re-enter that pool." No aging-fairness concern applies there — a released unit's own Lot-receipt-date travels with it regardless of release order, so D5's ordinary FIFO still governs its next consumption correctly no matter which specific unit is chosen. Applying commit's FIFO-forward rule to release by blind symmetry would arbitrarily release the *oldest*-standing commitment first — the one Ana is least likely to be mentally revising — instead of undoing the most recent staged change, which is what "Guardar cambios" on a lowered stepper value actually represents to her. It is also the cheaper, more directly correct implementation: it requires only this `EventAllocation`'s own `AllocationMovement` ledger, filtered `unitSource = fifo_assignment`, ordered by `createdAt` descending (ties broken by a stable secondary key), popping `unitIds` until `quantity` is satisfied — no join against `Lot`/`InventoryEntry` receipt dates at all, unlike commit's own selection.

**Write mechanics — the identical shape `commitAllocation()` established, run in reverse.** For each selected candidate, in the order above: `UPDATE InventoryUnit SET status='available' WHERE id=? AND status='reserved'` — a conditional write, zero-rows-affected meaning the unit already left `reserved` by some other path (sold, or released concurrently), tolerated the same way commit tolerates insufficient stock. All of this happens inside one local transaction. `EventAllocation.allocatedUnitIds` is **not pruned** — the array remains the cumulative record of every unit ever committed, exactly as `commitAllocation()`'s own append-only behavior already implies and exactly what makes `quantityRemaining`'s derivation need a live-status check ("entries whose referenced unit is *still* reserved") rather than a bare array length. `EventAllocation.quantityAllocated` is likewise unchanged — it is documented as monotonic, a hard-committed lifetime total, not a live count. The transaction writes exactly one `AllocationMovement` row: `type = adjustment`, `unitIds` = the actual released set (may be fewer than the requested `quantity` under the same partial-fulfillment tolerance commit already has), `unitSource = fifo_assignment`, `quantityDelta` = the negative count (retained convenience/audit figure per §6), `counterpartEventAllocationId = null`. `EventAllocation.status` is untouched by this operation — it stays `open`.

**`adjustment` already fits; no new `AllocationMovement.type` value is needed.** `events.md` §3.21's own already-Approved copy states that a "Guardar cambios" commit resolves invisibly to one of exactly three movement types: `initial_allocation` (first-ever nonzero commit), `replenish` (an increase on top of an already-nonzero commitment), or `adjustment`. By elimination, `adjustment` is already the type this Approved UX text reserves for the one remaining case a stepper commit can produce — a decrease. This RFC's `unitSource = fifo_assignment` tag on that row (§6) is the only new information `releaseAllocation()` requires; the type vocabulary itself needs no schema change.

**A second call site, added by §8's 2026-09-09 reconciliation-mechanism revision, reuses this identical operation with `type = return_to_general` instead of `adjustment`.** `releaseAllocation()`'s candidate-selection, ordering, and write mechanics — everything else in this section — are unchanged and identical for both call sites; what differs is only the caller's own `type`/`quantityExpected` arguments and its trigger (a staged "Guardar cambios" commit for the mid-Event decrease; an immediate one-tap confirm for reconciliation). This is a generalization of the operation, not a fourth mechanism and not a second, parallel `releaseAllocation`-shaped function — see §8, above, for the reconciliation call site's own full design.

**Naming.** `releaseAllocation()`, chosen for the mechanism-neutral action ("release"), not `returnAllocation()` — deliberately, since (per §8's 2026-09-09 revision) this one operation now legitimately serves two distinct triggers with two distinct `type` outcomes: an ordinary, in-life stepper decrease (writes `adjustment`) and an Event-closed reconciliation confirm (writes `return_to_general`). A verb specific to either trigger would have been wrong for the other once both existed. This is the same generalization already applied elsewhere in this RFC — `initial_allocation`/`replenish`/`adjustment` are each already written by two structurally different mechanisms depending on mode (scan vs. `commitAllocation()`/`releaseAllocation()`); `return_to_general` now follows the identical pattern (NFC's own direct known-unit flip vs. manual mode's FIFO-based `releaseAllocation()` call), rather than being a new exception to it.

## Key mechanisms (for `domain-model.md`'s Key Mechanisms, once promoted)

> **Physical-location-exclusivity invariant (amended).** A unit of stock can only ever be committed to one open Event at a time. Both enforcing mechanisms are now the same shape: (a) NFC allocation-time exclusivity (unchanged from RFC 0009 §3(a)) — a conditional `available→reserved` write on the scanned unit. (b) Manual/quantity-mode commitment (corrected) — `commitAllocation()` performs the identical class of conditional write, FIFO-ordered across `N` `available AND untagged` units, inside one transaction that also updates `EventAllocation.allocatedUnitIds`/`quantityAllocated` and appends one `AllocationMovement` row (`unitSource = fifo_assignment`); the symmetric decrease, `releaseAllocation()`, reverses only `fifo_assignment`-sourced units, most-recently-committed-first. `EventAllocation.quantityRemaining` is no longer written — it is derived at read time as the count of `allocatedUnitIds` entries whose unit is still `reserved`. Reconciliation reverses either mechanism the same way (`reserved → available`) via an explicit merchant-initiated action, required for both modes and never triggered by Event close for either — unit-identified for `scan`-sourced stock (unchanged, `events.md` §3.16/§3.25), quantity-confirmed via this same `releaseAllocation()` operation for `fifo_assignment`-sourced stock (see "Event close: no auto-release, explicit reconciliation for both modes," below). See `decision-log.md` (this RFC's eventual promotion entry).

> **Logical vs. physical identity.** Every `InventoryUnit` carries logical identity unconditionally from receipt. Verified physical identity exists only during an `NFCTag`'s attachment window (`assignedAt` through detachment/reassignment, if any). No movement or allocation predating tagging may be presented as physically verified. Standing invariant, not scoped to this RFC's own mechanics. See `decision-log.md` (this RFC's eventual promotion entry).

> **Event close: no auto-release, explicit reconciliation for both modes (revised 2026-09-09; supersedes this RFC's own originally-proposed split-release design).** `Event.status`'s automatic `active → closed` transition writes nothing to `EventAllocation` or `InventoryUnit`, for either mode — any `EventAllocation` with 1+ unit still `reserved` in `allocatedUnitIds`, of either `unitSource`, stays `status = open` straight through close, exactly as RFC 0009/D57 originally established. `events.md`'s persistent reconciliation section keeps rendering for both modes until explicitly resolved. `scan`-sourced units resolve exactly as `events.md` §3.16/§3.25 already specify (unchanged, unit-identified, two-button). `fifo_assignment`-sourced units resolve via a live-derived expected quantity (the count of still-`reserved` `fifo_assignment`-sourced units, computed fresh on every render) and a one-tap confirmation calling `releaseAllocation(eventAllocationId, quantity)` with that expected count as the default; a lower confirmed quantity releases only that many (most-recently-committed-first, same ordering as the mid-Event stepper decrease), leaving the remainder genuinely `reserved` and excluded from every consumption path. This call writes `type = return_to_general` (reused, not new) plus a new `AllocationMovement.quantityExpected` field, distinguishing it on the ledger from an ordinary mid-Event `adjustment`. `EventAllocation.status` flips to `reconciled` by the same unchanged rule as before — only as a consequence of a reconciliation-typed write leaving zero `reserved` units of either `unitSource` remaining, never automatically. See `decision-log.md` (this RFC's eventual promotion entry).

## Entity relationships diagram addition (diff against RFC 0009's existing block)

```
EventAllocation (root; businessId, eventId, productId,
     quantityPlanned [NEW — soft intent, no pool effect],
     quantityAllocated [unchanged — monotonic, hard-committed total],
     quantityRemaining [RETIRED as stored field — now derived: count of
       allocatedUnitIds entries whose unit is still status=reserved],
     allocatedUnitIds [now populated by BOTH modes — NFC scan and
       commitAllocation()'s FIFO flip, one shared row-level committed set],
     status)
     gains operations: commitAllocation(quantity) — FIFO-flips N
     available-AND-untagged InventoryUnit rows to reserved, same
     transaction appends allocatedUnitIds + AllocationMovement row;
     releaseAllocation(quantity) — reverses only fifo_assignment-sourced
     units, most-recently-committed-first, same transaction shape; called
     both by the mid-Event stepper decrease (writes type=adjustment) and,
     per the 2026-09-09 rework, by manual-mode Event-close reconciliation
     (writes type=return_to_general + quantityExpected) — one operation,
     two callers, distinguished only by the AllocationMovement row written
       └─ AllocationMovement (internal-only; type,
            unitIds [NEW — now unconditional, every row],
            unitSource: scan | fifo_assignment [NEW],
            quantityDelta [retained, convenience/audit only],
            quantityExpected [NEW, 2026-09-09 rework — nullable, populated
              only when type=return_to_general AND unitSource=
              fifo_assignment, i.e. manual-mode reconciliation writes
              specifically; null everywhere else including NFC's own
              return_to_general rows],
            counterpartEventAllocationId on reallocate pairs — unchanged)

InventoryUnit
  └─ optional 1:1 NFCTag (assignedAt [NEW] — attachment-window start)
```

## Sequencing

Same posture as RFC 0009 itself: target schema/invariant correction, not a redesign of already-Approved UX. Slice A (§10, above) is implementable independent of any new screen and should be treated as a priority correctness fix given it closes a confirmed, reproduced defect, not a speculative hardening pass. Slice B is explicitly deferred pending `ux-designer`. Once Accepted, `architect`'s next step per the Migration Workflow (D43) is an Architecture Gap Analysis against Slice A's schema above, checking implementation-readiness — not against any new UX, since none is introduced by Slice A. Sequenced independently of RFC 0009's own remaining open items (below) — none of them depend on this correction landing first, and this correction doesn't depend on any of them.

## Open items, named for the Product Owner / Architect, not resolved here

1. **`unitSource` field addition (§6/§8)** is this RFC's own addition, not present in any prior settled design as written — flagged explicitly for Product Owner/Architect sign-off before being treated as settled, the same posture RFC 0009 used for its own `counterpartEventAllocationId` addition (RFC 0009 Open Item 2).
2. **`quantityExpected` field addition (§6/§8/§11, 2026-09-09 rework)** — same posture as item 1: this RFC's own addition, not present in any prior settled design, flagged for Product Owner/Architect sign-off before being treated as settled.
3. **Resolved, 2026-09-09 — `events.md` §3.16/§3.24 amended.** Manual/untagged rows no longer resolve automatically (§8's rework retracts that entirely) — `events.md` §3.16 now designs the new, deliberately lightweight, quantity-based reconciliation interaction this item called for: the one-tap "sí, regresaron las N" confirm, a secondary "Ajustar cantidad" affordance for a lower confirmed number, an N=1 direct-tap simplification, and a "Ya revisaste esto — todavía falta N" framing on any later visit to a shortfall row. NFC-tagged rows keep rendering and resolving exactly as before, unchanged. The trigger-condition prose is corrected to this RFC's stated invariant, also fixing the pre-existing "available"-vs-"reserved" wording inconsistency this item originally flagged. `ux-critic` (two rounds) and `reviewer` both ran clean against the applied amendment.
4. **Resolved, 2026-09-09 (`ux-designer`) — "Mover a otro evento" stays offered for manual rows too, not NFC-only.** Applied in `events.md` §3.16/§3.24: reuses the closed-source variant unchanged, moving the full current live-expected quantity. On a mixed row (both pools outstanding on one Product), each button carries a mode qualifier ("de las con tag" / "de las sin tag") to prevent a mis-tap between two controls that move genuinely different stock — a real gap `ux-critic` found and closed in the applied design, not present in this RFC's own original framing of the question.
5. **Unaffected, confirmed still open:** RFC 0009's existing Open Item on NFC Readiness's "sellable tagged inventory" count understating reality while units sit `reserved` (RFC 0009 Open Item 3, `product/02-ux/architect-questions.md` Q20 per D57). This correction doesn't touch it — `commitAllocation()` deliberately excludes tagged units from its own pool (§2, above), so it introduces no new units into that undercount.
6. **Resolved (Product Owner direction, 2026-09-09) — previously item 4.** "Confirm that dropping the persistent 'must never happen silently' visibility for manual/untagged rows specifically is the wanted behavior" is now answered: it is explicitly **not** wanted. §8 (above) is rewritten in full to keep that visibility, and the required explicit action, for both modes uniformly — differing only in evidentiary shape (quantity-confirmed for manual, unit-identified for NFC), never in whether an explicit merchant action is required at all. Retained here, marked resolved rather than deleted, per this document's own non-deletion discipline for superseded findings.
7. **New, flagged by `reviewer` (2026-09-09) — `home.md` §3.8a-d ("lost the race" concurrent-selling conflict) and §3.9 (Event-scoped remaining-stock tile line) both ground their design explicitly in the pre-RFC-0010 mechanism this RFC replaces.** §3.8a's own text describes manual-mode conflicts as "fungible units drawn from one shared counter," and §3.9's remaining-stock line is defined as "this device's last synced read of `quantityRemaining`, not a live lock" — both accurate against today's Accepted Foundation (RFC 0009/D57), and neither wrong *yet*, but both describe exactly the counter-based asymmetry this RFC exists to close, once `quantityRemaining` is retired as a stored field (§3) in favor of real row-level `InventoryUnit` reservations. Not resolved here — this RFC's own "What it touches" section never named `home.md`, and should have. Flagged so `home.md` §3.8a-d/§3.9 are swept for consistency at the point this RFC is Accepted and its Architecture Gap Analysis runs (§10, Sequencing), rather than silently missed the way an earlier governance gap (D27/D31) was the first time, before this project's Governance Rollout Cascade existed to catch it.
