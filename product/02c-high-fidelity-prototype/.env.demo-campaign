# demo-mode.md, Architecture Review §8 item 1 — validation-campaign build
# flag. Only present/loaded when building or running with `--mode
# demo-campaign` (see package.json's `build:demo-campaign` script). A real
# production build (the default `production` mode, e.g. `npm run build`)
# never loads this file, so `import.meta.env.VITE_DEMO_MODE` is undefined
# there — DemoModeGate.tsx's compile-time branch relies on exactly that.
VITE_DEMO_MODE=true
