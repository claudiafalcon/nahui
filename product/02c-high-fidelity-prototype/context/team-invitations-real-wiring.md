# Team Invitations — real backend wiring (RFC 0013 implementation gap)

**Status: Backend + client-store half complete (`builder`, 2026-09-14), UI half complete (`ui-designer`, 2026-09-14), `ux-critic`/`reviewer` fix round 2 complete (`ui-designer`, 2026-09-14), `architect` Blocker fix on `accept_invitation`'s membership-conflict handling complete (`builder`, 2026-09-14) — `tsc -b`/`npm run build` genuinely clean, zero errors anywhere in the tree.** Working file per this folder's own knowledge-architecture discipline. `createInvitation`/`acceptInvitation` in the built React prototype were 100% local-mock — RFC 0013 (Accepted, `decision-log.md` D64) specifies the real token-based mechanism in full; it's now wired to the database (backend RPCs + `store.tsx`/`types.ts`/`selectors.ts` domain layer + minimal `/invite/<token>` routing plumbing), and `TeamScreen.tsx`/`InvitationFlow.tsx` are both rebuilt in full against the reworked `settings.md` §2.7/§3.11-§3.14 and the new `authentication.md` §2.0/§2.2/§2.2a/§3.9a-§3.9b/§3.10-§3.13a. **Still pending a fresh `ux-critic`/`reviewer` re-verification pass on fix round 2** before commit (Main's own instruction — not committed by this dispatch).

## What `ui-designer` built (2026-09-14) — the UI half

**`src/screens/Authentication/InvitationFlow.tsx` — full rewrite, not a patch.** Previously the post-auth-only acceptance screen (phone-keyed, reached via an implicit session-resume/phone-match `AppRouter.tsx` no longer performs). Now the entire §2.0/§2.2a gate: resolves `peekInvitation(token)` before any authentication (§3.9a/§3.9b), shows the offer (§3.10) pre-auth, and — for a session-less device accepting — mounts its own inline `AuthenticationFlow` instance (carrying the token as UI-only context, not global state) rather than handing off to `onboarding.md`'s ordinary Business-creation flow. On a fresh `currentUserId`, proceeds straight to `acceptInvitation`'s four-outcome resolution (`welcome`/§3.10c, `already-member`/§3.10d, `revoked`/§3.10e, `not-available`/§3.13a reused verbatim from the pre-auth checkpoint, `accept-error`/§3.10b). Calls `retryHydration()` after a `success`/`already_member` outcome so `state.business`/`memberships` are populated before "Ir a Hoy" hands off to `App`.

**Structural §3.7e bypass, not a flag.** §2.2's new case 0 ("checked FIRST... skip cases 1-3 entirely") means a token-carrying authentication must never show §3.7e (`PhoneMismatchConfirm`), even on a device with real prior-identity history. `InvitationFlow.tsx` achieves this by construction: while it's mounted, it fully preempts `AppRouter.tsx`'s own ordinary branches (including the one that renders `PhoneMismatchConfirm`) — there's no `needsPhoneMismatchConfirmation` check running at all during this window, so nothing needs to suppress it. If the outcome is `invitation_not_available`, `onDone()` hands control back to `AppRouter.tsx`'s ordinary branches, which correctly *do* re-consult that same flag — matching §2.2a's own "falls through to §2.2's ordinary cases 1-3" instruction, for free.

**Why almost every branch just calls `onDone`.** §3.10/§3.10d/§3.10e/§3.13a's own "Behavior" text each resolve to either `onboarding.md §2.1`'s ordinary session resolution or `authentication.md`'s own ordinary §2.2 cases 1-3 — both already correctly implemented by `AppRouter.tsx`'s existing ordinary branches, unconditionally, off whatever `AppState` looks like when control returns to them. So unmounting `InvitationFlow` and letting those branches run *is* the correct destination in every one of those cases — not a shortcut around real branching, direct reuse of already-correct code.

**`AppRouter.tsx`** gained `invitationGateActive` (derived once from the existing `inviteToken` path-detection state) as a new, highest-precedence render branch — `InvitationFlow` is the sole renderer while it's true, checked before even `!authenticated`, matching §2.0's "checked before every other check in §2" sequencing. `onDone` clears the URL back to `/` via `window.history.replaceState` — without this, a reload after resolving the Invitation would re-derive the same `inviteToken` from `window.location.pathname` and re-open the offer forever, since the path itself never changes merely by resolving what it named. This closes the file's own previously-open question about whether persistence "actually still works once wired" — it does, but only because `onDone` now actively rewrites the path, not because persistence alone would have sufficed.

**Invitation-context copy line (`authentication.md` §2.0 step 4, closes that document's own `ux-critic` M1) threaded through the authentication sub-flow.** New shared `InvitationContextLine.tsx` + an `invitationContext?: { businessName }` prop threaded through `AuthenticationFlow.tsx` → `ChooseMethodStep`/`PhoneStep`/`EmailStep`/`GoogleError`/`CodeStep`. Deliberately **not** threaded into any near-instant/slow silent-skeleton transition (`GoogleProgress`, the "Enviando…"/"Confirmando…" states) — disclosed simplification, reasoned in `InvitationContextLine.tsx`'s own doc comment: those screens show no text at all (or one generic status word) by the same "technology should disappear" convention every other tab's own §3.1/§3.2 already holds itself to, and stacking a business-name line onto a state designed to carry zero content would contradict that convention. Also **not** threaded into `PhoneMismatchConfirm.tsx` (§3.7e) — disclosed as structurally inapplicable, not a gap: §2.2 case 0 provably never reaches §3.7e on this path (previous paragraph), so §2.0 step 4's own "(and §3.3-§3.7e, wherever this path reaches them)" citation is partially dead text for this exact reason.

**`src/screens/Settings/TeamScreen.tsx` — full rewrite against `Invitation`'s new token shape.** No more phone collection at "Nueva invitación" (§3.12) — an always-enabled "Generar invitación" button, an optional `targetHint` email field with the spec's own loose-format/duplicate-pending inline advisories (both non-blocking). Real one-time link display/copy/share flow (§3.12c): `[ Listo ]` gated on having tapped Copiar/Compartir at least once (`navigator.clipboard`/`navigator.share`, with a disclosed clipboard-fallback when `navigator.share` is unavailable — e.g. this desktop dev preview). New `cancelInvitation` local-mock store function (§3.12d, same disclosed not-yet-real-backend-wired shape `revokeMembership` already has — the real `cancel_invitation` RPC stays the named, out-of-scope gap noted below). `regenerateInvitation`'s existing "any pending row, not just expired" backend loosening is deliberately **not** exposed in the UI — `[ Generar otra ]` stays gated to expired rows only, matching `settings.md` §3.11's own Approved gating; the loosening is used internally as the recovery path when `createInvitation`/`regenerateInvitation` return `token: null` (an idempotent replay of an already-completed request whose original response was lost) — handled transparently via the existing "Generando…" write states, not a new, un-Approved screen state.

**`selectors.ts`'s `teamRows`/`TeamRow` widened from three row states to five** (`active`/`revoked` Membership, `pending`/`expired`/`cancelled` Invitation — `settings.md` §3.11's own text), in the document's own stated order. Split into five single-literal-discriminant union members rather than two multi-literal ones (`{kind: 'active'|'revoked'; ...}` etc.) after hitting a real TypeScript narrowing limitation: sequential `if`-return control-flow narrowing doesn't reliably eliminate a union member whose discriminant property is itself a multi-literal union — confirmed by reproducing and fixing the exact compile error, not assumed.

**Full `tsc -b`/`npm run build` verified clean** — zero errors anywhere in the tree, not just "confined to" the two files this dispatch targeted. Live-smoke-tested via a real `npm run dev` instance: root path and `/invite/<token>` both serve `index.html` (vite's own dev-server SPA fallback) with no startup errors in the dev-server log. No `puppeteer`/Playwright walkthrough of the actual Invitation flow's screens was performed this pass (disclosed, not silently skipped) — the review pipeline below (`ux-critic`/`merchant-user-tester`) is where that verification belongs.

## `ux-critic`/`reviewer` fix round 2 (2026-09-14, closed)

Both reviews of the `InvitationFlow.tsx`/`AppRouter.tsx`/`TeamScreen.tsx` rebuild above found real, concrete issues. All fixed in this pass; `npm run build` confirmed genuinely clean throughout (`reviewer`'s own B2 `teamRows` TS-narrowing concern was a false positive against the real compiler — no action needed).

**Blocker (`reviewer` B1) — `InvitationFlow.tsx`'s `handleAccept` skipped the required §3.7e exclusion, letting an unconfirmed identity mint a real `BusinessMembership`.** `authentication.md` §2.0 step 4's "does this device currently hold a valid, verified session?" explicitly means "§2.1 step 1's existing test" — a valid session *with no §3.7e confirmation left pending*. `handleAccept` only checked `state.currentUserId != null`, so a device sitting on an unconfirmed §3.7e screen (backgrounded mid-verification, then opening an `/invite/<token>` link — `invitationGateActive` fully preempts the `AppRouter.tsx` branch that would otherwise show §3.7e) could tap "Aceptar y empezar a vender" and run the accept write immediately.

**Fix**: extracted the shared test into a new `src/domain/authResolution.ts` — `phoneMismatchConfirmationTarget(state)` (the identical `user.phoneMismatchConfirmationPending` + zero-Membership/zero-Business guard `AppRouter.tsx`'s own `needsPhoneMismatchConfirmation` already computed, now computed once) and `mismatchDisplayValue(identity, googleDisplayLabel)` (the identical per-channel display-copy resolution). `AppRouter.tsx` now consumes both instead of its own inline copies — one shared rule, not two independently-typed ones that can drift again. `InvitationFlow.tsx` gained a new `'mismatch-confirm'` step: `handleAccept` checks `phoneMismatchConfirmationTarget` first; when it fires, renders `PhoneMismatchConfirm.tsx` inline (never handing off to `AppRouter.tsx`'s own §3.7e branch — structurally unreachable while `invitationGateActive` is true, and would lose the Invitation context anyway). `PhoneMismatchConfirm.tsx` gained a new optional `invitationContext` prop (threading `InvitationContextLine`, the same M1 pattern every other §3.2a-§3.2f/§3.3-§3.7d screen already uses) so the Invitation-acceptance context stays visible through this gate too. Only "Sí, es mío/mía" (`onConfirm`) proceeds to the accept write; "No, elegir otro" (`onCorrect`) reverts the verification and returns to this flow's own `'authenticating'` step with the just-typed value preserved — never AppRouter's ordinary flow.

**Major (`ux-critic` MAJ1) — a Google OAuth redirect return, while accepting an Invitation on a session-less device, wasn't detected by `InvitationFlow`'s own mount logic.** `AuthenticationFlow.tsx`'s `computeInitialStep()` detects a resumed Google redirect via the URL hash on mount — but `InvitationFlow` has the *highest* render precedence in `AppRouter.tsx`, so on return from Google with `/invite/<token>` still in the URL, `InvitationFlow` remounts fresh, defaults to `'resolving'`, and never checked the hash itself — she'd land back on the re-fetched offer screen with her completed Google credential sitting unconsumed until tapping "Aceptar" a second time.

**Fix**: added `hasGoogleRedirectSignal()` to `InvitationFlow.tsx` (the identical hash/search check `AuthenticationFlow.computeInitialStep` already performs), captured once at mount as `resumedGoogleRedirect`. `resolveToken()` now checks it after confirming the Invitation is still `pending`: if a Google-redirect signal is present and no session exists yet, it routes straight into the `'authenticating'` step instead of `'offer'` — mounting the inline `AuthenticationFlow`, whose own `computeInitialStep` independently detects the identical hash and resumes straight into `'google-verifying'`, consuming the credential without a second tap. Traced end to end: mount → `resolveToken` (fast, still needs the canonical `businessName`) → `'authenticating'` → `AuthenticationFlow` resumes `google-verifying` → `resolveGoogleSignIn` consumes the hash → `currentUserId` set → `InvitationFlow`'s existing `step.kind === 'authenticating' && currentUserId != null` effect fires `runAccept` → done. One flow, no dangling ambiguity, no extra tap.

**Major (`ux-critic` MAJ2) — accessibility regression: all five of `InvitationFlow.tsx`'s screens used "Nahui" as their only `<h1>`.** Every sibling screen (`ChooseMethodStep`/`PhoneStep`/`EmailStep`/`PhoneMismatchConfirm`) correctly uses a small `.eyebrow` `<p>` for "Nahui" and a separate `<h1 className={styles.heading}>` for the real screen title; `InvitationFlow.tsx` wrapped "Nahui" itself in `<h1 className={styles.eyebrow}>` on all five screens, so a screen-reader user navigating by heading heard "Nahui" five times with no way to distinguish them.

**Fix**: `InvitationFlow.module.css`'s `.eyebrow` rewritten to the small-caption treatment (`PhoneStep.module.css`/`PhoneMismatchConfirm.module.css`'s own already-established style — `text-label`, uppercase, letter-spaced), replacing its previous oversized wordmark styling; added a matching `.heading` class (`text-display-md`). `InvitationFlow.tsx`'s five screens (`not-available`, `offer`, `welcome`, `already-member`, `revoked`) now each render `<p className={styles.eyebrow}>Nahui</p>` plus a real `<h1 className={styles.heading}>` with a title summarizing that screen's own body copy: "Esta invitación ya no está disponible," "Te invitaron a vender," "Ya quedaste registrada," "Ya formas parte del equipo," "Ya no tienes acceso."

**Minor (`ux-critic` MIN1) — `TeamScreen.tsx`'s duplicate-pending-invitation check could call an already-expired Invitation "pendiente."** `hasDuplicatePending` filtered on raw `inv.status === 'pending'` instead of the read-time `invitationDisplayStatus(inv) === 'pending'` derivation `teamRows` already uses one file away. **Fix**: swapped to `invitationDisplayStatus(inv) === 'pending'`.

**Additional finding (`knowledge-mentor` consultation on the one-time-secret-display pattern, closing this file's own previously-open item, below) — `TeamScreen.tsx`'s `handleCopy` silently swallowed a clipboard-write failure, giving her no signal either way.** A rejected `navigator.clipboard.writeText` promise was caught and ignored, then `markAcknowledged()` ran regardless — she'd tap "Copiar enlace," see nothing, and have no way to know whether it worked, on a secret that's gone forever after this screen closes. `knowledge-mentor` confirmed this differs meaningfully from the Share case (a dismissed native share sheet is a *visible* non-completion she chose; a rejected clipboard promise is *invisible* to her entirely) and that common practice (e.g. GitHub's own copy button) surfaces an explicit success/failure signal.

**Fix**: added `copyFeedback: 'copied' | 'error' | null` local state, ambient and self-dismissing after 2.4s (the identical `toast` mechanism `EventsList.tsx`'s own "Evento cancelado ✓" already establishes, reused as a local copy per this folder's own "each screen owns its CSS" convention — new `.copyConfirmation`/`.copyError` classes in `TeamScreen.module.css`, `--color-success`/`--color-error` tokens). A successful copy shows "Copiado ✓" and still unlocks "Listo" (`markAcknowledged()`); a genuine rejection shows an honest "No pudimos copiar el enlace. Intenta 'Compartir...' o selecciona el texto." and deliberately does **not** call `markAcknowledged()` — "Listo" no longer unlocks on a failed copy as if it had succeeded.

## What `builder` actually built (2026-09-14)

- **Migration `supabase/migrations/20260914030000_invitation_token_write_path.sql`**, deployed via `supabase db push` — `_generate_invitation_token()` (private helper), `create_invitation()`, `regenerate_invitation()`, `peek_invitation()`, exactly as `architect`'s design below specifies. `accept_invitation()` untouched. Verified live against the hosted project: `peek_invitation` reachable by the anon key and returns an empty result for an unknown token; `create_invitation` correctly rejects an anon caller with `permission denied` (no grant).
- **`types.ts`**: `Invitation.phone` removed; `expiresAt`/`targetHint`/`acceptedByUserId` added; `status` narrowed to `pending | accepted | revoked` (`expired` is now purely a read-time derivation). No raw-token field — never stored client-side.
- **`selectors.ts`**: `pendingInvitationsForPhone` removed (structurally retired by RFC 0013 — nothing to match a phone against anymore, and the mechanism itself is gone per RFC 0013 §2: "the new mechanism never auto-surfaces anything"). Added `invitationDisplayStatus(invitation)`, the `expired` derivation every future caller should read through instead of `.status` directly.
- **`store.tsx`**: `createInvitation`/`acceptInvitation` rewritten as real async RPC calls (signatures per `architect`'s design, below). Added `regenerateInvitation`/`peekInvitation`. `declineInvitation`/`revokeMembership` left untouched (out of scope — see Open items).
- **`AppRouter.tsx`**: added minimal path-based routing — a `window.location.pathname` match against `/invite/(.+)`, captured once at mount into a small piece of local state (`inviteToken`), not yet consumed by any screen. Removed the phone-match auto-offer mechanism that used to mount `InvitationFlow` automatically (dead code once `Invitation.phone` no longer exists, and already retired on the merits by RFC 0013 itself, not a UI redesign choice made here).
- `supabase/README.md` and this file updated.

## `reviewer` fix round 1 (2026-09-14, Important — closed)

`create_invitation` re-checks `businesses.subscription_tier = 'paid'` server-side before minting an Invitation; its sibling `regenerate_invitation` never performed that check — only OWNER-ship was re-verified. A Business that downgraded to Free while holding an old, expired pending Invitation could still mint a fresh, working invite link via "Generar otra" even though that UI surface no longer exists for her on Free tier.

**Fix**: `supabase/migrations/20260914031000_invitation_token_write_path_fix.sql`, deployed via `supabase db push` — added the identical tier check to `regenerate_invitation`, keyed off `v_invitation.business_id` (the row already looked up by `invitation_id`), placed right after the existing `is_active_owner_of` check and before the idempotency-key insert, matching `create_invitation`'s own ordering. Raises the identical `paid_tier_required` exception (42501).

**`store.tsx` alignment checked, no change needed**: `createInvitation` itself has no `paid_tier_required`-specific handling — it logs and returns `null` on any RPC error, generically. `regenerateInvitation` already did the same before this fix and still does after — both siblings handle every error path identically, so there was no asymmetry to correct.

`npm run build` confirmed clean except the pre-existing, expected `TeamScreen.tsx`/`InvitationFlow.tsx` errors (those two screens' own rebuild is still a separate, not-yet-started `ui-designer` dispatch, per Sequencing step 3 below).

## `architect` Blocker fix round (2026-09-14, closed)

`architect` reviewed `20260914030000`'s own design note — "the raw token is never persisted anywhere except this row's own `idempotency_keys.result`... a deliberate, narrow exception to 'never store the raw token,' not an oversight" — and found the framing wrong against RFC 0013/D64's actual threat model. D64 is about a full database dump/leak, not an application-layer RLS bypass: `idempotency_keys` holding zero RLS policies only ever meant no *client* could read the cached token back through PostgREST. A DB dump doesn't go through RLS at all, so the cache never was safe against the threat D64 actually names.

**Fix**: `supabase/migrations/20260914032000_invitation_token_no_raw_cache.sql`, deployed via `supabase db push` — both `create_invitation` and `regenerate_invitation` `create or replace`d (written from the tier-check-carrying `20260914031000` version, not the original, so that fix isn't regressed):

- `idempotency_keys.result` no longer carries `'token'` in either function's success path — `create_invitation` stores only `invitation_id`/`expires_at`; `regenerate_invitation` stores only `expires_at`. A replay of either (the `v_key_id is null` branch) now returns `null::text` for `token` instead of reading it back from the cache.
- `regenerate_invitation`'s precondition loosened from "`status = 'pending'` **and** `now() < expires_at`" (genuinely expired) to "`status = 'pending'`" alone (any still-pending row, expired or not) — making it the legitimate recovery path for a dropped `create_invitation` response, since neither function persists the raw token anywhere beyond its own single response. The error code `invitation_not_expired` renamed to `invitation_not_pending` everywhere it appears (fresh-attempt-fails branch and the replay branch's cached-error check).
- `settings.md` §3.11's Approved UI is unaffected by this loosening — the `[ Generar otra ]` button stays gated to expired rows only there; only the backend function's own precondition changed.
- `peek_invitation`/`_generate_invitation_token` untouched, as scoped.

**`store.tsx` alignment**: `createInvitation`/`regenerateInvitation`'s `StoreValue` signatures and implementations corrected from `token: string` to `token: string | null` (a replay now genuinely returns no token — the earlier assumption that a replay always carries a displayable token was invalidated by this fix). Doc comments on both updated to state the `token: null` replay case explicitly and name `regenerateInvitation` as the fallback a caller must use to obtain a fresh, displayable token in that case. `TeamScreen.tsx`/`InvitationFlow.tsx` untouched (separate `ui-designer` dispatch, per Sequencing step 3 below) — their pre-existing compile errors are unrelated to this null-handling change (wrong call arity/a stale `phone` field reference, both predating this fix).

`npm run build` confirmed clean except the same pre-existing, expected `TeamScreen.tsx`/`InvitationFlow.tsx` errors.

## What this is

Stage 7 Backend Integration is otherwise complete. This is the one real, named follow-up: make Team Invitations (`settings.md` §2.7 "Tu equipo") actually work end to end against the real backend, per Product Owner direction, 2026-09-14.

## `architect`'s design (2026-09-14, full detail in the agent's own report, not restated here)

- **A real gap the original scoping missed: `accept_invitation` alone isn't sufficient.** RFC 0013 §2 requires resolving a pending Invitation by token *before* authentication runs — but `accept_invitation` requires `auth.uid()` as its first check and atomically consumes the row in the same call. A new **`peek_invitation(p_token)`** RPC is required — read-only, callable by `anon` and `authenticated`, never mutates status. This is also the real enumeration-attack surface RFC 0013 §4/§7's "rate-limit the token-lookup endpoint" note refers to — IP rate-limiting itself is a Supabase API-gateway/Edge-Function concern, not designed here, same as RFC 0013 already deferred it.
- **`create_invitation(business_id, idempotency_key, target_hint?)`** — OWNER-only, Paid-tier-gated (re-checked server-side), idempotency-keyed. **Server generates the raw token and returns it once** — a deliberate call `architect` made after finding RFC 0013 is genuinely silent on *where* generation happens (only that it's stored hashed): keeps the hashing algorithm defined in exactly one place, matches every other RPC's own "server mints the identifier" precedent, and doesn't newly expose the token any more than the very next hop (handing it to a share sheet) already does under RFC 0013's own accepted risk posture.
- **`regenerate_invitation(invitation_id, idempotency_key)`** — its own RPC (not folded into `create_invitation`, different authorization/precondition shape), in-place mutation matching `settings.md` §4's own "Generar otra mutates in place" resolution.
- **`_generate_invitation_token()`** — shared private helper (256-bit random, `sha256`/hex, 24h expiry per `settings.md` §2.7a) used by both.
- **Client `Invitation` type changes**: `phone` removed; no raw-token field ever stored client-side (matches the server never persisting one — the raw token is an ephemeral return value only, held in component state just long enough for the one-time display); `expiresAt`/`targetHint`/`acceptedByUserId` added; `status` narrows to `pending|accepted|revoked` — `expired` becomes a read-time derivation, never stored.
- **A genuinely blocking gap, not just a UI rewrite**: `authentication.md` §2.2a/§3.10–§3.13a is *still phone-scoped* and was already explicitly flagged stale in `product/02-ux/CLAUDE.md`'s own status line months ago ("confirmed not to compose cleanly... routed as a separate, not-yet-started follow-on"). RFC 0013 §2 gives the acceptance *mechanism* but explicitly defers the exact pre-auth offer screen / token-threading-through-auth UX to `ux-designer` — no Approved spec for this currently exists anywhere. `InvitationFlow.tsx`/`TeamScreen.tsx` cannot be correctly rebuilt until this lands.
- **A governance point**: per `product/02c-high-fidelity-prototype/CLAUDE.md`, `ui-designer` is the sole writer to this folder — `builder`'s own charter is "preserve the approved interaction model, in place," but the currently-*built* interaction model (phone-based) isn't the currently-*Approved* one (`settings.md` was reworked 2026-09-13). The UI half of this fix belongs to `ui-designer` via the standard Migration Workflow, not `builder` alone.
- **No URL/path routing exists anywhere in this prototype today** — `AppRouter.tsx` is entirely state-driven. Supporting `nahui.app/invite/<token>` (RFC 0013 §2's own route shape) needs a minimal `window.location.pathname` read — not a full routing library (`architecture-principles.md` #5 restraint) — Main's own call, made below, not deferred further.
- **Two adjacent, same-shaped gaps found, not in scope for this pass, named so they aren't lost**: `settings.md` §3.12d "Cancelar invitación" has no real RPC either (`cancel_invitation`, same pattern as the three above); `revokeMembership` in `store.tsx` is also still 100% local-mock despite a real, already-working `revoke_membership` RPC existing since Phase 0.

## Sequencing

1. **Backend + client store wiring** (independent of the UX spec gap) — `create_invitation`/`peek_invitation`/`regenerate_invitation` RPCs, `store.tsx` wiring, minimal path-based routing for `/invite/<token>`. Dispatched to `builder`. **Done, 2026-09-14** — see above.
2. **`authentication.md` pre-auth acceptance-flow amendment** (the real blocking gap for the UI rebuild) — dispatched to `ux-designer`. **Done, 2026-09-14** — §2.0/§2.2/§2.2a/§3.9a-§3.9b/§3.10-§3.13a reworked in full against RFC 0013, folded into Approved per `product/02-ux/CLAUDE.md`'s own status line.
3. Once #2 was Approved: `ui-designer` rebuilds `TeamScreen.tsx`/`InvitationFlow.tsx` against both the reworked `settings.md` §2.7/§3.11-§3.14 and the new `authentication.md` amendment, via the standard Migration Workflow. **Done, 2026-09-14** — see "What `ui-designer` built," above.
4. Full review pipeline (`ux-critic`/`reviewer`) before commit, matching every other Stage 7 workstream's discipline. **First pass run, all findings fixed (fix round 2, above) — a fresh `ux-critic`/`reviewer` re-verification pass on the fix is the one remaining step; do not commit until that comes back clean, per Main's own explicit instruction on this dispatch.**

## `architect` Blocker fix — `accept_invitation` conflict handling (2026-09-14, closed)

`architect` diagnosed a real Blocker in the original `20260913000000_identity_persistence_layer.sql`'s `accept_invitation`: `already_member`/`membership_revoked` (RFC 0013/D64, `authentication.md` §2.2a, `store.tsx`'s `acceptInvitation`) were never actually raised. When a `BusinessMembership` row already existed for `(user_id, business_id)` — active OR revoked — the membership `insert ... on conflict (user_id, business_id) do nothing` silently no-op'd, and the fallback `select` then returned that pre-existing row's id as an ordinary success. Both distinct outcomes `store.tsx`/`InvitationFlow.tsx`'s §3.10d/§3.10e screens already expect were unreachable from the real backend.

**Fix**: `supabase/migrations/20260914040000_accept_invitation_membership_conflict_fix.sql`, deployed via `supabase db push` — on the membership-insert conflict, looks up the pre-existing row's own `status` and raises the exact exception name (`membership_revoked` if `status = 'revoked'`, `already_member` otherwise) instead of silently returning success. Both branches raise an uncaught exception, aborting the whole transaction and rolling back every write this invocation performed — including the CAS's own `status = 'accepted'` update — so the Invitation stays exactly `pending`, same mechanism `invitation_not_available`'s branch already relies on.

**No client-side change needed** — confirmed by reading `store.tsx`'s `acceptInvitation` directly: it already checks `error?.message === 'invitation_not_available' || 'already_member' || 'membership_revoked'`. `InvitationFlow.tsx`'s §3.10d/§3.10e screens (`already-member`/`revoked` step kinds) are already built and correctly routed. `tsc -b`/`npm run build` confirmed genuinely clean.

**Open item, found along the way, not fixed in this pass (out of scope for this Blocker):** the pre-existing `invitation_not_available` branch's `update idempotency_keys set result = jsonb_build_object('error', ...) ... raise exception` pattern — and its structural twin in `regenerate_invitation` (`20260914032000_invitation_token_no_raw_cache.sql`) — has the same rollback problem for a different reason: because the `raise` aborts the whole transaction, that `update` (and even the original `insert into idempotency_keys` at the top of the call) never actually persists on a failed attempt, so the "replay a cached error on retry" branch is structurally unreachable. This doesn't break correctness (a retry just redoes the check from scratch, same outcome) — only the intended "answer a retry from cache without rerunning logic" optimization silently doesn't fire for error outcomes.

## Open items, not resolved

- **New (above):** the `idempotency_keys` cached-error-replay branch (`invitation_not_available` in `accept_invitation`, and its twin in `regenerate_invitation`) is structurally unreachable — the `update` that would populate it never survives the transaction rollback the subsequent `raise exception` triggers. Correctness is unaffected (a retry just re-runs the check); only the cache-replay optimization silently doesn't fire for error outcomes.
- `cancel_invitation` RPC and real `revokeMembership` wiring — same defect class, named, not started. `TeamScreen.tsx`'s own "Cancelar invitación" (§3.12d) is wired to a new, equally local-mock `cancelInvitation` store function (added this pass, matching `revokeMembership`'s existing disclosed shape) so the UI is complete and correctly-behaving in this prototype; the real backend write for both stays this same, still-open gap.
- IP rate-limiting on `peek_invitation` — infrastructure-layer, not a Postgres-function concern, not designed here.
- `declineInvitation`/`User.declinedInvitationIds` (`store.tsx`/`types.ts`) are now genuinely dead code, not merely unused — RFC 0013 §2 eliminates the per-device decline-memory marker outright, and no screen in this rebuild calls `declineInvitation` (confirmed by grep, not assumed). Left in place rather than removed — out of this dispatch's own scope (it compiles cleanly, harms nothing at rest, and touching it wasn't necessary to close the compile errors this dispatch targeted) — flagged here as a real, named candidate for a future small cleanup pass, not silently carried forward as if it were still live.
- **Resolved, fix round 2 (above):** a `knowledge-mentor` consultation on the one-time-secret reveal + copy/share pattern (`TeamScreen.tsx`'s "Invitación lista," §3.12c) has now run, specifically on `handleCopy`'s silent clipboard-failure swallow — found and fixed (see "Additional finding," fix round 2, above). A broader Design-System-level visual/pattern review of this implementation beyond that one specific defect remains open, not yet requested.
