# Team Invitations — real backend wiring (RFC 0013 implementation gap)

**Status: Complete.** Superseded by the archive record — see `README.md`'s "Team Invitations — real token-based UI rebuild" pass-history entry for the full build detail, and `product/02-ux/CLAUDE.md`'s `authentication.md` status line for the spec-side amendment record. Reduced to this pointer per this folder's own knowledge-architecture discipline (a working file is superseded once its slice is fully Approved and complete, not kept as a duplicate long-term record).

Full history for reference: `git log --follow` on this file, or the commits `4b901ec` (backend RPCs + store wiring + `authentication.md` RFC 0013 rework) and `57ba043` (UI rebuild + fix rounds + spec-authorization gap closure).
