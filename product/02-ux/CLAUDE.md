# UX Documentation — product/02-ux

Low-fidelity, implementation-independent UX specifications for each merchant-facing
experience in Nahui. These are design specs, not UI code: no HTML/React, no
components, no colors/typography. Visual design is a separate, later pass.

Scope is the Merchant Application only, per `information-architecture.md`'s own frozen boundary — a document whose own scope excludes it from that application (e.g. a customer-facing surface like loyalty-claim, `decision-log.md` D10/D21) does not belong here regardless of pipeline stage; see `product/02-ux-loyalty/` and `decision-log.md` D38.

## Where this fits
- Upstream: `product/00-foundation/` — domain model, information architecture, and
  principles are frozen ground truth. Every doc here cites them; none of them
  restate, contradict, or re-derive foundation content.
- Downstream (React-first as of D42): an Architecture Review, then `ui-designer`
  builds the feature as a React/TypeScript living prototype
  (`product/02c-high-fidelity-prototype/`) once a doc is approved. `builder`/
  `03-build` enters only at backend-integration time, once the prototype's
  interaction model is itself approved. These docs are the handoff artifact —
  implementation-independent by design, so visual/technical decisions don't
  leak backward into them.
- Sibling: `company/backlog.md` gates what's real/in-progress vs. blocked. Don't
  design for a backlog item marked "Blocked by" or "Do not start" — flag it as an
  open question instead.

## Rule
One file per experience, named after its `information-architecture.md` nav label:
`home.md` (Hoy), `inventory.md` (Inventario), `events.md` (Eventos), `reports.md`
(Resultados). An experience not present in the frozen IA nav is not given a file
here without an Architect/RFC decision first (`product/00-foundation/decision-log.md`,
`product/99-rfc/README.md`).

## Document structure (every file, same order)

§1 also serves as the standing feature workflow's Product Definition stage (D42) — state the business objective, the acceptance criteria, and any scope boundaries explicitly whenever they're non-obvious, not just the merchant goal in isolation. No separate pre-UX artifact exists; this document's own front matter is where that lightweight definition lives.

1. Merchant goal
2. Resolution / decision logic
3. Low-fidelity wireframes
4. Interaction flow — see the expanded rule below; not a bare summary
5. Screen states
6. Minimum step count
7. Automation opportunities
8. Open questions for Architect
9. Principle justification
10. Decisions made
11. Future considerations (if any)

## §4, Interaction flow

Added 2026-08-07, after an architectural investigation into where wiring/navigation should be defined (`architect` → `ux-designer`/`ui-designer`, each independently grounded via a `knowledge-mentor` consultation — see `company/bitacora.md` and `decision-log.md` D31). Every branch point — including error states and interruptions (app backgrounded, network loss, session timeout, and similar) — resolves to exactly one of:
- **(a) a named destination**: a screen state defined in this doc's §5, or another Approved doc's screen state referenced by its canonical ID (`docname.md §x.y`) — never re-described in full, only cross-referenced; or
- **(b) an explicit "Not yet resolved" marker**, cross-referenced to the specific entry that owns it in `product-decisions.md`, `business-decisions.md`, or `architect-questions.md`.

A branch with neither is not acceptable at this fidelity. This bar applies to primary, secondary, error, and interruption branches alike.

This scope — defining every branch's destination exhaustively at this stage — is Nahui's own process decision for how `product/02-ux/` operates, not a claim about where "wireflows" universally sit in UX methodology; convention on that varies even within a single source. What matters here is what this project requires of this document, independent of the label.

**This is a navigation-definition requirement, not an implementation-wiring requirement.** §4 defines the canonical navigation and behavioral destination for every branch, in text; it does not build interactive/clickable resolution — that belongs to Medium-Fidelity in Figma (`product/02b-medium-fidelity/`), never to this doc. Figma's own reaction constraints (one destination per interactive node, clone-vs-share tradeoffs) don't exist as a concept at this stage and this rule doesn't ask §4 to anticipate them — that's `ui-designer`'s own pre-wiring caller-audit discipline (`.claude/agents/ui-designer.md`), a separate fix at a separate layer.

**Wiring originates in this definition, not in Figma.** Medium-Fidelity build realizes exactly the destinations §4/§5 already define — using build-layer techniques (e.g., node cloning to work around Figma's one-destination-per-reaction limit) is expected and does not require a new design decision. If Figma work surfaces a genuine need for a destination or behavior not already defined here, that is a spec gap: it's flagged back to `ux-designer` and recorded as a deviation in the Medium-Fidelity tracking file (`product/02b-medium-fidelity/CLAUDE.md`'s own document-structure item 4), never invented or resolved unilaterally at the Figma layer. This formalizes a rule `product/02b-medium-fidelity/CLAUDE.md` already states and this project has already lived by (see its incident log: JOURNEY2-MAJ1's fix composed already-approved states rather than inventing one; the corrected "Todo listo Variant C" exception, where a destination assumed missing turned out to already be defined in `onboarding.md` §2.4/§4) — this cross-reference makes it explicit at the Low-Fidelity side too, not just the Medium-Fidelity side.

**Shared states across diverging branches** (within a doc, or across docs — e.g. `onboarding.md` §3.5's "Creando tu negocio," reached by all three onboarding paths): reference the shared state by its canonical ID at every entry point instead of duplicating its description, and note in parentheses which flow type reaches it there (e.g., "shared by all three paths," "reached via: onboarding entry / settings reactivation"). Don't restate a shared state's content more than once.

## Rules for every doc
- Implementation-independent: layout/hierarchy/affordance only, ASCII/text
  wireframes, no color, no component names, no framework references.
- Merchant-facing copy in natural Mexican Spanish, using the merchant's own
  vocabulary (never literal translation, never engineering terms). Agent-facing
  reasoning in English. See `global-principles.md#product-language`.
- Cite `global-principles.md` and `architecture-principles.md` by name for every
  major decision — a general "this aligns with our principles" is not sufficient.
- Never modify `product/00-foundation/` from this folder. If a doc surfaces a gap
  the foundation doesn't cover, record it under that doc's "Open questions for
  Architect" section — don't resolve it unilaterally here.
- One experience is designed, reviewed, and approved before the next one starts. Once every document in one of `information-architecture.md`'s canonical journeys is Approved, that rule is complemented, not superseded, by a Horizontal Journey Review (`company/CLAUDE.md`) across the full sequence — reviewing each document on its own was never meant to substitute for reviewing the seam between already-approved ones.

## Status
- `authentication.md` — approved (2026-08-13). Phone → OTP → Owner-identity access gate preceding `onboarding.md`, implementing `company/business-decisions.md` Q14 and `decision-log.md` D44 (User/BusinessMembership). Placement confirmed in this folder per `decision-log.md` D45 (fits D13's precedent, not D38's). `brand-guardian` consultation complete — §3.7c copy revised. `ux-critic` clean (1 Major + 2 Minor, fixed/verified in one round — see `ux-critic-findings.md` AUTH-M1/AUTH-MIN1/AUTH-MIN2). `reviewer` found 1 Important (§2.2's Owner-ness causal wording implicitly re-derived, rather than cited, D44's actual invariant — fixed, restated to track D44 precisely) + 2 Suggestions (both non-blocking, logged); folded back into Approved. Two open items routed, not blocking: `architect-questions.md` Q17 (Resolved via RFC 0007/D44), `product-decisions.md` Q18 (deferred, non-blocking at pilot scale).
- `home.md` — approved; amended for `decision-log.md` D23 (Session-scoped selling mode) — full remediation cycle complete (four Major findings across three rounds, see `ux-critic-findings.md`), `reviewer` clean, folded back into Approved. Further amended for `settings.md` §2.1 (Configuración entry point) — full cycle complete (3 Major + 2 Minor across two rounds, see `ux-critic-findings.md`), `reviewer` clean (2 non-blocking documentation-hygiene findings, addressed), folded back into Approved. **Further amended for `decision-log.md` D27** (NFC capability corrected to derive from `subscriptionTier`, not kit/code activation) — 5 stale passages fixed (§2's Ready-branch check, §3.6a's capability-revoked bullet and design note, §3.6c's entry-point notes, §10's decisions bullet), all now point to `settings.md`'s "Activar plan de pago" as the real restoration mechanism. Full cycle complete (1 Blocker in `onboarding.md`'s sibling copy, 2 Major + 3 Minor across the coordinated three-document amendment, all fixed/verified), `reviewer` clean, folded back into Approved. **Further amended 2026-08-04 (HOME-Q1, Product Owner-raised):** new §3.8e ambient "Venta finalizada ✓" confirmation after a successful Finalizar Venta, previously indistinguishable from a fresh Session start or a cancelled sale. Full cycle complete, `ux-critic`/`reviewer` clean, folded back into Approved. **Further amended 2026-08-08 (`decision-log.md` D33, MVP pricing operating model):** existing dollar figures (§3.7, §3.8f) now explicitly grounded as sums of `SaleItem.pricePaid`. Part of a four-document batch with `inventory.md`/`events.md`/`reports.md`; `ux-critic` clean across both remediation rounds, no findings against this document specifically. `reviewer` clean (no Blockers, no Important findings; independently re-verified both open Minors as correctly non-blocking, plus flagged one unrelated pre-existing drift — `product/00-foundation/CLAUDE.md`'s status line, fixed separately by Main). Folded back into Approved. **Further amended 2026-08-08 (Product Owner decision, "Define lo que vendes" moved into Onboarding):** §2 step 3's cold-start test corrected from "Product ever registered" to "`available` InventoryUnit exists." Two rounds — round 1 missed a stale copy of the old test in §4's own wiring section; round 2 corrected it. `ux-critic`/`reviewer` both clean, folded back into Approved. **Further amended 2026-08-08 (Product Owner decision, Business Identity captured at Onboarding):** §3.8f's receipt now shows the merchant's own captured identity (`Business.name`, and her own logo if set) instead of "(marca Nahui)" — a deliberate brand-facing decision, reasoned in full in this document's own §10. `ux-critic`/`reviewer` clean (`reviewer`'s 2 Important findings were `onboarding.md`-side documentation gaps, not scoped to this document — see that entry below). Folded back into Approved. **Further amended 2026-08-09 (`decision-log.md` D40 — `loyaltyEnabled` retired):** §3.8f's future-registration placeholder now renders only on a Paid-tier receipt; a Free-tier receipt stops at business identity (three elements, not four) — a false "someday" promise otherwise, since Free never gets Frequent Customers under the corrected model. `ux-critic` round 1 found 1 Major (§3.6c still listed "clientes frecuentes" as something Configuración manages/checks-or-changes, a stale toggle-implying residue the cascade missed) — fixed, verification clean. Ready for `reviewer`. **Further amended 2026-08-13 (Product Owner decision — §3.7's ongoing header redefined context-scoped, not Session-scoped, closing a `merchant-user-tester` re-walk finding):** "Hoy: $X · N ventas" becomes a running sum across every Session sharing today's `eventId`, not only the active Session's own contribution; "Venta actual," the close-confirmation dialog, and the closing-summary screen stay explicitly Session-scoped. Folded back into Approved. **Further amended 2026-08-13 (Product Owner decision — Selling-Session-close action renamed "Cerrar sesión" → "Cerrar jornada de venta"):** copy-only, the interlock itself unaffected. Folded back into Approved. **Further amended 2026-08-14 (Product Owner-raised — "Cerrar jornada de venta" discoverability):** during an active Session, the header's "⋯" sheet is replaced by two direct, always-visible affordances — a gear icon (⚙) routing straight into Configuración, and a labeled `[ Cerrar jornada de venta ]` button — retiring §3.7a for this state only; non-Session states keep the unchanged "⋯" sheet. `settings.md` §2.1 receives the matching correction. `ux-critic` clean (4 Minor + 1 Suggestion, all fixed), `reviewer` clean (2 findings, both fixed). Folded back into Approved.
- `onboarding.md` / `settings.md` — **both amended for `decision-log.md` D27** (see their own entries below) — full coordinated three-document remediation cycle complete for both (`ux-critic`: one Blocker in `onboarding.md`'s milestone copy plus two Major/three Minor across the two docs, all fixed and verified; `reviewer`: zero Blockers, one Important documentation-drift finding, fixed), folded back into Approved. **Corrected 2026-08-06** — this line previously read "full remediation cycle pending for both" after the cycle had already completed; a stale status left behind, caught during an `architect` build-readiness review. See `product/02-ux/onboarding.md`/`settings.md`'s own status headers for the full remediation record. **Further amended 2026-08-08 (`decision-log.md` D33, Product Owner decision — "Define lo que vendes" moved into Onboarding):** a new required step (§2.2a, §3.5b–§3.5e) captures the merchant's initial Selling Groups on both real paths, reusing Inventory's own Product-creation write path. Two remediation rounds (2 Major found and closed — a stale `home.md` §4 branch label, a new Inventario "sin registrar" vs. "sold out" ambiguity). `ux-critic`/`reviewer` both clean (reviewer's two Important findings, a missing findings-log entry and two stale `inventory.md` uniqueness claims, both fixed by Main). Folded back into Approved. **Further amended 2026-08-08 (Product Owner decision, Business Identity captured at Onboarding):** a new required step (§2.2b, §3.9–§3.10a) captures `Business.name` (required) plus optional logo/description on both real paths, positioned before §2.2a's "Define lo que vendes" step; §1's stale exclusion rationale and §2.1's resume-check state machine were both corrected in the same pass. `ux-critic` round 1 found 1 Major (ONB-ID-MAJ1: intro copy falsely claimed customers would see Descripción, contradicted by `home.md` §3.8f's actual rendering) + 2 Minor + 1 Suggestion; fixed and independently re-verified clean (0 Blockers, 0 Major, 1 non-blocking documentation-precision Suggestion, applied). `reviewer` found 2 further Important findings, both documentation-persistence gaps (no `decision-log.md` entry for the three new `Business` fields; this cycle missing from `ux-critic-findings.md`) — both closed by Main directly: `decision-log.md` D36 added, `domain-model.md`/`ubiquitous-language.md` updated, `ux-critic-findings.md` entry added. Folded back into Approved. **Built in Medium-Fidelity 2026-08-09, `ux-critic`/`reviewer` clean — see `product/02b-medium-fidelity/onboarding.md`.** **Further amended 2026-08-09 (`decision-log.md` D40 — `loyaltyEnabled` retired):** §2.2's capability table drops its `loyaltyEnabled` column; every reference to it as a value the demo/real paths set is corrected — Frequent Customers is now a pure, automatic consequence of `subscriptionTier`. `ux-critic` verified clean — no findings against this document specifically. Ready for `reviewer`.
- `inventory.md` — approved; amended for D23 (cross-reference/terminology only) — zero findings, `reviewer` clean, folded back into Approved. **Further amended 2026-08-04 (Product Owner-raised, INV-Q1):** Cantidad now defaults to 1 (was blank) with an explicit tap-affordance requirement (bracketed per the doc's own `[ ]` = tappable convention) and a "revisa antes de guardar" marker on the unreviewed default, carrying into the §3.7 committed-lines list. Full cycle complete, `ux-critic`/`reviewer` clean, folded back into Approved. **Further amended 2026-08-07 (Product Owner-directed, pending-tags task-priority refinement):** in the pending-tags Catalog-view state (§3.5/§3.17), "Continuar etiquetando" (renamed from "Terminar de etiquetar") is now the primary action, "Registrar mercancía" explicitly secondary in that one state only — same position/destination/gating otherwise. Grounded in a joint `architect`/`ux-designer` investigation (Foundation confirmed silent on priority; no business-rule conflict). Full cycle complete, `ux-critic` clean (2 Suggestions logged), `reviewer` clean (1 Important documentation-hygiene fix applied), folded back into Approved. **Further amended 2026-08-08 (`decision-log.md` D33, MVP pricing operating model):** `Product.defaultPrice` capture added at Product creation (new §3.8a) plus a Catalog-row price-edit affordance (new §3.4a). Two remediation rounds — round 1's wiring-completeness gap (§4/§5/§6 never threading the two new states) closed in round 2; `ux-critic` verified clean (one Minor open, `INV-D33-MIN-A`, non-blocking). `reviewer` clean (no Blockers, no Important findings; independently re-verified both open Minors as correctly non-blocking, plus flagged one unrelated pre-existing drift — `product/00-foundation/CLAUDE.md`'s status line, fixed separately by Main). Folded back into Approved. **Further amended 2026-08-08 (Product Owner decision, "Define lo que vendes" moved into Onboarding):** a zero-`disponibles` Catalog row now distinguishes "sin registrar" (never stocked) from "0 disponibles" (sold out) — resolves a first-impression risk for a merchant fresh out of Onboarding. `ux-critic`/`reviewer` both clean, folded back into Approved.
- `events.md` — done, approved (one open item, Q6, doesn't block approval). **Further amended 2026-08-04 (Product Owner-raised, EVT-Q1/EVT-Q2):** Empieza now defaults to hoy (was blank), Guardar evento's gate narrowed to Lugar + Tipo, and the D17 overlap-check warning's visibility (not computation) deferred until her first engagement with the form. Full cycle complete, `ux-critic`/`reviewer` clean, folded back into Approved. **Further amended 2026-08-08 (`decision-log.md` D33, MVP pricing operating model):** `Event.bazaarCost` (optional) added to Nuevo Evento, plus a new per-Product Price Override mechanism ("Ajustar precios," new §3.19/§3.20). Two remediation rounds — round 1 found "Ajustar precios" reachable during active selling, undercutting D33's own offline-planning premise; round 2 applied the Product Owner's ruling that it's strictly an Event-*planning* capability, removed entirely once an Event is `active`. `ux-critic` verified clean (one Minor open, `EVT-D33-MIN-A`, non-blocking). `reviewer` clean (no Blockers, no Important findings; independently re-verified both open Minors as correctly non-blocking, plus flagged one unrelated pre-existing drift — `product/00-foundation/CLAUDE.md`'s status line, fixed separately by Main). Folded back into Approved.
- `reports.md` — done, approved (Q8, Q9 resolved and applied — "Tus clientes" and "Rendimiento por bazar" are both real, reviewed specs; Q10 resolved — `reviewed` stays dormant, no UX design needed, `decision-log.md` D18). **Further amended 2026-08-08 (`decision-log.md` D33, MVP pricing operating model):** every dollar figure now explicitly grounded as a sum of `SaleItem.pricePaid`; `bazaarCost` explicitly never read or netted against anything here. Part of the same four-document batch as `inventory.md`/`events.md`/`home.md`; `ux-critic` clean, no findings against this document specifically. `reviewer` clean (no Blockers, no Important findings; independently re-verified both open Minors as correctly non-blocking, plus flagged one unrelated pre-existing drift — `product/00-foundation/CLAUDE.md`'s status line, fixed separately by Main). Folded back into Approved. **Further amended 2026-08-08 (`decision-log.md` D34 — Customer Segmentation visibility gate corrected):** "Tus clientes" now gates on `subscriptionTier=paid` alone, matching "Rendimiento por bazar"; `loyaltyEnabled` narrowed to gating Claim collection only, never visibility. §3.6's two previously-separate `loyaltyEnabled`-dependent states collapse into one (§3.13). `ux-critic` found 2 Major (a stale joint-gate statement left in the "Out of scope" preamble; the collapsed §3.13 state dropping all pointer language even where a case-agnostic pointer would help) + 1 Minor, all fixed and re-verified clean. `reviewer` clean (no Blockers; 3 Important documentation-persistence findings — this status line, a missing `ux-critic-findings.md` entry, D34's own Applied paragraph omitting `domain-model.md` — all fixed by Main directly). Folded back into Approved. **Further amended 2026-08-08 (`decision-log.md` D35/D37 — Loyalty Participation view added):** new §3.15–§3.18 plus additive appends to §3.12/§3.13 give Ana a per-customer view of loyalty progress, reading D35's seven-field Loyalty Participation Record allowlist exactly. `ux-critic` two rounds — round 1: self-contradictory Button-class naming (fixed, reworded to implementation-independent terms per `reviewer`'s FC-S1 call), a confirm-screen reusing a reversible-action template without disclosing irreversibility (fixed); round 2 verification clean. `reviewer` clean (0 Blockers). The "Confirmar recompensa entregada" write action was designed against `product/99-rfc/0005-reward-cycle-confirmation-write-edge.md` while still Proposed — now Accepted, `decision-log.md` D39, no amendments; the Product Owner explicitly accepted the residual mis-tap risk and deferred any correction mechanism until real evidence warrants one. Folded back into Approved, no longer speculative. **Further amended 2026-08-09 (`decision-log.md` D40 — `loyaltyEnabled` retired, Frequent Customers unified as a `subscriptionTier=paid`-gated capability):** every remaining reference to `loyaltyEnabled` as a live merchant decision, or pointing to `settings.md`'s now-retired "Activar clientes frecuentes" action, is corrected — §1/§8 preamble, §2, §3.4/§3.5/§3.6, §3.12/§3.13 (zero-Claims empty state simplified, Configuración pointer removed entirely), §4-§7, §9, §10. "Tus clientes"'s own visibility gate is unchanged (already `subscriptionTier=paid` alone, D34). `ux-critic` verified clean — no findings against this document specifically (the one Major found this round was scoped to `settings.md`/`home.md`). Ready for `reviewer`.

- `onboarding.md` — approved; first document written and reviewed from a blank slate (one Blocker, one Major, four Minors — all fixed and verified, see `ux-critic-findings.md`). **Amended for `decision-log.md` D27**: the "Activar kit NFC" path (activation-code entry/validating/invalid-code mechanism) is retired entirely, replaced by "Activar plan de pago" — a bare payment-confirmation path granting `subscriptionTier=paid` (and therefore `nfc`, derived per D27). `defaultSellingMode` now written as `buttons` unconditionally for both real paths, deferred to `settings.md`'s new self-service control. Full remediation cycle pending. **Further amended 2026-08-15 (`frontend-design` audit, proposal #4):** new §3.9b — a live, non-interactive identity preview on "Tu negocio" (§3.9/§3.9a), grounded via a `knowledge-mentor` consultation (no prior live/as-you-type precedent in this document family). Pending `ux-critic`/`reviewer` review before folding back into Approved.

- `settings.md` — approved; six merchant-facing actions (three self-service capabilities × two directions each, `decision-log.md` D25), a reusable pending-change indicator, a way to cancel a pending change before it lands. Full remediation cycle complete across three rounds (see `ux-critic-findings.md`) — one Blocker (SET-B1: NFC activation losing its required kit-confirmation step) and five Major findings, including a self-inflicted regression from a restructuring pass that briefly reverted an already-fixed entry-point issue and reintroduced a copy issue. `reviewer` also caught a real Foundation-consistency Blocker (segmentation copy not jointly gated on both `subscriptionTier=paid` and `loyaltyEnabled=true`, per D22) — fixed, verified clean. One narrower open item (Q11 — the specific per-transition immediate/deferred timing rule) doesn't block approval — the UI supports either shape per capability, with today's assignment stated as illustrative. **Amended for `decision-log.md` D27**: the dedicated "Activar venta con tags" activation-code path (SET-B1's own mechanism) is retired entirely — `nfc` is no longer independently self-service-toggleable, it changes only as an automatic consequence of the `subscriptionTier` actions. A new `defaultSellingMode` control ("Cambiar a vender con tags"/"...con botones") is added, previously out of scope. Full remediation cycle pending. **Further amended 2026-08-08 (`decision-log.md` D34 — Customer Segmentation visibility gate corrected):** "Activar plan de pago," "Activar clientes frecuentes," and "Desactivar clientes frecuentes" copy corrected — none frame `loyaltyEnabled` as a joint precondition, alongside `subscriptionTier=paid`, for Resultados' "Tus clientes" to show anything. `ux-critic`/`reviewer` both clean (reviewer's Important findings were `reports.md`-side documentation gaps, not scoped to this document — see that entry above). Folded back into Approved. **Further amended 2026-08-09 (`decision-log.md` D40 — `loyaltyEnabled` retired):** "Activar clientes frecuentes"/"Desactivar clientes frecuentes" (§3.4) are removed entirely, not merely re-copied — narrows Configuración from six actions to four. "Activar plan de pago"/"Volver al plan gratis" copy now discloses the fuller consequence (Frequent Customers as a whole, not only Resultados visibility). §1, Scope, §2.2's table, §3.3a/§3.6's wireframes, §4-§7, §9, §10, §11 corrected. `ux-critic` round 1 found 1 Major (§2.1 still listed "clientes frecuentes" as something Configuración manages/checks-or-changes, a stale toggle-implying residue the cascade missed) — fixed, verification clean (one further non-blocking Minor caught and fixed in the same pass: §1's stale "six actions"/§2.3 citation). `reviewer` clean. Folded back into Approved. **Further amended 2026-08-14 (`decision-log.md` D46 — tag-assignment auto-entry gates on merchant intent, not mere capability, corrected same day per D46's own Addendum — architect ruling on a dependency-cycle finding):** new §2.6 specifies "Cambiar a vender con tags" as writing `defaultSellingMode` and handing off a bare entry marker (never a queried Inventory fact) into `inventory.md` §2's own new highest-priority trigger condition — never reading Inventory-owned state directly, avoiding an Identity→Inventory→Identity dependency cycle `architecture-principles.md` #6 forbids. `reviewer` clean (1 Suggestion, applied). `ux-critic` found 1 Major (SET-INV-D46-MAJ1 — the already-fully-tagged outcome silently landed her on a different nav tab with no acknowledgment) — fixed (new one-time banner at `inventory.md` §3.4) and re-verified clean. Folded back into Approved. **Further amended 2026-08-14 (Product Owner-raised, matching `home.md`'s own "Cerrar jornada de venta" discoverability fix):** during an active Session, §2.1's entry-point icon becomes a gear ("⚙") routing directly into Configuración, with no intermediate sheet — "Cerrar jornada de venta" no longer shares this trigger at all, having moved to its own direct header button specified in `home.md` §3.7. Outside an active Session, the "⋯" icon and its sheet (§3.3) are unchanged. `ux-critic` clean (4 Minor + 1 Suggestion, all fixed), `reviewer` clean (2 findings, both fixed). Folded back into Approved.

All six designed experiences (Hoy, Inventario, Eventos, Resultados, Onboarding, Settings) are done and approved — the Low-Fidelity UX phase is complete.

- `demo-mode.md` — **not one of the six Merchant Application experiences above; validation-campaign-only infrastructure, co-located here for pipeline/deploy convenience.** Product Owner-approved in concept and wireframe (2026-08-15); `brand-guardian` reviewed and fixed the welcome copy; `architect` ruled on its placement (`decision-log.md` D48 — fits neither D13/D45's class nor D38's, a third category: no code path in a real, shipped build, its own §1 explicitly disclaims Merchant Application capability status). Not yet through `ux-critic`/`reviewer` or Architecture Review.

## UX Critic Findings

`ux-critic-findings.md` is the standing log of UX-quality findings from `ux-critic`'s review of any spec in this folder — usability, missing states, complexity, consistency. Different in kind from the three Decision Ownership logs below: findings here are actionable directly by `ux-designer`, not questions needing an owner's decision. Check it before assuming a UX weakness you've found is new, and before treating any "Approved" doc's status as meaning its UX quality has been checked — approval before this log existed only ever meant `reviewer`'s Foundation-consistency pass.

## Open Questions — classified by Decision Ownership

Domain ambiguities the Foundation doesn't resolve are split into three logs by who needs to decide them, per the Decision Ownership policy in `company/CLAUDE.md`:

- `architect-questions.md` — resolvable by Architect from the existing Foundation alone.
- `product-decisions.md` (this folder) — needs a Product Owner call on product behavior/UX/capabilities/feature scope.
- `company/business-decisions.md` — needs a Business Decision (pricing/commercialization/legal/compliance/operations).

Check all three before assuming a gap you've found is new. When escalating a new one, classify it by ownership first (per `company/CLAUDE.md`'s Decision Ownership section) and log it in the matching file — don't resolve it yourself, and don't default everything into `architect-questions.md` the way earlier entries did before this policy existed.
