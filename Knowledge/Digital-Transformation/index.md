# Digital Transformation — Knowledge Index

Points at an external, project-curated source. Does not reorganize, copy, or
mirror its contents — the source stays exactly where it lives, and whatever
inspects it must recursively read the actual folder structure rather than
assume a standard shape (session decks at the root, plus a subfolder of
already-submitted assignments, not a uniform layout).

## Source

- **Root directory:** `/Users/boofalcon/Library/CloudStorage/OneDrive-Personal/EGADE/TransformacionDigital`
- **Type:** local filesystem, not part of this repository
- **Content:** EGADE graduate course "Transformación Digital" — session
  slide decks (frameworks: MVE, MVP, Digital Value Capture, Viability,
  Governance, and others per session) framed for an IT-background audience
  building a native-AI/agentic enterprise. `AssignmentsDelivered/` holds the
  Product Owner's own already-submitted coursework (Friction Map, Pitch,
  Demo) — reference for expected format/rigor, distinct from the lecture
  material itself; don't treat submitted-assignment content as course
  doctrine the way the session decks are.

## Notes

- Second entry in `Knowledge/`, same pattern as `Knowledge/UX-UI/`.
- Nahui is this course's own primary case study — when a coursework
  deliverable is requested, `product/00-foundation/`/`company/` stays the
  source of truth for Nahui itself; this domain supplies the academic
  frameworks/concepts used to analyze it, per the Learning Resources tier
  (`company/CLAUDE.md`'s Knowledge Mentor section) — never the reverse.
- This index records *where* the source lives, not its contents — nothing
  here should restate or summarize the material itself.
- **Deliverables produced for this course are written in Spanish** (Product
  Owner instruction, 2026-08-15) — the same UI-facing-language convention
  `global-principles.md#product-language` already applies to anything Ana
  sees, extended here to anything the professor sees. Agent-facing
  reasoning about the deliverable (this index, any working/planning notes)
  stays English as usual; only the deliverable itself is Spanish.
