# UX-UI — Knowledge Index

Points at an external, project-curated source. Does not reorganize, copy, or
mirror its contents — the source stays exactly where it lives, and whatever
inspects it must recursively read the actual folder structure rather than
assume a standard shape (this source is week-by-week course material with an
inconsistent number of files per week, not a uniform layout).

## Source

- **Root directory:** `/Users/boofalcon/Library/CloudStorage/OneDrive-Personal/UX_UI`
- **Type:** local filesystem, not part of this repository
- **Content:** UX/UI research course materials (weekly folders, PDFs, slide decks)

## Notes

- This is the first entry in `Knowledge/`. Additional course or source
  directories will be added here over time, each under its own subfolder
  (e.g. `Knowledge/<source-name>/index.md`), as they're identified as useful.
- This index records *where* the source lives, not its contents — nothing
  here should restate or summarize the material itself.
